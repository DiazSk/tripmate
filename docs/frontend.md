# Frontend — Feature Tracker

Covers `src/app/**/page.tsx`, `src/app/globals.css`, `src/app/layout.tsx`,
`src/components/**`. See [`project-crux.md`](./project-crux.md) for how these
tables are maintained.

## Features

| Feature | Status | Since | Developer | Notes |
|---|---|---|---|---|
| Every shipped image is now attributable, and the wizard stopped cutting to a different continent | Active | 2026-09-09 | Claude | Follow-on from the beat photographs. Three things, and the middle one was a real defect nobody had noticed. **(1) The footer banner had no provenance.** `footer_scenic.webp` was Santa Maddalena in Val di Funes — a fine picture whose only reference anywhere was the `src` in `SiteFooter`, so it could not be re-cropped, re-encoded or shown to be licensed. Replaced with the Engadin from above (Pexels 34451716, Oskar Gross, 4024×3018 → 2800×653 WebP, served at **150KB** against the outgoing 156KB). Chosen because it *recedes* — a page about going somewhere should close on distance — and because at 4:3 the 4.29:1 banner is a band cut from a landscape rather than a squeeze. **Deliberately not the Tuscan dawn that beat it on looks**: Vernazza already opens `ImageRow` and Val d'Orcia is the first plan card, and a third Italian landscape on a page claiming "anywhere you can name" argues the other way. **(2) `SceneBackdrop` had been contradicting its own documentation since the hero became a film.** Its doc states the rule plainly — *"It is the same photograph, not a matching one… an unrelated image here reads as a scene change and quietly costs the flow its continuity"* — and it was rendering a street in Seattle while the hero showed a canyon in Jordan. The comment stayed true while the hero changed underneath it. It now renders `HERO_SEQUENCE.poster`, which is frame one of the film and therefore literally the image the traveller was just looking at; verified by clicking a map marker through to `?step=basics` and reading the backdrop's `src`. Three things fell out: the note about the file being "in cache from the landing anyway" became true again rather than aspirational; **`heroScene.ts` and its 652KB photograph had no other caller and are deleted**, which also retires the last image in the repo with a stated licence but no photo ID; and **`.hero-photo` went with them** — its `object-position: 54% 44%` (and `26% 56%` below 48rem) was measured against a terracotta wall in a frame nothing renders any more, and a crop tuned for a retired picture is worse than no crop because it silently art-directs the wrong one. Both consumers are now plain centred `object-cover`. **(3) 440KB of unused images deleted** — `cinque-terre-sunset`, `san-francisco`, `taj-mahal`, `tokyo`, `hilltop-monument-illustration`, `earth-background`, none referenced anywhere, all shipping from `public/` on every deploy, which this repo already has a recorded lesson about. **Net: every image in `public/scenes` is now referenced by exactly one module and credited in it, and nothing unused ships.** Also fixed three comments that had gone false: `Hero.tsx` claiming `.hero-photo` was "untouched and still used by `SceneBackdrop`", `HomeView.tsx` contrasting against "the hero-photo bands", and `ImageRow`'s placeholder gradients described as "the two net-new scene hues" — tokens that stopped existing when `.blue-hour-scene` was emptied to "one timing token, no colour". Verified at 1440 and 375 on both the footer and the wizard step: images load, no console errors, 718 tests, verify-build 48 chunks, lint and typecheck clean |
| The four landing beat photographs, re-sourced and finally attributable | Active | 2026-09-09 | Claude | `ImageRow`'s four pictures were the weakest imagery on the site and **none of them had a recorded origin** — the only reference to `dusk-skyline.jpg`, `budget.jpg`, `weather.jpg` or `tiers.jpg` anywhere in the repo was the `src` string in `sceneBeats.ts`, so no one could re-crop them, re-encode them, or answer whether they were licensed. That is the exact failure `planExamples.ts` records learning the expensive way at its own `photo` field. They were also 2400px JPEGs totalling **2.3MB** against the curated plan-card set's 1200px WebP at 124–164KB. **Each frame now states its beat instead of gesturing at its topic.** The Blue Hour was a near-black city skyline, which argues against its own caption — the claim is that for ~20 minutes the town and the sky are the same brightness, so a sky already gone black is the wrong photograph; it is now Vernazza from above with the lights on and the sky still blue. Real Prices says *itemized*, so the picture had to read as counted rather than abundant: spice barrels ranked in a market window, replacing a cluttered night stall. Live Weather had weather as incidental background to a wet street; it is now storm cloud over a forested ridge, weather as the subject. Three Ways to Travel was a generic palm sunset saying nothing about three tiers — now a room opening onto its own view, and **deliberately not the far prettier riad courtyard shortlisted against it**, because a tier photograph must not take a side and PRODUCT.md's one standing tension is that luxury imagery on a budget-honest product reads as a mismatch. What a tier buys is the view you wake up to, which is true at all three. All four are Pexels (licence permits commercial use without attribution, so nothing renders the credits — they exist so the next person can find the original): 4254555 Grafixart_photo Samir BELHAMRA, 10224325 Ayşe BOLAT, 1699022 eberhard grossgasteiger, 34962016 Bilge. Processed with `sharp` to **1200×1309** — 11:12 to match the frame, at the 1200px floor `planExamples.ts` derives from `sizes` resolving to 100vw on a phone (430px at 3x wants ~1290 real pixels) — cropped `cover` from centre, which is the crop each was judged on via a contact sheet rendered at the real aspect ratio rather than from thumbnails. **2.3MB → 649KB**, and the four orphaned JPEGs are deleted rather than left to ship from `public/`. Verified at 1440 and 375: all four load, no console errors, `next/image` serving w=750 for a 303px frame at dpr 2. Sourced from Pexels rather than the Pinterest pin that prompted this — pins are re-hosted third-party work with unclear rights, and this repo records provenance for exactly that reason |
| The map beat says "anywhere" instead of "these twelve" | Active | 2026-09-09 | Claude | `DestinationMap`'s heading is "Anywhere you can name" and its standfirst says there is no list of supported cities to be missing from — and the drawing under it was **exactly twelve pins**, which states the opposite. Three things were wrong beyond the look. `featured: true` flagged Kyoto, Lisbon, Reykjavík and Marrakesh, which have **zero overlap** with the four plans the site actually sells. All twelve wore an accent radial glow while doing nothing, against the One Meaning Rule's "jade acts" — and zero-offset coloured halos are the treatment DESIGN.md already records removing from the app's own day badges. And the file's own comment called the dots "the accent's one real appearance on the page", which was false in two directions: `SectionOpener`'s asterisk is jade on every band, and 24 static circles are not an appearance of *action*. **The fix makes the density carry the claim and spends the accent on what can be acted on.** The land is filled with one SVG `<pattern>` — 14-unit cell, a 0.028 wash plus an `r=1.35` mark at 0.115 — used as the existing Natural Earth path's `fill`, which clips to the continents for free. Hundreds of faint marks across every landmass is the claim stated in the artifact rather than only in the heading, and it needs **no generated geometry**: the costed alternative was rasterising the path offline to sample a grid, meaning a new build script and ~12KB of committed coordinates to do what `<pattern>` does in five lines. The twelve dots become **four**, read straight from `planExamples.ts` with `lat`/`lon` added to `PlanExample` so the coordinates live with the plan and the `featured` fiction cannot come back — and each one is now a **control**, opening the wizard prefilled through the same `toPrefill` path `FeaturedPlans` uses one band above. Verified end to end: clicking Wadi Rum lands `?step=basics` with destination `Wadi Rum, Jordan`, `2027-03-20`→`2027-03-26` and budget `1200`. Accent-bearing elements in the band go **24 → 4**, all four buttons with `aria-label` names (measured in the page, counting `color-mix()`'s resolved `oklab()` and the `::after` core, which a naive `rgb()` scan misses). Hairline ring plus a 4px core, place name in the label step, price in **gold tabular mono** — gold because it counts, mono because four figures read across a map are a column, which is the split `.price-display` already documents. **The markers are HTML over the SVG, not `<text>` inside it**, because SVG scales with the viewBox and this one spans 335px on a phone to 1760px at the band's 110rem ceiling — a 5× range with no `vector-effect` for font size. Positioned HTML gets a fixed type step, real `:focus-visible`, and `.focus-ring` for nothing. Centring is negative `margin`, not `translate`, and that is load-bearing: GSAP writes the whole `transform` property, so a layout transform is dropped on the tween's first frame. **The marks are `lg`-and-up in full, measured rather than assumed**: three of the four plans are in western Eurasia, and at 375px they land inside a 22×17px box where three rings are a smudge and three tap targets 13px apart fail WCAG 2.2's target spacing outright. `display: none`, not a visual hide, so they leave the tab order too; below `lg` the band is the stipple field and the paragraph, which is still strictly more than twelve haloed pins, and nothing actionable is lost because all four plans are full cards with their own buttons one band above. Label anchors are hand-placed — left, up, right, right — because with four marks a solver is more code than the answer; closest approach between label boxes measured at **48px at 1440 and 62px at 2560**, none intersecting. Motion is one `ScrollTrigger` timeline: field opacity, then the marks staggered on **`expo.out`**, which deletes the app's only `back.out(1.7)` — undocumented, and an overshoot two inches under a heading revealing on `expo.out`. Deliberately **not** moved to the `--story` timeline every other scroll-driven thing on this page uses: `--story` offsets are absolute scroll lengths from the scroller's origin and this band is fifth of six, so its range would shift whenever a band above it changed height. Nothing loops, because the fixed navbar's `backdrop-filter` is on screen at every scroll position — the same reason `.hero-light` is paused while the film runs; verified no `infinite` keyframe was added and the tween settles to `opacity: 1` / identity transform. Under `prefers-reduced-motion` no tween is created at all and every element sits at rest with GSAP having written nothing. The `.destination-*` rules are **unlayered in `globals.css`** on purpose: `.destination-dot` existed for a year as a GSAP selector only, so the moment it became a control a `hover:` or `ring-*` utility here would be emitted inside `@layer utilities` and lose to any unlayered rule on the same property. 718 tests, `verify-build` 48 chunks, lint and typecheck clean, no console errors, walked at 390 / 1440 / 2560. **Revised the same day — four marks read as half-finished, and that was fair.** The first cut put only the four plans on the field, which is the twelve-pin defect seen from the other side: a world map with four points on it looks unfinished whatever the field says. **Quantity is what resolves it, not tiering.** `mapPlaces.ts` adds ~90 well-known destinations across every inhabited continent as small identical neutral marks — `--foreground` at 0.42, one radius for all of them, no label, no hover, no `aria`, drawn as `<circle>`s in the same SVG because they need neither a fixed type step nor a focus ring. Twelve is a *countable* set and reads as an inventory; ninety is not counted and reads as "the world is full of places", which is the claim. **Nothing in the layer is tiered** — a "major" subset drawn larger would be `featured: true` under a new name, and the four things that genuinely differ differ by *being controls*. The alpha is the whole design of the layer: it has to sit clearly above the stipple under it (a 0.115 mark on a 0.028 wash) and clearly below the jade plan markers over it, or the map has two competing sets of points and no answer to which one you press. Also fixes the weakest part of the first cut — the layer renders at every width, so the phone gets a populated map instead of a bare stipple. Verified: 91 marks, **0 escaping the viewBox** (a bad coordinate leaves the frame silently), and four sitting in open water **on purpose** — `isPointInFill()` against the rendered path puts Santorini, Malé, the Galápagos and Bora Bora off Natural Earth 110m, as it does Havana, Cuba being about three user units tall at this scale; they stay, because dropping French Polynesia and the Maldives from a travel map to satisfy a coastline dataset has the priority backwards, and the note is in the module so nobody "fixes" them. One entrance tween for all ninety rather than a stagger, which across that many marks either takes seconds or is imperceptible, and seconds of the navbar's blur panel re-rastering is exactly what this beat's one-shot budget excludes. **And a real legibility defect the new layer caused, caught by measuring:** a destination mark landed inside all four label boxes and one sat mid-glyph on Val d'Orcia's price, reading as punctuation rather than as a place. Fixed with `.hero-legible`'s device at a tighter radius — two shadow layers, both **centred** where the hero's three are offset downward, because the hero sits over a photograph (a downward shadow reads as light from above) while the interference here arrives from every direction, and a 2.8px mark needs 3px to disappear where the hero's 44px halo across a 78px label would read as a dark lozenge. Marks landing in a label's *whitespace* rather than on its glyphs are left alone: `text-shadow` follows glyphs, and a place mark beside a label is what every real map looks like. **Revised once more — the destination layer was drawn too quietly to work.** It shipped as faint 4px dots at `--foreground` 0.42, on the reasoning that a stronger symbol would be `featured: true` coming back, and the user's reply was that the map still looked like it had four locations. **They were right and the reasoning was wrong.** At that weight the marks read as *texture*, not as places, so the layer was decorating the map instead of populating it. And the `featured` sin was flagging four *arbitrary* cities as special when they had nothing to do with the product — distinguishing the four *shipped plans* is not that, because those four genuinely differ by being the ones you can press, which is the same argument already accepted for giving them the hue and the labels. So each destination is now a real point symbol: hairline ring plus solid core, the same geometry as a plan marker, in the one slate instead of jade. Hierarchy restored by measurement rather than by eye — the neutral marks initially out-shone the jade ones, so the plan ring went `accent` 55%→78% and its core 4px→5px while the destination core came down to 0.62 and its ring stroke to 0.3. **Ratio of plan ring to destination ring: 1.6x at 1440, 1.9x at 1600, 1.7x at 2560, worst case 1.5x** at 1599. That worst case is what forced a clamp: the destinations are SVG and scale with the viewBox while the plan markers are HTML at a fixed 13px, so across the map's 335px-to-1760px range the ring went 2.0px → 7.9px → **10px, and the ratio fell to 1.3x** at the band's 110rem ceiling — leaving the two layers separated by colour and label alone, when "the four you can press are unmistakable" is the claim that makes jade legal here at all. Clamped above 100rem with **`r` as a CSS property**; SVG geometry properties are settable in CSS in every engine above this project's browserslist floor, and where they are not the `r` *attribute* in the markup still applies, so the fallback is the unclamped mark rather than a broken one. Verified live before being relied on — `CSS.supports("r", "3")` **and** a real `getBoundingClientRect` before and after, since "parses" and "takes effect" are different questions for that property family. There is a **real 2px step across the breakpoint** (8.8px at 1599, 6.7px at 1600) and it stays: a step-free version needs one radius to serve a 1.26x map-width range, so any clamp either steps or pulls 1440 down with it, and 2px on a decorative mark at a width only crossed by dragging a window edge is the cheaper side. Deliberately not clamped at the small end — on a 350px map the world is a thumbnail and 91 marks at a fixed 8px would cover it, so shrinking with the map is correct there, which is why this is a ceiling and not a fixed size. Re-verified at 390 / 768 / 1440 / 1599 / 1600 / 2560: 91 marks, 0 escaping the viewBox at any width, accent still 4-in-named-controls, and `prefers-reduced-motion` still creates no tween. **Hover names the destination, 2026-09-09.** One delegated `pointerover` on the group for all 87 marks rather than 87 listeners — the name rides on `data-place` and the anchor is re-read from the target's own `cx`/`cy`, so the marks stay plain geometry and there is one tooltip element instead of 87 hidden ones. `pointer-events: all` on the ring is load-bearing: it is `fill: none`, so by default only its **0.7px stroke** hit-tests and the core is 3px, which is not a target — `all` makes the whole 7.9px disc catch the pointer without the 273 SVG nodes a third invisible circle per place would have cost. Side and flip are picked from the anchor because the extremes are real (Nadi at 99.3% of the map's width, Bora Bora at 7.8%, Tromsø at 10% of its height, where "above" is the standfirst). No transition: sweeping across Europe re-anchors it twenty times a second and a fade on each is jitter plus twenty navbar-blur re-rasters. **The name is all it says, and that is a decision** — there is no destination-level cost data in this codebase, `estimateTierTotal` being destination-independent (`dailyRate × days + nightlyLodgingRate × nights`, so $730 for a 7-day budget trip whether Hanoi or Zurich), so a per-city "from $X" would be identical on all 87 or fabricated, and neither belongs on the page headed "costs what you said it would"; user chose name-only over a live-weather fetch, a hand-set cost band, and a labelled tier baseline. Hover-only by design: the marks are `aria-hidden` decoration and making them focusable would add 87 tab stops between the heading and the footer. **Two real defects found while wiring it, both by measurement.** (1) Four destinations were drawn through a plan marker — **Rome 4.8 units from Val d'Orcia**, Agra 7.4 from Jaipur, Jerusalem 7.4 from Wadi Rum, Venice 8.4 — against a 10.5 collision threshold (6.5 plan-ring radius + 4 destination-ring radius). Filtered, and **computed from `planExamples` rather than by deleting rows**, so a plan moving cannot silently re-collide or leave a hole; 91 → 87 marks, closest approach now 14. (2) The 26px plan-marker box hit-tests as a **square**, so its corners reach 18.4 units and `elementFromPoint` returned the Wadi Rum button at Cairo's centre. `clip-path: circle(50%)` fixes that — and was reverted, because `clip-path` clips descendants and the label and leader line sit well outside the box, so all four markers lost their names. Recovering a tooltip on 4 of 87 decorative marks does not pay for restructuring the label out of the control; Cairo, Luxor, Zermatt and Istanbul each lose theirs to a permanently labelled, priced marker 14–18px away, and both the attempt and the reason are recorded at `.destination-dot`. Re-verified at 390 / 1440 / 1600 / 2560 and under reduced motion: 87 marks, 0 escaping the viewBox, 4 labels at `lg`+ and 0 below, a real `page.mouse.move` onto Rio returning its tooltip at every desktop width, and `gsapTouched: 0` under reduced motion |
| The export's accent is jade, and its route line is finally legible | Active | 2026-09-09 | Claude | `The Trip Line` — the downloadable `.html` a traveler keeps and forwards — carried the previous system's amber (`#fb9826` / `#A85C05`) through two palettes, because the file had been byte-verified against its own approved comp and nobody wanted to disturb it. DESIGN.md tracked it as an open item rather than aligning it quietly, which is why it was still findable. **Re-hued by re-running the export's own validation, not by pasting the app's hex.** These marks are checked *all-pairs* because on the map any two can sit side by side, so the check was rebuilt: sRGB→Lab, dE2000, and Viénot dichromat simulation. Jade's worst pair is **dE 18.7** (jade/transit) against the file's stated 15.5 floor, where amber was 18.5 — a small improvement, not a risk — and the worst CVD pair (`food`/`transit`, 10.9) is identical for every candidate, so the accent never touched it. Coral was rejected outright at **dE 4.0** against transit under protanopia; gold separated best (24.3) but had the worst ground contrast and gold means money in this system. The green-on-green worry that prompted the measurement — jade beside `--cat-transit: #357F52` — did not survive it. **The two shades now split fills from strokes, and that split is a fix rather than tidiness.** On a light ground one mid-tone cannot do both jobs: `--accent` fills, where dark ink sits on it (6:1 for the day number) and a white ring separates it; `--accent-ink` strokes and rims, where the mark *is* the colour (7.58:1 on white for the 1.9px stay icon, 3.01:1 against the fill for the open day's rim). The map's active route line had been using the fill shade, measuring **1.86:1 on land and 1.65:1 over water** as a 2.6px stroke — under the 3:1 floor for a graphical object, and amber was the worse of the two. On the ink shade it is **6.48:1 / 5.72:1**, still 4.69:1 against the inactive lines at their 17% opacity. **Also caught: the rationale was shipping.** The first pass put the explanation inside the CSS template literal, so ~1.5KB of prose about a superseded hex travelled in every export; the `DIRECTION_CONTRACT` ships deliberately because a reader can audit it, but a changelog cannot be audited from the artifact. Moved to a JSDoc outside the literal — **1,418 bytes saved per export**. The contract itself was stale too, still naming `#fb9826` as the active marker and weight 900 for a destination that is now Melodrama 400. Verified on the shipped bytes rather than the source: export 200 at 281,325 bytes, `--accent:#28b981` / `--accent-ink:#0D6042`, **zero amber occurrences**, open day badge computing to `rgb(40,185,129)` on a `rgb(13,96,66)` rim with `rgb(11,42,50)` ink, route line `rgb(13,96,66)`. Closes DESIGN.md Open Item 3, whose type half closed earlier the same day. 718 tests, lint and typecheck clean |
| The hero is a scroll-scrubbed film of Petra, pinned for 400vh | Active | 2026-09-09 | Claude | 120 WebP frames of a camera dolly down the Siq to Al-Khazneh, drawn to a `<canvas>` at whatever frame the scroll position asks for — the Apple product-page technique, on a 400dvh `.hero-track` with a 100dvh `position: sticky` `.hero-stage`. Hero type, copy, colours, spacing and `HeroSearch` are untouched; the only structural change is the track wrapper. **The brief asked for `h-[400vh]` *and* "do not change the layout", which cannot both hold** — the user chose the pin, so the hero's internals are byte-identical and only its offset down the page moves. Its "deep gold background" was declined against the user's own "do not change colors" and the One Meaning Rule (gold means money): the canvas ground is `--canvas`, which is what actually makes it seamless, and the sandstone supplies the gold. **Two blockers that only surface at runtime.** `position: sticky` fails silently inside the old hero — `.hero-section` carried `overflow-hidden`, which makes an element a scroll container, so a sticky child resolves against a scrollport that never scrolls and pins at its start offset forever, indistinguishable from sticky never being applied. Hence track-owns-height / stage-owns-overflow, and the whole chain up to `.content-overlay` must now stay free of `overflow`/`contain`/`content-visibility`. And `window.scrollY` is always 0 here, so progress comes from `.content-overlay`'s `scrollTop` via `useScrollContainer()`. **This hero was pinned once before and the pin was deleted with five measured reasons** (`globals.css:2190`, pre-Kiln `Hero.tsx`): "the first thing a visitor does is discover it does not move"; the composition left on screen was a flat rectangle; `pinType: "transform"` rewrote `translateY` every frame because the scroller is an element; the pin spacer mutated the scroller's `scrollHeight` mid-gesture; and `refreshPriority: -1` sorted the pin *last* (the key is `refreshPriority * -1e6`), so every trigger below measured a spacer-less layout and fired a viewport early. A Chrome trace found `SCROLL_MAIN_THREAD` on 1688 of 3426 frames. **Three of the five are GSAP `pin: true` properties that CSS sticky does not have** — no spacer, no `scrollHeight` mutation, no per-frame transform, never in ScrollTrigger's refresh sort — which is why this is viable and why `pin: true` must not come back. Reason 1 is answered by the premise: that pin held a *still* photograph. Reason 2 was live again and had to be fixed — `.hero-dusk`'s `--story` range is an absolute scroll length, so `25vh 85vh` against a 400dvh track completed the wash at one fifth and held, meaning three viewports of film behind an 80% black rectangle with nothing logged; re-ranged to `290dvh 370dvh`. **The 400vh is opt-in**, which is what makes every fallback correct: the track is one viewport until `data-seq` lands, so no-JS, reduced-motion and a failed decode all get today's hero rather than three empty viewports. Derived via `useSyncExternalStore` with a `false` server snapshot, not a setState-in-effect, so it also tracks the OS setting changing mid-session. **`img.decode()` is unusable and that is the bug that cost the most time**: measured here, its promise *never settles* — detached, attached, or after `onload` fired with `complete === true` and correct dimensions — which deadlocks the six-deep load queue and sticks the film near frame 1, precisely the failure the brief named. Readiness is `onload`; a 1×1 scratch draw forces the decode off the scroll path. **`createImageBitmap` was rejected on memory**: an `ImageBitmap` is non-evictable by spec, so 120 frames is 358MB resident; `HTMLImageElement` leaves the decode cache evictable by the UA. **Perf, measured: 0 draws over 3s idle and 16.2 draws/sec while scrolling** rather than 60, because the loop self-terminates and only redraws when `Math.round(index)` changes — the real mitigation for the navbar `backdrop-filter` re-raster, which is also why `.hero-light`'s 24s infinite transform is paused while the film runs (it was re-rasterising that panel at refresh rate, scrolling or not, and the hero is now on screen 4× longer). **Frames are pre-blurred 1.0px as compression**: q72→q36 saved only 33%, the signature of a noisy render, while a 1.0px blur took 1152×648 from 123KB to 44KB/frame. Two tiers, one downloaded — 1152×648 landscape 5.13MB, 480×854 portrait 2.90MB; the portrait tier is a different *crop* because a 16:9 frame in a 9:16 viewport keeps 27% of the width, i.e. canyon wall and no Treasury. DPR capped at 1.5 (source is 1152px; 3840 is pure upscale at 4× fill rate). Frames load stride-halving so eight requests make the whole run coarsely scrubbable, and an undecoded frame draws its nearest neighbour within ±8 rather than blanking. **Contrast improved**: worst case behind the headline, composited through the scrim, is 2.55–3.77:1 across the film against **2.38:1 for the photograph it replaced**. **Also fixed in passing: the source footage was inside `public/`** — `next build` copies all of `public/` into the deployment, so ~1.2GB of zips and MP4s were a live public download from any production start. Moved to a gitignored `assets/scene-source/`. `next.config.ts` gains an `immutable` header for `/scenes/petra/*`, without which Next serves `public/` at `max-age=0` and a reload costs 120 conditional requests. `scripts/browser-matrix.mjs` gains a per-route per-engine pin assertion (chromium yes, webkit yes) because that failure is otherwise silent. **Revised 2026-09-09 after review — three defects, two of them mine.** (1) *Quality.* The first cut pre-blurred 1.0px on the argument that grain was costing bytes and softness would hide behind the scrim. Half right: q72→q36 saves only 33% on this footage, the signature of a noisy render. But it read as mush, because it compounded with the canvas upscaling a 1152px source into a 2160px retina backing store — **1.88×**. Now no blur, 1600×900, and `DPR_CAP` 1.5→**1.25**, which gives a **1.13×** upscale at 1440 CSS *and* lower fill rate than before. The two numbers are a pair. Rule: **sharpen the source and match the backing store; never soften the source to save bytes.** (2) *The awkward scroll* was three separate source defects, found by measuring rather than looking: frames **33–42 carry a double-exposure artifact** where the render morphs and the canyon walls change identity (edge energy 18.8 vs a 24.7 median) — that was the reported cave→gap jump; frames 1–30 are the crispest in the footage but the camera is nearly static through them (diff 1–6 vs 6.8 median); and consecutive-frame difference swings **14×** across the sequence, so an even-index sample mapped linearly to scroll crawls then rips. No hard cuts anywhere, so this was fixable by *selection*, not new footage. Window narrowed to unique frames **86–239** and sampling changed to **equal cumulative visual change**, which collapsed the speed spread **14× → 1.9×** and left **0 frames below 0.72× median edge energy**. 120→80 frames; the shorter window means the same sampling density with the budget spent on pixels. (3) *A legibility regression the fix caused*, caught by re-measuring rather than assuming: the brighter film on the untouched scrim ran **1.92:1 worst behind the headline with 36 of 80 frames below the 2.38:1 the outgoing photograph measured**, concentrated in frames 23–64 where a visitor reads longest. Three deeper ramps measured; shallowest that clears ships — **2.56:1 worst, 3.22:1 mean, 0/80 below baseline** — scoped to `[data-seq="live"]` like `.hero-dusk`'s re-range, so the poster and every fallback keep the original scrim. **And a real caching bug found by the second build run:** `immutable` had been pointed at fixed filenames (`land-001.webp`) on the reasoning that the script rewrites the whole directory. That is not what `immutable` means — the browser never revalidates — so the re-cut served **74,022 cached bytes against 77,002 on disk**, and in production would serve a returning visitor a *mixture* of two edits. Frames now live under `/scenes/petra/<hash>/` where the hash covers window, count, tiers and source archive. AVIF re-measured and rejected: on noisy footage it only beats WebP below q52 and is *larger* at q60 (89 vs 84 KB). **Revised again the same day — the narrowed window had cut the cave, and that was the wrong call.** Revision 2 dropped unique frames 1-85 because the camera is nearly static through 1-57 (difference 1-6 vs a 6.8 median), so they bought little scroll travel per byte. But that section *is* the cave, which is why this footage was chosen, and measured properly it is the **highest edge energy in the whole reel** (34.4-35.5 vs a 27.2 median) — a motion metric said "cheap" about the most valuable material in the shot. Also corrected: the ghost zone is **seven frames (unique 72-78), not the twenty** the first scan reported; that scan sampled every second frame and smeared the boundary. Window is now the full **unique 1-239 less 72-78**, at **100 frames** so the longer arc keeps the step size the narrow cut had (10.0 units of change per frame, 27px of scroll). The seven excluded frames are a **hard skip, not a crossfade** — a dissolve would put a deliberate double-image on screen, which is the defect being removed — costing one step of 2.4× the median, in the darkest part of the shot; seven was measured against five (cheaper seam, two low frames left in) and nine (dearer seam, nothing more removed). Payload **9.86 MB landscape / 4.29 MB portrait**, one per visitor — 101 KB/frame rather than 87, because the cave is the most detailed material and detail is what WebP spends bytes on. Re-verified rather than assumed: **0 frames below 0.80× median edge energy**, exactly **one step above 1.6× median** (the seam) with the cave section landing *below* target so it is smoother than asked, and headline contrast unchanged on the existing live scrim at **2.56:1 worst / 3.17:1 mean / 0 of 100 below the 2.38 baseline**, so no scrim retune was needed. 718 tests, `verify-build` 48 chunks, lint and typecheck clean, detector 0, pin holds chromium + webkit, walked at 1440 / 390 / 2560. **Re-cut 2026-09-09 on fresh footage, and the frame source changed with it.** The morphing was inherent to the old render, so no amount of selection fixed it — 57 of 100 frames shared the enclosed-slot composition the user flagged, i.e. removing "frames that look like this" meant removing the cave again. **The metric was the deeper problem.** Edge energy is structurally blind to morphing: geometry can drift while its texture stays sharp, so the broken frames scored 34-35 against a 27 median — the crispest in the reel. Replaced by a **rigidity** check (block-match consecutive frames, fit the radial expansion a forward dolly produces, measure the residual), which separates the two clips cleanly at identical spacing: old **median 0.541px, 94/120 pairs above 0.4px**; new **median 0.095px, 0/120**. 5.7x more rigid, so `EXCLUDE` is now empty and all 240 source frames are in play. **ezgif is out of the loop.** It had written 24fps source into a 30fps container (60 of 300 frames byte-identical, on a 5n+3 stride) and added a compression generation. `scripts/extract-frames.swift` uses AVFoundation instead — exact frame at exact presentation time, lossless, no install, since ffmpeg is not on this machine. `requestedTimeTolerance` is pinned to zero both ends or the generator returns the nearest *keyframe*, which looks identical to footage that stalls. The CRC deduplicator is gone; a hash check remains as an assertion. Two decode passes: all frames at 320px for the selection curve, then only the ~100 survivors at native size, so scratch stays ~200MB instead of 1.2GB. **Format flipped to AVIF, reversing the earlier call.** AVIF lost on the old clip because that footage was noise-dominated, which is the one case AVIF has no edge on; this one carries real detail (edge energy 36 vs 27) and AVIF is **37% smaller at matched quality** — 100 KB/frame at q44 vs WebP's 163 at q60, indistinguishable from a q88 reference at 1:1. Re-measure format per clip. Justified by the `browserslist` floor being Safari/iOS 16.4, exactly AVIF's ship release; an unsupporting browser degrades down the reduced-motion path. Decode measured, since that was the stated risk: **median 6.9ms per frame including a cache-busted fetch**, with the pixel decode already complete at `onload`. Payload **9.76 MB landscape / 4.33 MB portrait**. Output re-verified: edge energy min 0.98x median, **0 steps above 1.6x and 0 cuts** (no exclusion seam, because nothing is excluded), contrast **min 2.55, 0/100 below the 2.38 baseline** so the scrim needed no retune. 718 tests, 48 chunks, lint and typecheck clean, detector 0. **Gated on bandwidth and given a verifier, 2026-09-09.** The film is 9.76MB of ornament and nothing in the codebase honoured metered connections, so `src/lib/reducedData.ts` declines it on `prefers-reduced-data: reduce`, `navigator.connection.saveData`, or an `effectiveType` of `2g`/`slow-2g` — composed in `Hero` as a second `useSyncExternalStore` alongside the motion one, since motion and bandwidth are separate questions that happen to gate the same element. Costs nothing to decline: `data-seq` never lands and the poster hero is already the fallback. `3g` is excluded on purpose — the film loads stride-halving and is scrubbable from a quarter of its frames. The inferred `effectiveType` signal is included despite being a guess because the error is asymmetric: a false positive loses decoration with a good fallback, a false negative saturates a connection somebody is using for the page. **And `npm run verify-hero`**, because every defect this feature shipped with was *silent* — sticky-inside-overflow renders identically to no sticky at all, `img.decode()` never settling looks like slow footage, `immutable` on unversioned names only misleads returning visitors, and `.hero-dusk`'s absolute `--story` range played the film behind a black rectangle. None threw. Nine assertions: sticky computed, `data-seq` reaches live, the stage stays flush at 1/3 and 2/3 travel, all frames served with none failing, served paths match the manifest version, the film advances between endpoints, progress clamps past the end, and both opt-out paths collapse the track (reduced-data emulated by stubbing the Network Information API, which Playwright cannot emulate). **Proven to fail, not just to pass:** injecting `overflow: hidden` on `.hero-track` exits 1 on the flush check while `getComputedStyle(stage).position` still reads `sticky` — which is exactly why that assertion is separate from the computed-style one. Manual rather than a build gate: it needs a browser and a running server, and Playwright is deliberately not a dependency here |
| Kiln II's type system: a display serif, a real ramp, and a figure rule that is finally true | Active | 2026-09-09 | Claude | Kiln II's colour, layout and component decisions are untouched; only type moved. Kiln's faces had been picked three days earlier by elimination — avoid the training defaults, avoid Geist because Vercel was the named bar, ship — and **Wix Madefor Display + Text + Sometype Mono reads as well-made SaaS, not as considered.** Three strategies were comped at 1440 and 390 against the real hero, a real price and a real day row: *Quiet* (one family, hierarchy from size and space alone), *Contrast* (a high-contrast display serif at a light weight against a neutral grotesk), *Numerals-first* (a restrained sans with every figure re-set by role). The user took **2 + 3**. Shipped: **Melodrama 400 + Switzer + Tabular**, all Fontshare, licence verified as free for commercial use with self-hosting permitted, delivered via `next/font/local` from committed woff2 rather than a CDN. **A census ran first and reordered the work.** Role-by-role, the ramp was not coherent enough for any face choice to read as premium: **20 distinct letter-spacing values** (six of them on uppercase labels 10–13px apart, no two derived from each other), **six label sizes in a 3px range** written four ways (`0.62rem`/`0.6875rem`/`0.7rem`/`10px`/`11px`), five body sizes, `.font-display` spanning 16→72px on a single tracking value while its own comment claimed "tracking tightens as type grows", 211 of 328 weight utilities at 500, and prose running **96–110ch** in three places. Phase 1 fixed the foundation before a single face changed; the detector went 21 type findings → **0**. **Four tracking tokens replace the 20 literals** (`--tracking-display` -0.018em, `--tracking-heading` -0.012em, `--tracking-body` 0, `--tracking-label` 0.18em), and the discovery that justifies them: the display negatives were still at **-0.085em and -0.106em**, tuned for Wix Madefor and never revisited when the face changed — at the footer's 45px links that is −3.8px per gap, which in Switzer is letters touching. **Tracking belongs to the face, so a face swap invalidates every literal in the codebase.** Three literals survive on purpose, documented at their declaration. **The figure rule was mostly fiction and is now narrower and true.** "Mono for every figure", written into DESIGN.md, was honoured in about a quarter of cases: **5 of 19** money renders used the mono face, and **no date, time, duration or temperature anywhere** — including `StopList.tsx`'s `{time} · {duration}` line on every stop of every day. Meanwhile a whole variable family was preloading on every route for eight spans. The rule is now **tabular mono where values are compared in a column**, which is five sites, only one of them on a landing route and that one in the third band — so mono ships `preload: false` rather than competing with the first viewport. What left mono: three of `ImageRow`'s four "stat" lines *contain no digit at all* ("Lodging, food, and transit — itemized") and were set in monospace with `tabular-nums` on them, aligning nothing; and the `DAY n OF m` stamp, which has two numerals and no column. What arrived instead is **`.price-display`** — the landing price at 44px in the serif with the currency mark subordinated to 0.42em at 60% opacity and raised -0.62em, so what a visitor reads is the figure. `splitMoney()` in `format.ts` does the split. **`.font-display` had to be cut in two:** it was one class for both faces, and the 20px navbar wordmark came out spindly in a display serif. `.font-display` is now the sans heading step; `.font-display-xl` is the serif at any size. Removing `--font-display` from `@theme inline` was required in the same change — the Tailwind utility it generated collided **by name** with the unlayered class, two rules on one selector resolved by layer order rather than by anyone. **The export was still entirely in Archivo**, the face the app removed, with its own nine-size five-weight ramp topping out at 900 and every tracking value tuned for it — and the export is the one artifact a traveler keeps and forwards. It now inlines the app's two faces (~84KB against ~420KB of photographs in the same file), each degrading independently, with the day total given the app's price treatment. `public/fonts/archivo-latin-var.woff2` (88KB) was its last reference and is deleted. **Measure caps** at one value, 58ch, on the three places running uncapped. **Payload:** 115KB of Google-hosted families → 104KB self-hosted, 22KB of which no longer preloads. Verified: 718 tests, `verify-build` 47 chunks clean, lint and typecheck clean, the export route exercised end to end at 200, and the landing, `/trip/[id]`, `/trips` walked at 1440 and 390. Contrast is unchanged and its earlier measurement stands — no colour token moved, and the only size changes were four 10px→11px sites (strictly better) |
| "Kiln" visual system — the borrowed token layer replaced | Active | 2026-09-08 | Claude | The palette, root scale, type system, navbar treatment, hero and three named devices were **measured off an external reference site and adopted verbatim** — `globals.css` said so itself ("the values are theirs, measured off the live site, not approximated"), as did `layout.tsx`, `Hero.tsx`, `ImageRow.tsx`, `SectionOpener.tsx` and `HowItWorks.tsx`. All 59 mentions are gone from `src/`; `docs/design-audit.html` and `docs/design-breakdown.html` are retired into `docs/design-history.md`, which says plainly it is for accuracy, not concealment. **Ground `#121110`, card `#1C1A18`, type `#F7F5F2`, and three named roles where there was one amber: jade `#28B981` action, gold `#E9B44C` money, coral `#E4553F` alert.** That split is a fix, not a flourish — on one screen of `/trip/[id]` the single accent was painting **55 elements**: the primary button, a "DAY 1 OF 3" label, the budget fill, the download control and the selected day tab, with no way to tell an action from a readout, and a healthy budget rendering as a nearly-full bar in the please-click-me colour. **Two measured failures fixed.** `.glass-nav` was `rgba(13,46,55,0.15)`, which over the white vector tiles **MapLibre serves by default** composites to `rgb(219,224,225)` — white nav links at **1.33:1**; it was tuned for dark satellite (13.79:1), the case nobody sees by default. Now 0.86, and transparent only on the landing where the background is a photograph we chose. And `MapControls` gated on `ready` alone while both its AppShell siblings gate on `globeWanted`, so the compass rendered over content on globe-less routes. **Type:** one face (Archivo, five static weights) → Wix Madefor Display / Text plus **Sometype Mono on every figure**; the 36 existing `tabular-nums` sites were the figure surface already marked. Net load roughly flat — three variable files against five static ones. **Root:** `clamp(16px,1.13vw,20px)` → flat `16px`. Safe because every breakpoint here is `rem` and `rem` in a media query resolves against the *initial* 16px, so nothing moves below ~1416px; fluidity moved onto the two display classes. `HomeView`'s basics cap went 84rem → 105rem to hold the same 1680px. **The unlayered trap:** adding `font-size` to `.font-scene-display` silently beats every `text-[clamp()]` utility regardless of specificity, so the clamps were deleted in the same commit. **Hero:** two interlocking alpha-cut photographs → one `object-cover` image on a CSS scroll parallax, which deletes `LAYER_WIDTH` and four documented workarounds and finally allows `min-h-dvh`. A destination-plus-dates capsule replaces the CTA and opens the wizard prefilled through the `PlanPrefill` path `FeaturedPlans` already used — its money/party fields are now optional so a partial prefill cannot overwrite a returning traveller's saved profile. Direction seed `02f66ad0`, standing exit; comp and approval recorded in `.impeccable/comps/`. **The map went with it.** Finish review named the `--route-neon-*` ramp and the Orbitron/Rajdhani map labels a surviving sci-fi register on the product's core screen, and the re-derivation was funded rather than deferred. Five earth pigments (`--route-day-1..5`) at a tight luminance band replace the neon; the tokens were **renamed as well as revalued**, because a name that says "neon" about a sage-green route is worse than no name. **None of the five now falls inside `ACCENT_HUE_BAND`** — better than the handled collision the old ramp had, where one day relied on `emphasisColorFor` swapping to white. The day badge is printed rather than lit: the product's own display face instead of Orbitron, and one offset shadow instead of a 20px outer bloom, a 12px inner bloom and two coloured text-shadow layers — zero-offset coloured halos being the clearest "AI dashboard" tell there is, and one DESIGN.md had already recorded rejecting for the *stop* markers while the day badge kept it. **Five webfonts down to three.** Known cost, recorded rather than discovered later: Rajdhani was condensed and the marker declutter budget (`MIN_SEPARATION_X_PX`) is measured in horizontal pixels, so marginally fewer stop names survive the separation scan — the remedy if the map reads sparse is that constant, not a fourth face. **Sharpened 2026-09-09 against six shipped design systems** (Hyer Aviation, Flighty, Superhuman, Jeton, Linear, Seline), read as craft references — **no token, hex or font stack copied from any of them**; that source packages named companies' palettes for pasting, which is the exact move this row exists to undo. Two of their rules *validated* Kiln: one rationed chromatic action (6/6) and mono reserved for metadata. One was unanimous against it — **never use shadows for elevation** — so the hero capsule came down from `0 24px 58px / 0.55`, the heaviest offset shadow in the file, to `0 8px 24px / 0.28` plus an inset hairline. The bigger find was structural: **bands 2–5 were one template rendered four times** — same unpainted ground, same `py-16 sm:py-24`, four byte-identical `<h2>` classNames, distinguishable only by grid arity. The references split on the fix (two alternate grounds, two use gaps and hairlines); we took the second, which only works if the rhythm genuinely varies. `.scene-band` now carries an **asymmetric** dense/quiet rhythm — a dense band pushes off tight and leaves air behind it — producing seams of 160/208/144/208px where **no two consecutive match**, plus a hairline between bands and a second display step (`.font-scene-display.is-quiet`, a class not a `text-[...]` utility, since the unlayered base now owns `font-size`). Verified by replacing every glyph with a block and checking the bands were still tellable apart: they were not before, they are now. Also — an ultrawide ceiling of **110rem, not the hero's 72rem**, which would have flattened ImageRow's deliberate full-bleed row; landing prose off 14px onto `.scene-prose`'s own 1rem/1.65 (it set leading alone, so every call site picked `text-sm` and the page ran three different leadings); the featured-plan prices onto the money role, gold having reached the landing **once** before, on the section headed "Four trips, already priced"; and two stale coloured glows cleared, one an amber from *two* palettes ago on a rule whose own comment already claimed it fixed. Hero display weight 900 → 600 on a side-by-side; four of the six cap below 600. Detector: 20 findings → **0**. **One navbar, 2026-09-09.** The bar had two shapes and two grounds. `/` ran a transparent `.is-over-hero` variant with no blur and no bottom rule, while every other route ran a 0.86 surface; and `isUserFacing` gated the vertical rules and the trailing cell off `/offline`, `/backend`, `/bench` and Next's built-in 404, so four routes rendered a bare wordmark on an empty bar. Both are gone. **One class, no route branch** — nothing about the ground, blur, rules or cell structure varies by path now; only the middle cell's contents do, which is navigation, not design. Alpha settled at **0.72**, chosen by measurement once a single value had to serve every ground: 6.86:1 over the white vector tiles MapLibre serves by default and 9.33:1 over the hero photograph's brightest band, against a 0.60 floor where white text lands at 4.54:1 with nothing spare. 0.86 was safe but heavier than it needed to be. The removal paid for itself twice — **the hero's top scrim went with it**, since it existed only to give a groundless bar something to sit on, returning about 140px of the photograph's brightest band. The old `isUserFacing` argument (a dev dashboard shouldn't carry a Profile link) was a content argument used to justify a structural one; it is answered by content instead — those routes have nowhere to navigate to, so their middle cell is simply empty, and a 404 is the worst page of all to hand someone a smaller header than the one they arrived with. |
| Offline map and live GPS inside the downloaded `.html` itinerary | Active | 2026-09-04 | Claude | The exported file gained a map of the trip drawn as inline SVG, and a blue dot on it from `navigator.geolocation.watchPosition()`. **GPS is a receiver, not a network service**, so the dot works with the phone in airplane mode — which is the whole point, since the artifact's thesis is that it renders with no network. `mapProjection.ts` (Web Mercator, bounds, viewBox fitting), `mapGeometry.ts` (Douglas-Peucker, clipping, path serialization), `exportMapData.ts` (one Overpass call) and `mapSvg.ts` (markup, CSS, runtime) — the first two pure and carrying 42 of the 59 new tests, because nothing in this suite renders. **Vector SVG, not baked raster tiles.** Tiles for a metro bbox are 3-15MB after base64 against a 1.2MB whole-file imagery cap; the measured Lisbon export adds **43.5KB of road path data** to a 692KB file. **`fetchMajorHighways` was the wrong source and is not reused** — `motorway\|trunk` within a fixed 30km radius (`roads.ts`, calibrated to a Cesium camera flight) returns *nothing* in a walkable historic core and a ring road at the frame edge everywhere else, and it is the one geometry client that throws. New export-scoped query instead: one union of `primary\|secondary\|tertiary` up through motorway plus coastline, named water and rivers, **in bbox form rather than `around:`** (`arrivalPoints.ts` measured the radius form timing out server-side at 32s against 18s for the box). `["name"]` on the water clauses is load-bearing — unnamed `natural=water` at city scale is hundreds of retention ponds and swimming pools. Water splits into **filled rings and stroked lines**, because OSM models a coastline as a line with land on its left and filling it draws a blob; filled rings skip clipping, since a lake cut into runs at the frame edge fills as a different lake. **The no-absolute-URL rule bans the two obvious ways to build SVG from script.** `xmlns` and `createElementNS` both carry `the SVG namespace literal`, which is a URL, and `itineraryHtml.test.mjs` asserts the whole document is free of them — so every element the runtime shows is pre-rendered hidden and moved by attribute, and attribution is plain text rather than a link. **The client re-implements the server's projection**, which is the one silent-failure risk here: a drift lands the dot somewhere plausible but wrong. Verified numerically against the shipped artifact at **0.5m worst-case disagreement** across all 24 Lisbon pins (that residual is just the 1dp coordinate rounding), and a test asserts the script still contains `Math.tan` so it cannot decay to plate carrée. Three phone-specific lines that are each invisible until they aren't: `touch-action:none` (without it a phone scrolls the page and the pointer handlers never see a drag), `{passive:false}` on wheel (`preventDefault` is ignored otherwise, so the map zooms while the page scrolls) and `vector-effect="non-scaling-stroke"` (at the 8x zoom clamp the road network otherwise goes solid); pin radius is driven in JS instead, since a circle's `r` is geometry and does scale. **Geolocation is opt-in behind a button, never requested on load** — an unprompted dialog on open is spent for good if denied, and on iOS `file://` it fails anyway. Overpass runs concurrent with the photo walk in the route's existing `Promise.all`, so the download click is bounded by the longer of the two rather than their sum, and is memoized 10min on trip + stop bbox because the route is `force-dynamic`/`no-store` and the shared instances rate-limit by *our* IP across every user. Measured live: 8.0s export, 200, zero absolute URLs, 24/24 pins inside the frame. Every failure path exercised — all five Overpass failure modes (refused, 429, **200-carrying-a-`remark`**, non-JSON, over-large `content-length`) try all three mirrors and degrade to routes-and-pins rather than throwing; no-coordinate and Null-Island trips return `null` and the section is omitted; a single stop lands dead centre at (500,340) instead of dividing by zero. Rendered offline from `file://` in Chromium with zero page errors and zero blocked requests: day focus reframes and recolours to the reserved amber, zoom clamps at 8x and back to the fitted frame, pan stays in bounds, and the accuracy halo scales with real ground distance (40m suppressed under the dot, 250m → 32.6 units, a 20km IP-grade fix capped at 600). **Known and accepted: iOS Safari refuses geolocation to `file://` origins**, so on iPhone the map renders and the dot does not — the denied-permission copy says so rather than spinning. Desktop has no GPS chip at all (Wi-Fi lookup is a network call), so offline desktop is map-only too. A trip spanning more than 60km diagonal skips Overpass entirely and draws routes and pins on a clean ground, which is the honest answer for a multi-city plan; using `worldLand.ts` as a coarse basemap there was considered and deferred, since it would put a second projection branch in the runtime for the rarest case **Dots carry two encodings, because one is not enough.** Colour is the *kind* of stop (`Stop.category`) and the number inside it is *which* stop — its position in the day's list, repeated as a matching numbered chip on that stop's node in the itinerary below, so a reader goes from "breakfast at Graça" to the right dot without a single name on the map. **Per-stop colour was asked for and is not possible:** real days run to 14 stops (median 8), and a map of dots is a *scatter* — any two marks can sit side by side, so the palette must clear the all-pairs CVD pairlist, where even the documented eight-hue default caps out at three slots. Fourteen distinguishable hues do not exist, and the fix for "more series than the palette carries" is composite encoding, not a wider palette. So: three validated hues plus a neutral. `--cat-food:#B4650E` / `--cat-entry:#31699E` / `--cat-transit:#357F52`, run through the palette validator all-pairs against **both** surfaces the marks appear on (`#E9EEEF` map, `#fff` list) — lightness band, chroma floor, normal-vision floor 15.5 and contrast ≥3:1 all pass; CVD sits at ΔE 6.2, in the band that is legal **only** with a secondary channel, which is precisely the number in every dot. Two earlier candidate sets were rejected by the validator, not by eye: a four-hue set collapsed blue↔violet to **ΔE 0.7 under deuteranopia** (and 8.6 even in normal vision), and a more muted set fell under the chroma floor and read as grey. `other` is deliberately achromatic in `--deep` — it is the *absence* of a category, not a fourth competing for identity, and it is 24% of real stops. A legend names all four, since identity may never be colour alone. **Numbering is assigned before the coordinate filter**, so a stop saved without a position costs its dot and not the count — otherwise every dot after the gap points at the wrong row. Focusing a day no longer repaints its pins (colour means category now); the amber accent stays on the route *line*, which is what "active" means here. Dot and glyph counter-scale through one `scale()` on a group rather than two attribute writes that could drift. Measured on the 14-stop Lisbon day: 24 dots and 24 list chips agreeing exactly on category and number, numbering restarting per day. **Crowding is real and resolved by zoom rather than by moving stops**: the tightest pair in the trip sits 4.0 units apart on an 11km frame (~50m on the ground) and merges at fit view, but both numbers are legible at the 8x cap because dots hold screen size while the map expands and each carries a 2.5px surface ring — so positions stay honest instead of being nudged apart. **The map became the artifact's one visual anchor, and the photography went.** The cover image, the horizontal trip-line day strip and its station thumbnails are all removed, which takes `exportPhotos.ts` and its Wikipedia walk with them (`wikiTitle.ts` survives — `/api/place-photo` still uses it). The export now makes exactly one network call: **692KB → 210KB and 8.0s → 3.1s**, with zero `<img>` and zero inlined bitmaps. `ExportAssets` lost `photos` entirely. **The map pins to the top while a day is open**, so the day's stops scroll underneath it, and returns to normal flow when everything is collapsed — `body.dayopen .mapwrap{position:sticky}`, the class driven by the existing `toggle` listener. The pinned height is *measured* into a `--stick` custom property rather than guessed, because `details.day` needs it as `scroll-margin-top` or a day opened by the check-off runtime's `scrollIntoView` lands underneath the map that is covering it; verified at a summary top of 328px against a map bottom of 327px. `VIEW` went 1000×680 → **1000×580**: a squarer frame pinned to the top eats the room the stops need to scroll in, and 1.72:1 is header-shaped without cropping. **Numbers are scoped to the focused day.** Each day restarts at 1, so with nothing open day 1's "4" and day 2's "4" were two different places wearing one label — the overview now shows small unlabelled coloured dots (shape and stop-mix, which is what an overview is for) and identity arrives with the day the reader opens. Verified: 0 visible numbers collapsed, 1-10 on day 1, 1-14 on day 2, never a duplicate. Copy trimmed to match — the pan/zoom instructions and the check-off note about "the app" (meaningless in a file the reader may hold with no app) are gone; ODbL attribution stays, and drops out of the *pinned* strip only, where it is still one collapse away. **One pinned bar, and the day heading locks under it.** The title and the map ride together in `.topbar` (`position:sticky;top:0`), the title collapsing from `clamp(3.2rem,17vw,5rem)` to 1.7rem with the trip's dates and budget folding away — measured 503px → 362px of pinned height on a 430×860 viewport. Each `details.day > summary` then pins at `top:var(--stick)`, so "Day 1" and its chevron lock flush under the map; because a summary's sticky container is its own `<details>`, the heading rides along while that day is on screen and leaves with it, which is exactly the wanted scope. `--stick` is the *measured* bar height, re-read on every scroll frame and by a ResizeObserver, since the collapsing title changes it continuously. Both sticky rules are gated on a `js` class the runtime adds, so with no script the file is still the plain single column it promises rather than a broken sticky one. **The title was first scrubbed continuously from `scrollY` and that is a feedback loop, not a preference:** collapsing the bar removes ~126px of document height, which drags `scrollY` back toward the top — the very position that decided to collapse it. Measured settling at a 0.24 scrub value where it should have been 1, then **31 class flips a second** parked on a naive 56/20 threshold pair. Fixed by latching two states with a CSS transition and thresholds whose gap exceeds the collapse they cause (shrink 220, grow 24, plus a refusal to collapse at all on a page with under 420px of scroll room): 0 flips at every parked position, 3 across a full slow drag down and back. **The highlighted day now follows the reading position, not the last toggle** — sticky headings make several open days the normal state, so `dayInView()` probes just under the pinned bar, the same line the headings lock to, and re-highlights on scroll while deliberately *never* re-framing (moving the viewBox mid-scroll yanks the map about). It also refuses to focus a day with no dots: Kyoto's third day carries 0 stops, and focusing it dimmed every other route to highlight nothing, washing the whole map out. Verified across a 3-day Kyoto trip: exactly one day highlighted at every scroll offset, never all-dimmed, the pinned heading always matching the focused day. Zoom cap raised **8x → 18x**, which is what finally separates coincident stops: the tightest pair measured in a live plan sits 4.0 user units apart, and a 40-unit dot needs 10x before the two stop overlapping at all — at 18x the dots are 2.22 units and both numbers are readable. The ceiling is the road geometry rather than the interaction: paths are simplified to 0.4 user units, a quarter of a CSS pixel at the fitted view and about 4 at this cap, past which streets read as angular. Verified at 18x and 6x — dots clean, roads still smooth. **Zooming past the city scale buys separation and precision, not more streets**, because the query fetches only motorway-through-tertiary; residential grades would multiply the response and Kyoto already saturates the 90KB road budget, so the byte cap would drop them again anyway. Cap then raised again to **48x**, from a measurement rather than from feel: sweeping every saved trip for the closest distinct pair of stops in projected units gives Lisbon 2.96 (needs 14x), Kyoto 1.39 (29x) and Amsterdam **0.91 (44x)** — two stops about ten metres apart. Below ~45x those plans carry dots that can never be told apart at any magnification, which defeats the whole point of numbering them. Verified on that Amsterdam pair: at 48x its 0.94-unit gap beats the 0.833-unit dot and both numbers read clearly at 8px. **This is affordable because only the context layers are simplified.** Roads, water and boundary carry the 0.4-unit tolerance and do soften past roughly 20x; the day's route line and the stop dots are projected exactly with no simplify step, so the things a reader zooms this far to look at stay sharp at any magnification. Tightening the tolerance instead would have bought road smoothness at 48x by spending road coverage at the zoom people actually sit at — and Kyoto already saturates the 90KB road budget, so the cap would simply have dropped the extra detail again. |
| Search the map for cafés and put one in the plan — Map view only | Active | 2026-08-31 | Claude | `MapSearchPanel` under the Map/Satellite toggle, `/api/place-search`, `placeSearch.ts`. Searches **around what is on screen**, not around the trip: the camera centre is the query's origin, so panning somewhere and searching asks about *there*, which is the whole gesture. Six categories plus free text; results list beside the map and pin on it in amber — DESIGN.md's "you are pointing at this" colour, which is exactly what a candidate under consideration is. It stops being amber when it becomes a stop, because then it is a Tuesday. **Map only, by product decision.** The vector map has the street network, the labels and the venue data; satellite is for reviewing a plan that exists. `showSearchResults` is on `MapRenderer` so callers never branch, and `CesiumRenderer` implements it as a no-op — toggling to Satellite closes the panel and takes the pins with it. A place already *added* is a `Stop`, so it draws on both. **Google when there is a key, OSM when there is not.** `GOOGLE_PLACES_API_KEY` selects Places API (New) — `searchText` with words, `searchNearby` without, with a field mask because that API bills per field class — and Overpass answers otherwise, which is today. Overpass is genuinely good at this (`amenity=cafe` beats a paid attractions index on small venues) and is already a dependency; `nwr` + `out center` rather than `node`, because a café is a node in one city and a building outline in the next. **Throttling is reported, not disguised.** A public Overpass instance rate-limits exactly the traffic a search box makes — measured: a session of testing was enough for `overpass-api.de` to refuse connections outright, and all three mirrors were down at once — so the client fails across three mirrors and then returns `available: false`, which the UI shows as "search is busy" instead of "nothing found". `[]` with `available: true` still means the neighbourhood really has none. **The wiring was inverted once and had to be turned around.** `MapSearchPanel` is mounted by `AppShell`, so it is an *ancestor* of the page holding the plan; a provider inside `TripView` was invisible to it, and the bug showed as a search that found three places, drew three pins and offered no way to add any of them. `usePublishItinerary` now declares upward the way `useGlobeOnScreen` does, with the host's commit function in a ref so its per-render identity cannot loop the provider. One `Number(null) === 0` bug found and fixed on the way: reading `radius` as `Number(params.get(...))` before the null check clamped every request with no radius to the 200m floor instead of 1.8km — category searches still found something on a busy street and looked fine, name searches found nothing anywhere. Verified live: 24 real Lisbon cafés with addresses from the route; and with the provider stubbed (Overpass having blocked the test IP) three results listed, three pins drawn, the selection halo following the list, and the itinerary going 19 → 20 → 21 stops as places were added |
| MapLibre draws the day routes as real translucent tubes, from a custom WebGL layer | Active | 2026-08-31 | Claude | `maplibreArcLayer.ts`, replacing the `fill-extrusion` arcs. **Two style-layer attempts are on the record so nobody repeats them:** MapLibre has no elevated-line primitive at all — no `line-z-offset`, and every `line-*` layer is draped on the terrain — and `fill-extrusion` prisms are axis-aligned, so a segment spanning a steep stretch of the curve becomes a *tall box*. The curve is steepest exactly where it leaves each stop, so sizing segments off the height they climbed made the steps smaller without ever making them stop being steps: the arcs read as a staircase of opaque cubes where Cesium's ribbon is glass. A custom layer has no such constraint. The tube is swept geometry — a ring of `TUBE_SIDES` vertices carried along the centreline with a central-difference tangent (a forward difference twists the ring by half a segment at every join) and a horizontal `side` vector so the ring never rolls and the specular streak stays on top. Built in **mercator world units**, which is what `modelViewProjectionMatrix` expects and which is safe for a round tube because mercator is conformal: a metre is locally the same length on all three axes. The shader mirrors `glassRibbon.ts`'s three terms — cylindrical body falloff, a `pow(up, 10)` specular streak, and a rim that brightens *and* thickens in alpha at the silhouette. The rim is the one honest approximation: a true fresnel needs the view vector and a custom layer is handed only the combined matrix, so how far the normal points from vertical stands in for it, exact at nadir and slightly generous at a shallow pitch. Output is premultiplied because MapLibre sets `blendFunc(ONE, ONE_MINUS_SRC_ALPHA)` before calling a custom layer. `renderingMode: "3d"` shares the depth buffer so a tube behind a hill is occluded by it, with `depthMask(false)` while drawing — translucent geometry that writes depth occludes *itself*, discarding the far wall of the tube before the near wall can blend over it. Arcs spring from `queryTerrainElevation` at each end rather than from sea level, lerped across the hop (48 DEM lookups per arc is real work for a correction measured in metres). One buffer and one `drawElements` for the whole trip: ~17 hops × 49 rings × 8 sides ≈ 6.6k vertices. Verified live at zoom 15.2 and 70° pitch, which is where the prisms looked worst — smooth, translucent, map legible through them, zero console errors |
| Selecting a stop frames its neighbourhood instead of diving onto the roof | Active | 2026-08-31 | Claude | `flyToPlace` asked for `PLACE_HEIGHT_M` — 600m — which on MapLibre converts to zoom ~17: the camera on the pavement outside one building with nothing else in frame. That answers *where exactly is this* and not *where is this in the city*, which is the question somebody reading an itinerary is actually asking. `FlyToPointOptions` gained `contextRadiusM` (frame this much ground around the point) and `minRangeM` (never come closer than this), both in metres because that is the contract's vocabulary — MapLibre converts the floor to a `maxZoom`, Cesium clamps its range. **MapLibre now uses `fitBounds`**, not a computed zoom: a ±800m box around the stop, `maxZoom` at the 3.5km floor (≈ zoom 14.5), `linear: true` so it eases rather than flying van Wijk's arc — over a few hundred metres of ground that arc pulls out to altitude and descends again, which reads as the map lurching away and coming back — and a hand-written `cubicInOut` matching Cesium's `EasingFunction.CUBIC_IN_OUT` so the two engines glide identically and the Map/Satellite toggle does not change how arriving at a stop feels. **Padding is measured, not hardcoded.** `{top: 120, bottom: 80, left: 80, right: max(80, panelWidth)}` — the right inset comes off the docked panel's own box so Focus Mode's wider 62% split and the collapsed capsule both work without this knowing about either, and collapsed it falls back to the left inset so the framing is symmetric again. The top inset is the largest of the four for a specific reason: a marker card draws a full card-height *above* its anchor and a day badge floats `DAY_LABEL_LIFT_M` higher still, so a stop framed flush to the top edge has its own label off-screen — the one thing on the map that names it. Cesium takes the same route through `frameRouteBesidePanel`, the function a whole day's framing already uses, so a stop and a day are framed by one piece of arithmetic rather than two that can drift. The hover peek keeps its *relative* lean — `peekRangeM` derives depth from how crowded a stop's neighbours are, and replacing that with a fixed framing would throw the crowding away — but takes the same floor, so it can lean in and never past neighbourhood scale. Measured live with the panel open at 1440×900: MapLibre 13.95 → **14.52** on select (was heading for ~17), Cesium 4933m → 3686m, and the stop lands at x=488, dead centre of the 80..896 clear strip |
| Map / Satellite toggle, switching engines without losing the view | Active | 2026-08-31 | Claude | `MapEngineToggle` in the corner under the wordmark, where the dev container inspector used to sit — that badge named whichever `devLabel`ed box the pointer was over, which is a debugging aid on a surface whose whole argument is the imagery. `DevInspectorOverlay` is deleted; `devLabel` and its `data-dev-name` attributes stay, because they cost nothing in production and are still the fastest way to find a component from a rendered pixel. **Map is MapLibre, Satellite is Cesium**, and the labels name the view rather than the engine. `MapRenderer` gained `cameraState()`/`restoreCamera()` — an aim point, a range, a heading and a pitch, which is the only description of a view that survives the trip between a globe and a mercator map — and the provider gained a renderer registry keyed by engine, a handoff ref and caches of the last highways, city outline and pin. On a switch the outgoing engine's view is captured **synchronously in `setEngine`**, not in an effect reacting to the change: by the time an effect runs React has re-rendered and the outgoing background may have hidden its canvas, and a hidden canvas is a camera nobody can read. The incoming engine is then given that view and replayed the trip geometry, highways, outline and pin from cache — nothing is refetched, because Overpass answers take seconds and are throttled by IP, so paying again would make the toggle the most expensive control in the app. `applyDayStates`/`applyEmphasis` are re-applied explicitly after the replay, since the effects that normally drive them key on state that does not change across a swap and so never re-run — without that a toggle silently drops the selection highlight. **Neither map is destroyed.** Both backgrounds are always mounted and take an `active` prop that feeds their existing one-way `built` latch, so the engine nobody asked for never constructs a viewer, and the one you toggled away from keeps its tile cache and its camera. Measured live on a 2-day Lisbon trip: screen centre moves 127m across the swap (≈22px), heading preserved exactly (-16° → 344°), pitch converted correctly (45 MapLibre → -45 Cesium), 360 Cesium entities replayed with zero new requests, and toggling back lands on the same view with all 679 arcs intact. The residual 127m is the boundary's own cost: MapLibre's centre is a point on the terrain, Cesium's is a pick against the rendered photogrammetry surface, and at a 45° pitch over a hill those are not the same point |
| Two interchangeable map engines behind one `MapRenderer`, MapLibre by default | Active | 2026-08-31 | Claude | `src/lib/mapRenderer.ts` is the contract; `cesiumRenderer.ts` and `maplibreRenderer.ts` implement it, and `src/lib/mapEngine.ts` picks between them (`?map=`, then `localStorage.tripmateMapEngine`, then `NEXT_PUBLIC_MAP_ENGINE`, then `maplibre`). Nothing above the interface imports either engine: `mapCamera.tsx` kept its pending-request queues, its hover-peek dwell/scroll-intent gating and its day-emphasis rules unchanged and now calls `flyToPoint`/`frameRoute`/`drawRoute`/`project` instead of Cesium directly, `StopMarkerLayer` projects through `renderer.project()` on `renderer.onFrame()`, `MapControls` drives `zoomStep`/`setHeadingRad`/`setPitchDeg`, and `SplitEditor`'s map picking is `renderer.onMapClick()`. **The contract is metres, degrees and CSS pixels** — no `Cartesian3` and no `LngLat` crosses it — and pitch stays Cesium's convention throughout (negative is down), with MapLibre's complement converted inside its own renderer, because getting that backwards silently inverts the tilt slider. MapLibre runs OpenFreeMap Liberty vector tiles + an AWS terrarium DEM + extruded buildings: 3D, keyless, and deliberately **not** satellite. Routes are draped rather than arched, altitude resolves to 0 (correct for terrain-draped geometry — the marker layer's `STEM_HEIGHT_M` lift still lands on the stem), stems are extruded octagons instead of nested cylinders, and the glow is a blurred wide line under a solid narrow one instead of `glassRibbon.ts`'s fabric shader. Everything else is the same feature on both engines. Measured side by side in `scripts/map-engine-probe.mjs` — see [`map-engine-gpu.md`](./map-engine-gpu.md) — on an M4 through ANGLE Metal against a production build: **both idle at 0 WebGL draw calls/s**, which is the number that governs this app (every map frame re-blurs every `backdrop-filter` panel above it); under a drag Cesium issues **3.4× the draw calls** (8,706/s vs 2,539/s) and the cost lands in the tail rather than the median — identical 8.3ms median frame time, p95 9.8ms vs 25.2ms. Streaming is the other half: 158 requests vs 798, 9.2MB vs 16.9MB, 86MB vs 212MB of JS heap, 1.9s vs 5.8s of main-thread task time; boot is a wash at ~950ms. The probe counts draw calls by wrapping the WebGL prototypes rather than hooking either engine's frame event, because those run off `window.__tripmate*` handles that only exist in dev — so the first version of it could not measure the build worth measuring. **One trap cost most of the build:** MapLibre resolves its tile-parsing worker from `new URL(..., import.meta.url)`, which Turbopack does not serve, so the module worker died on its own import and *not one tile was ever parsed* — silently, with the style, sprite and raster layers all loading and every layer present in `getStyle()`. `scripts/copy-maplibre-assets.mjs` copies it to `public/maplibre/` at postinstall and `setWorkerUrl` points at it, the same shape as `copy-cesium-assets.mjs`. Two smaller ones: waiting on `load`/`isStyleLoaded()` before `addLayer` deadlocks when the style carries terrain (`style.load` is the event that gates it), and the construction guard has to be the in-flight promise rather than the finished map or StrictMode builds two maps into one container |
| Four-state day presence on the globe, and day badges above their route | Active | 2026-08-24 | Claude | `DayVisualState` + `dayVisualState()` in `mapRoute.ts` replace the old draw-or-skip model: every day is now always drawn and which one is being read is a matter of *presence*, not existence. `baseline` 0.75 (nothing selected — what "All Days" resets to), `active` 1.0 with arc glow and stems widened ×1.55/×1.5 (width reads as "nearer" where alpha only reads as "brighter", which is what survives a busy satellite background), `dimmed` 0.25, and `hover` 0.70 for a dimmed day the pointer rests on — lifting it *without* touching the selection, so Thursday can be checked without losing Tuesday. Selection and hover retint in place through `setDayState`; no geometry is rebuilt, and the effect calls `scene.requestRender()` because recolouring a material moves nothing under `requestRenderMode`. Day badges moved off the cluster centroid — the centroid is precisely where a day's pins are, so a badge there landed on what it was naming. Two sideways placements were tried and both read as detached, each only visible on the map rather than in a test. The **circumradius** was dominated by the run's *length* on a long thin day (airport to riverside), flinging the badge a kilometre past the outermost stop and out over the Tagus. A **perpendicular offset** put it beside the line but still clearly next to rather than part of it. It now sits **on the route and is lifted above it** — anchored to the stop nearest the day's centre (a centroid falls off the line whenever a day bends around a block) and raised `DAY_LABEL_LIFT_M` 260m, which must exceed `MAX_ARC_LIFT_M` 180m or the badge is buried inside its own arc at an oblique angle. Height was the free axis the whole time: the arcs already float at stem height, so lifting separates the badge from the pins without moving it away from the line at all. Screen distance to the nearest stop fell 281px → 137px → 104px across the three placements. `buildDayClusters` computes the radius in cos(lat)-scaled space (a raw-degree ring is an ellipse on the ground — 22% off at Lisbon's latitude) and hangs the label on the perimeter pointing *away from the trip's own centre*, so adjacent days splay their labels apart rather than into the gap between them; a single day has no "away" and takes north. Because the ring pushes labels outward and the camera is usually framed on the centre, cluster badges are then clamped into the viewport (`CLUSTER_EDGE_MARGIN_PX` 56, generous at the top since a card draws a full card-height above its anchor) — a badge names a group, not a point, so a few pixels of drift costs nothing where leaving the screen costs the day its label. Stop names get no such clamp: each is a claim about one building. Verified live: all-baseline in overview, 1.00/0.25 on selection, 0.70 on hover with the selection untouched, **zero** day-label/stop-name collisions in every state, and all four badges inside the viewport. Requirement for parabolic arcs and glow materials needed no work — `buildRouteGeometry` already lifts each geodesic by `surfaceDistance × 0.08` (clamped 12-180m) on a sine profile with `PolylineGlowMaterialProperty` + `PolylineDashMaterialProperty`. `#F59E0B` was rejected for the ramp: it is the `--accent` amber that DESIGN.md reserves for "you are pointing at this" |
| Guardrail warnings held back until the plan is edited | Active | 2026-08-24 | Claude | `ItineraryCard` captured the itinerary it first received (via `useState`'s initial value — a ref cannot be read during render, which is where the comparison belongs) and only evaluates guardrails once identity differs. A freshly generated plan is the model's own work and has already been through the critique pass; opening it under a row of warnings reads as "we made you something broken", and the traveler has done nothing yet to act on. They are feedback on *your* edit — move a stop somewhere unreachable in time and the warning answers what you just did. Identity comparison is sound because every edit path builds a new object (`moveStop`, `updateStop`, the refine's `revisedDays`), the same property `onItineraryChange`'s callers already rely on. The findings are unchanged; only when they surface is. The split editor still shows them throughout, since editing is exactly when they apply |
| Selecting a day in the itinerary panel draws that day alone | Active | 2026-08-24 | Claude | `showTripRoute`'s `soloFocus`, passed true by `ItineraryCard` and left false by `SplitEditor`. The two surfaces mean different things by "select a day": in the panel it means *show me this day*, and the answer is that day and nothing else — dimming the rest to 0.25 still left five days of line crossing the one being read. In the editor it means *work on this day*, where the others must stay visible because a cross-day drag is a decision about two days. `routeClusters` is filtered to drawn days under `soloFocus`, so no badge hangs over ground with no route under it |
| Destination city outline and neighbouring town names on the globe | Active | 2026-08-24 | Claude | `/api/city-context` + `cityBoundary.ts`, drawn by `showCityContext`. A pin says where a city is and nothing about how far it reaches, so a stop 12km out could be a tram ride or a different town. Overpass rather than Nominatim — same OSM relation either way, and Overpass is already a dependency here with its 406/`Accept` quirk and timeout behaviour documented in `roads.ts`. Outline only, never a fill: a translucent polygon over photorealistic terrain hides the city it describes. Four bugs, none of which typechecking or the API response could show. (1) `out tags` is a body modifier meaning "ids and tags **only**", so it stripped the coordinates off every place node and the neighbour list filtered itself to empty with no error anywhere. (2) Picking by `admin_level` answered "Lisbon" with *Arroios*, a parish — the level that means "city" is 8 in Germany, 7 in Portugal, 6 elsewhere — so selection is now `place=city` plus a name match, with level only as a tiebreak. (3) `out geom` on Tokyo Metropolis (which includes the Ogasawara islands) ran past a 30s abort; the lookup is now two queries, choosing from `out tags bb` metadata and fetching geometry for the winner alone, with a `MAX_CITY_SPAN_DEG` guard that rejects anything province-sized. (4) The two that actually kept it off screen: the same destination was requested **twice** (two entry points plus React's development double-invoke), Overpass throttled the second to an empty result, and being the newer generation it won while the good answer was discarded by the generation guard — and `viewerRef.current` was read *before* the fetch, which on a cold load is always `null`, so the drawing was skipped even when the data arrived. Deduplicated by destination key, and the viewer is read after the await. Verified live: Lisbon 923pts, Delhi 913, Paris 1033, all resolving to the city rather than a parish or a province; Tokyo resolves to a ward, because OSM has no city-sized Tokyo boundary and the metropolis is correctly rejected. Neighbour labels are the quietest layer on the map, last in the declutter scan and dropped entirely once a day is being read |
| Saved edit conversations, and a dot on days changed while you were elsewhere | Active | 2026-08-24 | Claude | `trip_chat_turns` + `/api/trip-chat`, written by `/api/trip-edit` as part of the edit that produced them so the transcript cannot record a change that never landed. Distinct from `trips.chat_session_id`, which is only the CLI's resume handle: that restores the *model's* memory and nothing of the traveler's, who reopened the panel to an empty box. Saved trips only — the pre-save view has no row to attach a conversation to. The dot reuses `daysModified`, already computed route-side from the ops the applier accepted; `markUnseenDays` in `unseenChanges.ts` owns the 1-based→0-based conversion (the route reports what people read, everything else is keyed on the array index) and refuses to mark the day being watched, since a badge on the tab you are already on reads as an unread marker on an open message. Cleared by visiting the day |
| Split-screen itinerary editor: globe left, editable day cards right | Active | 2026-08-24 | Claude | `SplitEditor.tsx`, replacing `ArrangeBoard`'s full-screen surface behind the same "Edit itinerary" entry point. The board was right for the one job it had — drag a stop between two days with both visible — but it covered the map entirely, so every *other* edit was made against a list with no idea where any of it was, and a cross-day move is a spatial decision. **The split is a panel, not a pane:** the editor takes the right 45% opaquely and the globe keeps the rest, with the camera aimed into the free strip by `frameRouteBesidePanel` (which measures the panel's real edge). Nothing resizes the Cesium canvas — a resize reallocates the framebuffer and re-rasters every resident tile, the same cost `AppShell` documents for hiding the globe with `display: none` — so the 55/45 read falls out of geometry already in the app rather than a second layout mode, and `AppShell`'s note that split-pane was *retired* still holds. Day tabs are "All Days" + one per day + "Add Day", each carrying its day's own map colour as a dot so the tabs and the globe agree without a legend. Selecting a day passes the new `keepOtherDays` to `showTripRoute`: the focused day gets the camera and full strength while the rest stay drawn at `DIMMED_RATIO` — the opposite of the reading view, deliberately, because moving a stop from Tuesday to Wednesday means both have to be on screen. Cards carry drag reorder (`@dnd-kit`, reusing `moveStop`/`insertionIndexByTime` so a drop re-times the day), inline name/time/note editing, delete, and a day selector for a move without a drag. Adding a stop goes through a search box or "From map", which arms a Cesium `LEFT_CLICK` handler and asks `/api/nearby-pois` what is standing at the picked point — real named venues rather than a reverse-geocoded address, because a plan reads as a list of places and every downstream consumer assumes a venue's own name. Pure edits live in `itineraryEdits.ts` (13 tests) so the off-by-one and calendar cases are reachable from the suite. Verified live: editor at 45% with the globe holding the left, All Days showing three headed sections against 25 cards, Day 2 showing its 10 alone with days 1 and 3 dimmed behind it, hovering the third card lighting "Terreiro do Paço" on the globe, Add Day appending an empty day, and a map click returning real Lisbon POIs |
| Map-first result: the whole trip on the globe, clustered and coloured per day | Active | 2026-08-24 | Claude | A finished itinerary — and a reopened saved one — now lands on its own map with the plan panel collapsed behind a single arrow. Every day is drawn at once, each in its own colour from a six-entry ramp (`DAY_COLOR_TOKENS`, cycling), with one `Day N` label per day at the centroid of its stops. `showDayRoute(stops)` became `showTripRoute(days, focusDay)`; `RouteStop` gained a `day`, which is the only place day identity exists outside `Itinerary.days[]`'s own indices — `Stop` in types.ts is untouched. Selecting a day in the panel draws **that day and nothing else** — reading one day means reading one day, and a week of other arcs crossing it is not context. Collapsing the panel passes `null` as the focus, which returns the globe to the whole-trip overview: every day, each in its own colour, a label on every cluster. `ItineraryCard`'s `panelCollapsed` is owned by the page so `DockedPanel` and the card read one boolean, and `showTripRoute(days, focusDay, panelVisible)` keeps "which day" and "how much screen is left" as separate arguments because they are separate questions. Two variants were built and dropped along the way: drawing the other days dimmed at `0.28` (at that alpha they still crossed the day being read, which is the problem the focus was for), and keeping the day focused through the collapse (which makes "Show the map" a no-op — the map-first overview is the feature, and a finished trip is meant to open on its whole shape before anyone picks a day). Stop *names* are only ever drawn for one day — all of them at once is ~45 serif labels over photography, unreadable however they are coloured — so the cluster labels carry the overview and hovering one names that day's stops without opening anything. Day labels are placed by the same `postRender` projection loop and declutter scan as the stop names, ordered ahead of them so a label is never suppressed by a stop standing where it wanted to be. Three constraints from DESIGN.md's original rejection of per-day colour are kept: day 1 is still `--route-blue` (a one-day trip is pixel-identical), nothing enters the amber/red band `--accent` and `--map-pin-red` already own, and the label names the day in words so colour never identifies it alone. **Cost:** the globe now holds roughly one day's geometry × the trip length — a 3-day Lisbon trip ~114 entities against ~38 before. Only the dashed arcs are non-constant, so the rest stays out of Cesium's dynamic batch; not yet measured on a real GPU (Playwright's swiftshader says nothing about frame cost — see `scripts/browser-matrix.mjs`). **Superseded in part:** the six-token `DAY_COLOR_TOKENS` ramp described here, and its `--route-blue` day 1, are gone — see the three 2026-08-28 enhancement rows for the neon rebuild, the motion pass and the core/glow palette pool that replaced them. Everything above about clustering, focus, labelling and declutter still holds |
| Camera aims at the centre of the strip the panel leaves, not the viewport centre | Active | 2026-08-24 | Claude | `frameRouteBesidePanel` in `mapRoute.ts`, replacing a `PANEL_BIAS_RATIO = 0.6` fraction-of-the-route-radius shove that bore no relationship to where the panel's edge actually was — it drifted with every viewport width and every trip's spread, leaving the day half under the panel with a matching band of dead space on the left. Two independent corrections: pull back by `viewWidth / freeWidth` so the route fills the strip rather than the viewport, then shift the aim point east by the metres corresponding to half the panel's width on screen (moving the aim east moves the route west). The pixels-to-metres conversion is real geometry — `2 · range · tan(½ fovₓ) / viewWidth`, the width of the world at the aim point's depth — with `fovₓ` derived from `fovy × aspectRatio` rather than read off `frustum.fov`, which is the horizontal angle only while the canvas is wider than it is tall and silently becomes the vertical one when it is not. Horizontal only, so the camera's -60° pitch never enters into it. The panel's left edge is **measured** off its own box, so Focus Mode's wider 62% split needs no special case. `MAX_FIT_SCALE` 2.5 caps the pull-back: a narrow window against the panel's 360px minimum would otherwise divide by a sliver and fly to orbit. Verified live at 1440×900 — panel edge 896, free-strip centre 448, route centre landed at 432 |
| Pointing at one stop makes every other label recede | Active | 2026-08-24 | Claude | `[data-focused]` on `.stop-marker-layer`, set whenever any stop is hovered or selected from either side (globe card or panel row — both write the same context index), drops every other stop name to `rgb(244 247 250 / 0.32)` and every day label to 34% of its own colour, halo included so a receded name reads as standing back rather than as a smudge on the map. Driven by `color`/`text-shadow` and **not** `opacity`, deliberately: that opacity is computed from `--marker-depth`, which the render loop rewrites on the anchor on every frame the camera moves, so a transition on it restarts a 400ms fade toward an already-moved target on every card at once — the trap the existing comment on that property documents. `color` and `text-shadow` are written by nothing per-frame and carry a transition safely. Measured at t≈80ms after hover: pointed-at card `rgba(252,253,254,0.96)`, siblings `0.557` en route to `0.32`. **Known, pre-existing:** the hover is dropped ~150ms later, because the hover-peek flies the camera and the card slides out from under a stationary pointer — not introduced here |
| `DockedPanel` collapses at every width, not just on a phone | Active | 2026-08-24 | Claude | The collapse existed to give a phone any view of the globe at all, and was `sm:hidden` above that on the reasoning that a side-docked panel leaves the map visible anyway. That held while the map showed one day; it stopped holding when it started showing the whole trip. Collapsed at `sm`+ the panel shrinks to its own toggle — a pill in the top-right — rather than to a bottom sheet; below `sm` the 45dvh sheet and the map-control rule that reads `--mobile-sheet-h` are unchanged. Children are hidden by `.docked-panel-collapsed > *:not(.docked-panel-toggle)` in globals.css rather than by a wrapper element, because the panel's `space-y-*` applies to its *direct* children and wrapping them would flatten its rhythm on every reopen. Collapse is now optionally controlled (`collapsed`/`onCollapsedChange`) so the page can hand the same boolean to `ItineraryCard`. The control itself is one arrow and no words: it read "Show the plan" / "Show the map" beside the chevron, which spent a wide glass pill restating what the direction already said — and while collapsed above `sm` that pill **was** the panel, so the words were the largest thing on a screen whose whole point was the map behind them. 44px circle sitting at the panel's own left edge above `sm` (block-level `w-11`, so it lands left in normal flow rather than at a negative offset `overflow-y-auto` would clip); full-width grab handle below it, where the sheet needs a thumb-sized target. One chevron, two gestures, rotated from CSS because the direction is breakpoint-dependent and a className ternary cannot say that: up/down against the bottom sheet, left/right against the side-docked panel — always pointing the way the panel will travel. The sentences survive on `aria-label`, where they cost no pixels and are the button's only accessible name. Restyled as a floating glass capsule: a 40px circle straddling the panel's left edge with **exactly half** of it over the map (`width: 2.5rem` at `left: -1.25rem` — one number halved, so the two cannot drift), vertically centred on the panel, `z-30` over both the map and the panel's own contents. Two-layer shadow (a tight contact shadow for the rim, a wide soft one for the lift), border brightening 0.14 → 0.28 and `scale(1.06)` on hover, `0.97` on press, all held still under `prefers-reduced-motion`. **The panel stopped being the scroll container to make this possible** — `overflow-y-auto` clips to the padding edge whatever the child's z-index, so scrolling moved to a new `.docked-panel-body` and the panel became a flex column. Two traps found by measuring rather than reading: `.glass-control` is a single class declared several hundred lines later in globals.css, so a bare `.docked-panel-toggle` tied on specificity and **lost on source order**, silently keeping the old flat `0 4px 16px` shadow (fixed by qualifying with the parent, `.docked-panel .docked-panel-toggle`); and the button must fall back to static positioning while collapsed, because an absolutely positioned only child contributes no size and the collapsed panel would shrink to a zero-width box and take the button with it. Collapsed, it parks at `calc(50% + var(--nav-h) / 2)` — the *expanded* panel's own centre — so toggling turns the arrow without moving it (measured: centre Y 482.5 in both states) |
| The shut panel is a capsule, and the itinerary's photo is its handle | Active | 2026-08-24 | Claude | Replaces the arrow that used to be the whole collapse control. **Shut**, the panel becomes a horizontal capsule in the same top-right corner it opened from — 32px destination thumbnail (the `usePlacePhoto` cache key the header and the /trips collage already use, so usually no request), `Kyoto, Japan · 2 days`, a `Day 1` badge and a `Maximize2`, the whole pill being one button. An arrow said nothing about the trip it had swallowed; this answers "which day was I reading" without reopening. **Open**, there is no window chrome at all: a header bar with a grab line and a minimize button was built first and thrown away, because it put a strip of furniture between the map and the photograph the card leads with. The grab line lives on the photo instead (`ItineraryCard`, `onMinimize`), an iOS-style 32x4 handle at its top edge inside a 40px target, carrying its own shadow because a white line vanishes against a sunlit roof. **The fold** is a CSS transition on `width`/`height`/`left`/`right` written inline, not a Framer layout animation: `layout` was tried on the element and then on its parent and snapped both times (measured 309x57 -> 505x38 -> 520x37), since Framer's projection has no answer for a `position: fixed` box whose insets change in the same commit. Two findings paid for by that detour, both measured on a probe with a linear transition: Chromium interpolates `40%` -> `20rem` (50 frames) and `calc(100dvh - 4rem - 3rem)` -> `3.5rem` (49) but **not** `min(40vw, 520px)` -> `20rem` (3 frames, i.e. a snap), which is why the panel's width is a percentage plus a `max-width` present in *both* states rather than the `min()` that reads better; and `ease-in-out` replaces the house `cubic-bezier(0.16, 1, 0.3, 1)`, whose front-loading is right for a 12px nudge and wrong for an 800px fold. A caution for the next person measuring this: headless Chromium renders this page at ~10fps with the globe up, so a per-frame sampler reports any 300ms transition as a snap — `transitionrun`/`transitionstart` events and timestamped samples (520x786 -> 345x113 -> 325x57) are what actually settle it |
| The itinerary's header photo collapses as the plan scrolls under it | Active | 2026-08-24 | Claude | The card's destination photo is now pinned to the top of the panel's scroller and compresses from 14rem (18 at `sm`) to 4.5rem as you scroll, springing back on the way up — the large title fading out by 45% of the way down and a compact `Kyoto · DAY 1` line arriving after 55%, so the strip still says where you are. `--hero-p` (0 to 1) is written to the element once per animation frame by a rAF-coalesced scroll listener and everything else derives from it in CSS (`.itinerary-hero`), so no React state is involved: a `useState` here would re-render the card, its day list and every consumer of the map-camera context on every scroll frame. One pixel of scroll gives up one pixel of header, because the header is in flow and a faster collapse makes the plan rush up to meet it. Two things had to move for this to work at all: the card wrapper became `overflow-clip` instead of `overflow-hidden` (both clip to the rounded corners, but `hidden` makes the card a scroll container and a sticky child sticks to *that* box, which never scrolls), and `.docked-panel-body` turned `overflow-anchor: none` — Chrome's scroll anchoring corrects for content shrinking above the viewport by pulling `scrollTop` back by the same amount, so the header and the scroller chased each other: scrolling to 40, 80, 120 or 160px all settled back at 0 with the header full-height, and only a jump past the whole range stuck. A destination whose Wikipedia photo misses keeps the old plain band and does not collapse, which is the existing "no reserved space for a value that isn't there" rule |
| The globe's hover peek holds still while the plan is being edited | Active | 2026-08-24 | Claude | `useHoverPeekSuspended()` in `FocusEditMode`, mirroring `useGlobeOnScreen`: the surface that knows declares it, and unmounting lifts it. The peek answers "which stop is this?" while *reading* a plan; while editing one the same pointer movement means dragging a stop between days or reading a chat turn, and the globe's marker cards are still under the cursor — `ItineraryCard` is unmounted in Focus Mode, so the rows are gone but the cards are not, and a camera diving at whatever one of them was brushed moves the ground mid-edit. Suspending also *undoes* a peek already in flight rather than merely refusing new ones, or opening the editor would strand the camera leaned in on a stop whose plan is no longer on screen. Verified in Chromium across all three transitions: hovering a row while reading peeks, hovering a marker card with the editor open does not, and closing the editor restores it |
| Home page trip form (destination / dates / budget) | Active | 2026-08-04 | Aryan | |
| `/trips` saved trips list | Active | 2026-08-04 | Aryan | |
| `/trip/[id]` saved trip detail | Active | 2026-08-04 | Aryan | |
| `/trip/[id]/print` reviewable itinerary document | Active | 2026-08-22 | Claude | An all-days, black-on-white copy of one trip to send to someone outside the team, with a fixed review-question sheet at the end. Exists as its own route rather than a print stylesheet over `/trip/[id]` because `ItineraryCard` is tab-based and renders exactly one day, so printing that page yields one day whatever the trip's length. Every stop is labelled `Day N · Stop M` in full rather than relying on an `<ol>` marker, which numbers stops within a day but drops the day half of the reference — the point is that a reviewer can name what's wrong. Two silent failures had to be fixed in `globals.css` first, both invisible on screen: `.app-shell` is `h-dvh overflow-hidden` around an `absolute inset-0` `.content-overlay`, which prints one screenful and clips every page after the first (measured: 954px document, 6693px once unpinned), and `body`'s `--foreground` is `#ffffff`, which prints white on white. Costs report `dayPlanned` for both the day and trip figures, not `tripSpend` for the total — `tripSpend` prefers `actualCost` where present, so mixing them would let the day numbers fail to sum to the trip number in a document whose whole purpose is line-by-line scrutiny. Zero new dependencies; the reader prints to PDF |
| `ItineraryMap` (Leaflet) | Active | 2026-08-04 | Aryan | |
| `BudgetBar` | Active | 2026-08-04 | Aryan | |
| `DayList` | Active | 2026-08-04 | Aryan | |
| `FeedbackLoop` (give feedback → regenerate, or save trip) | Active | 2026-08-04 | Aryan | |
| `PageHeader` shared component | Active | 2026-08-05 | Aryan | One component reused across all pages instead of duplicated markup — see Eliminated below |
| `PreferenceStep` (trending-tag chips + vibe chips + skip) | Active | 2026-08-05 | Aryan | Shown after the trip form, before generation |
| `TraceStatusBadge` shared component | Active | 2026-08-05 | Aryan | Shared by the LLM trace list/detail views |
| `/llm-trace` list page | <details><summary>Eliminated 2026-08-06 (Aryan)</summary>Folded into the `LlmTraceFab` widget's list view below — no route needed anymore</details> | 2026-08-05 | Aryan | |
| `/llm-trace/[id]` detail page | <details><summary>Eliminated 2026-08-06 (Aryan)</summary>Folded into the `LlmTraceFab` widget's detail view below</details> | 2026-08-05 | Aryan | |
| "View LLM trace for this call →" link on results | Active | 2026-08-05 | Aryan | Now calls `useLlmTraceWidget().openItem(traceId)` instead of navigating — see `LlmTraceFab` below |
| `LlmTraceFab` — floating action button + expandable panel for LLM traces | Active | 2026-08-06 | Aryan | Bottom-right icon (collapsed) → panel with list/detail views + a collapse button (expanded), mounted once in the root layout so it's available on every page. Content selection came from `.claude/skills/dev-analytics-fab/SKILL.md` ("LLM/AI calls" was the one catalog category that fit this app); the FAB/panel UI mechanics themselves came from the separate `.claude/skills/floating-widget-ui/SKILL.md` |
| `LlmTraceFabProvider` / `useLlmTraceWidget()` context | Active | 2026-08-06 | Aryan | Exposes `openList()` / `openItem(id)` / `close()` so any "View trace" link elsewhere in the app can open the widget pre-focused on a specific trace, without reaching into its internal state |
| `WeatherIcon` (dynamic Lucide icon per day) | Active | 2026-08-05 | Aryan | Picks Sun/CloudSun/CloudDrizzle/CloudRain/CloudSnow/CloudLightning/CloudFog/Umbrella from WMO `weatherCode` + rain probability |
| `WeatherPopover` (click-to-expand weather detail) | Active | 2026-08-05 | Aryan | Shows low/high temp, humidity, rain chance; falls back to the model's plain-text `weather` string when structured data is missing |
| Daily budget progress bar per day card | Active | 2026-08-05 | Aryan | Shows each day's spend as a % of the *overall* trip budget, in `DayList` |
| Hover-to-highlight: hovering a stop in `DayList` highlights its pin on `ItineraryMap` (and vice versa via shared state) | Active | 2026-08-05 | Aryan | Stops are keyed `${dayIndex}-${stopIndex}`; hovered marker swaps to a custom `L.divIcon` |
| Daily narrative summary line (model-written, 1-2 sentences + emoji) | Active | 2026-08-05 | Aryan | Rendered at the bottom of each `DayList` day card; optional field, so pre-existing saved trips just render without one |
| 3D CesiumJS globe landing experience (`CesiumGlobe`, `useCesiumViewer`) | Active | 2026-08-05 | Claude | Home page only. Continuously auto-rotating globe behind the search form; rotation stops on first canvas interaction or on search. Uses ion world imagery/terrain when `NEXT_PUBLIC_CESIUM_ION_TOKEN` is set, else falls back to Cesium's bundled offline Natural Earth II imagery + ellipsoid terrain |
| Liquid-glass search form (`TripSearchForm`) | Active | 2026-08-05 | Claude | Replaces the old plain white home-page form; floats over the globe |
| Cinematic camera flight on search (`useCesiumViewer.flyTo`) | <details><summary>Eliminated 2026-08-05 (Claude)</summary>Folded into the gift-box choreography below — the flight now happens after preferences are submitted, not immediately on destination resolve</details> | 2026-08-05 | Claude | |
| Split-view Trip Dashboard (`TripDashboard`) | Active | 2026-08-05 | Claude | Home page only, shown once itinerary generation completes: globe (with POI billboards) on one side, existing `BudgetBar`/`DayList`/`FeedbackLoop` on the other |
| POI billboards on the globe, hover-synced with `DayList` | Active | 2026-08-05 | Claude | Reuses the existing `${dayIndex}-${stopIndex}` hover-key convention so hovering a stop highlights the same pin on both the Leaflet map (saved-trip view) and the globe (home page) |
| "← New search" back action | Active | 2026-08-05 | Claude | Resets trip state to `IDLE` and flies the camera back out to a global view, resuming auto-rotation |
| `useTripState` state machine | <details><summary>Eliminated 2026-08-05 (Claude)</summary>Replaced by the 5-status version below (`IDLE`/`SEARCHING`/`PREFERENCES`/`GENERATING`/`DASHBOARD_ACTIVE`) — the old `FLYING` status conflated two things ("destination resolved" and "camera moving") that the gift-box choreography needed to happen at different times</details> | 2026-08-05 | Claude | |
| `useTripState` 5-status state machine (`IDLE`/`SEARCHING`/`PREFERENCES`/`GENERATING`/`DASHBOARD_ACTIVE`) | Active | 2026-08-05 | Claude | Orchestrates the home page: form submit → geocode (`SEARCHING`) → preferences interactive, box open (`PREFERENCES`) → box closes, camera pans then flies, itinerary POST concurrently (`GENERATING`) → dashboard (`DASHBOARD_ACTIVE`). `page.tsx` derives the 2D gift box's visual state directly from `status` |
| 3D Cesium gift-box choreography | <details><summary>Eliminated 2026-08-05 (Claude)</summary>The 3D primitive box (base + hinged lid, opening upward) looked distorted and its lid overlapped/obscured the preferences panel's chip text. Replaced by a plain 2D SVG box below</details> | 2026-08-05 | Claude | |
| 2D unboxing container (`GiftBox2D`) | <details><summary>Renamed/expanded 2026-08-05 (Aryan)</summary>Renamed to `UnboxingContainer` and grown from a single fixed box into 4 destination-themed containers below</details> | 2026-08-05 | Claude | |
| `UnboxingContainer` — 4 destination-themed containers (`classic_box`/`vintage_envelope`/`travel_trunk`/`furoshiki_wrap`) | Active | 2026-08-05 | Aryan | Theme (container type, title, primary color, icon) picked by a small dedicated Claude call (`GET /api/container-theme`, see `backend.md`) based on destination culture/geography — decorative and non-blocking, falls back to `DEFAULT_CONTAINER_THEME` (`classic_box`) on any failure. Each container type has its own distinct "opening" motion: box lid hinges open, envelope flap flips up, trunk lid lifts like a chest, furoshiki knot unties and the cloth corners peel back |
| Glassmorphism on the classic box container | Active | 2026-08-05 | Claude | The literal "cardboard box" theme (`ClassicBox`) got a frosted-glass treatment: translucent fills (`withAlpha` helper) instead of solid colors, light `rgba(255,255,255,0.55)` borders on each face, and a glossy diagonal highlight sweep. Since the container floats directly over the live globe canvas with nothing else behind it, the translucent fill lets that motion genuinely show through rather than faking it with a backdrop-blur (which doesn't reliably apply to SVG shapes). Scoped to the classic box only — the other 3 themes are paper/wood/cloth and a glass look wouldn't fit them |
| Motion/action lines on the preferences-card bubble collapse | Active | 2026-08-05 | Claude | 8 radiating speed-line accents fade in/out and travel downward in lockstep with the collapsing card (same spring transition) as it shrinks into the gift box on "Generate"/"Skip" |
| Camera panning shot (`useCesiumViewer.panTo`) | Active | 2026-08-05 | Claude | On "Generate"/"Skip", before the existing destination `flyTo`, the camera does a fixed-position pan: heading/yaw rotates in place (via great-circle bearing to the destination) to frame it, with camera position untouched (a "tripod" shot, not a zoom) — then `flyTo` reuses that same heading so the following descent doesn't snap to a different angle |
| `TripSearchForm`/`PreferenceStep` card-collapse + bubble-collapse animations (Framer Motion) | Active | 2026-08-05 | Claude | Submitting the search form collapses it (`scale:0, opacity:0`). `PreferenceStep`'s chips float in on mount with a fixed staggered offset (no per-frame position tracking needed now that the box is a simple fixed DOM element). On "Generate"/"Skip" the *entire* preferences card shrinks into a bubble (`scale:0`, `borderRadius:999`, translate down, fade) before the callback actually fires — replaced the earlier per-chip implosion, which no longer made sense once the box moved out of the 3D scene |
| `TripDashboard` sidebar slide-in (`x: "100%" → "0%"`) | Active | 2026-08-05 | Claude | Spring transition on the itinerary panel specifically, independent of the outer fade-in already on the dashboard's parent `motion.div` |
| Quick destination chips in `TripSearchForm` (Kyoto/Tokyo/Paris/Bali, one flagged 🔥 trending) | Active | 2026-08-05 | Aryan | Click populates the Destination field; no new state needed, just `setDestination` |
| Pin decluttering on the globe via Cesium `EntityCluster` | Active | 2026-08-05 | Aryan | POIs moved from `viewer.entities` into a dedicated `CustomDataSource` with `.clustering.enabled = true` — clustering only applies to entities that belong to a DataSource, not the default collection |
| Adaptive header contrast (`PageHeader` `variant="adaptive"`) | Active | 2026-08-05 | Aryan | Home page only. Samples the globe canvas under the "TripMate" title; dark backdrop → white text + shadow, light backdrop (terrain) → dark text in a `backdrop-blur-md` capsule badge |
| Floating top capsule ("← New search" + live budget) in `TripDashboard` | Active | 2026-08-05 | Aryan | Centered, elevated above both the map and list halves; text goes red when over budget, matching `BudgetBar`'s color language |
| Wizard seeds from the saved traveler profile | Active | 2026-08-16 | Zaid | Durable answers pre-fill on mount. Defaults only — the wizard always wins, and any failure falls back to the hardcoded defaults. No longer written back after generate — see the "Four-screen plan wizard" row below; `/profile` and the onboarding card are the only writers |
| `GenerationLoader` streams real per-stage progress | Active | 2026-08-16 | Zaid | Five real stages (geocode/context/generate/critique/placing) replace the old fixed 5-caption loop; captions rotate per-stage from `generationStages.ts`, sr-only text announces stage transitions |
| Four-screen plan wizard + "Adjust for this trip" | Active | 2026-08-16 | Zaid | Basics/purpose/group/pois only; explorer style, energy, crowds, tier and priorities sit behind one expander on basics fronted by a summary of the values in effect. One expander, never one per field — that is what keeps the worst case below the seven screens it replaced |
| `/profile` page | Active | 2026-08-16 | Claude | Owns the durable traits, reusing the wizard's own pickers. Surfaces save failures, unlike the onboarding card. Content sits directly on `--canvas` with no panel, in a two-column split — a memories column of real trip photography beside the form above `lg`, single column below. `map-chrome-hidden` (zoom/2D-3D/tilt/compass have nothing to act on here). Previously a `.profile-glass` frosted card; see the Eliminated entry below |
| Onboarding card on the result screen | Active | 2026-08-16 | Zaid | Offered once after the first trip, pre-filled with the answers just given plus dietary needs. Dismissing costs nothing |
| Delete a saved trip from `/trips` | Active | 2026-08-17 | Claude | Trash button on each postcard, hidden until the card is hovered or focused (and always visible under `@media (hover: none)`, since a hover-only control is unreachable on touch). It is a *sibling* of the postcard `<Link>`, never a child — HTML forbids interactive content inside an `<a>` — inside a `.memory-postcard-slot` wrapper that owns the reveal. Failures surface in their own `deleteError` state, not the page's fatal `error`, which early-returns a page with nothing but the message and would blank the very grid being reported on |
| `ConfirmDialog` shared component | Active | 2026-08-17 | Claude | The app's first modal, on the native `<dialog>` + `showModal()` rather than a hand-rolled overlay: focus trap, Esc, background `inert` and top-layer rendering all come free. Top-layer promotion only changes *paint* order, not the DOM tree — the dialog still renders as a child of AppShell's `pointer-events-none` content overlay and inherits it, so `.confirm-dialog` needs its own explicit `pointer-events: auto` same as everything else in that overlay (the Pointer-Events Opt-In Rule applies here too). Shipped once without it: every button inside was unclickable by a real pointer, but a programmatic `.click()` bypasses hit-testing and doesn't reveal that, which is how it went unnoticed until a real click was tried. Tailwind's preflight also zeroes the UA `margin: auto` that centres a modal dialog — `.confirm-dialog` restores that too, without which the panel renders hard against the top-left corner |
| `/bench` — dev-only model benchmark page | Active | 2026-08-17 | Claude | Server-component `notFound()` gate on `NODE_ENV`, same as `/backend/pipeline`. Not linked from any nav. Charts are hand-rolled SVG in `components/bench/charts.tsx` — no charting library was added; palette is validated categorical slots 1-3 (worst all-pairs CVD ΔE 9.2), with direct labels + a raw-numbers table as the documented contrast relief |
| Weighted flight-path progress in `GenerationLoader` | Active | 2026-08-17 | Claude | Replaces the five equal stage dots. Segment widths come from `STAGE_SECONDS` (kept beside `STAGE_ORDER`, with a test asserting matching keys) because the stages are wildly unequal — `generate` is ~95 of ~150 nominal seconds, so equal segments parked the indicator at 20% for a minute and a half, which reads as a hang. Inside the running stage the fill advances on `1 - exp(-t/tau)`, which structurally cannot complete a segment before the real `done` event arrives. `generationProgress()` is pure and unit-tested for monotonicity across a replay of the runner's real emission order, including the deliberate `context`/`geocode` overlap. Written to a CSS custom property through a ref at 10Hz rather than React state; `setInterval` not rAF, because rAF pauses in background tabs and returning to a frozen bar is the exact failure this exists to remove |
| Orbiting destination facts during generation | <details><summary>Eliminated 2026-08-17 (Claude)</summary>Two off-screen wheels carried the facts on arcing, tilted cards. Cut after a critique: text a person must read cannot move (the dwell was tuned 75px/s → 28px/s, optimising the wrong axis — the correct speed is zero); it was a WCAG 2.2.2 Level A failure with no pause/stop/hide and left magnification users unserved; and it broke DESIGN.md's own One Ambient Loop Rule with three stacked loops per card on four  cards outside . Simulation also found two bugs reading the code had not: with a pool of 3–4 every slot showed one fact forever, and on mobile the hidden wheel's frozen slots stayed in the exclusion set so two facts could never appear. Replaced by the static row below</details> | 2026-08-17 | Claude | |
| Destination facts during generation | Active | 2026-08-17 | Claude | True, trip-specific facts in a staggered stack beneath the caption: one card square-on and readable, neighbours fanned out either side, dimmed and tilted. Auto-advances every 7s and can be steered with arrows or by clicking a card forward; taking control resets the timer. **The front card is completely still while it is read** — motion happens only on a discrete advance, which is the whole distinction from the orbiting version it replaced and why a reader can stop the rotation by using it. Adapted from a stagger-testimonial pattern with its neobrutalist skin left behind (notched clip-path corners, hard block shadows) in favour of this system’s glass. Position comes from a cursor + modulo rather than a shifted array, because the pool grows mid-wait as the context and Wikipedia extract resolve; the fan narrows below five facts so no sentence renders twice at different depths. Also adds a line above the disc naming the trip (`Kyoto · Sep 19 – 22 · Mid-range`) — the plan form unmounts during generation, so nothing else said which trip was being built. **No model call**: `buildDestinationFacts` (pure, type-only imports, unit-tested) derives everything from data already fetched and never fabricates. Renders *outside* the `role="status"` region so a screen reader can reach the facts without their being announced |
| Cancel during generation | Active | 2026-08-18 | Claude | The wait had no exit: the form unmounts, the surface is pointer-events-none, and the only way out was a reload that destroys the run — so the most anxious user was the one most likely to kill a call that was nearly finished. A ghost Cancel appears after 10s (delayed so it never flashes on a fast path), aborts the fetch via AbortController, and returns to the form with every answer intact. An AbortError is deliberately not surfaced as an error: the user asked for this, and reporting their own decision back as a failure would be wrong. **Known limitation:** aborting the fetch closes the connection but does not kill the server-side  subprocess, so the model call still runs to completion — wiring the request signal through  to  is the follow-up |
| Trip length shown in the budget field | Active | 2026-08-20 | Claude | `days` had been computed at `HomeView.tsx` since the tier cards needed it and was never rendered, so picking two dates never told you how long the trip was. Shows `$375/day for 8 days` — the division the traveler was going to do anyway, which answers the length and what the budget buys in one line. The readout already existed verbatim in `blue-hour/TripFormConsole.tsx`, a fork of this form that nothing imports |
| Arrive / depart row on the plan form | Active | 2026-08-20 | Claude | Optional time + free-text point for day 1 and the last day. Start/End stay the trip's only date range — a second one would give the app two competing notions of trip length, and tier pricing, the day count and the weather window all read the first. Points are deliberately not geocoded: they are prompt facts, and a lookup adds a fetch that can fail for no planning gain. The native time glyph is hidden by the same `globals.css` rule the date cells use, without which `08:15 PM` clipped to `08:15 P` |
| "Other" group with free text | Active | 2026-08-20 | Claude | Fourth pill on `GroupTypePicker`. Five friends, a work offsite and three generations all had to pick the least wrong of Solo/Couple/Family, and the plan was built on that wrong answer. `GroupType` gains `"other"` and `travelerProfile.ts`'s `GROUPS` with it — omitting it there would make a saved profile fail to parse and silently wipe the traveler's defaults. The text itself stays per-trip, not on the durable profile |
| `PartyCounter` — adults / children / infants steppers | Active | 2026-08-20 | Claude | The app's first stepper; every other numeric input is a bare `<input type="number">`. Justified here because these are single digits from a tiny range where two taps beat a keyboard, and it is the control travelers have already met in flight booking. Shown for every group, not just families — a party of six friends is exactly where headcount changes the lodging and the table. Pills preset the counts (Solo 1/0/0, Couple 2/0/0, Family 2/1/0) **one-way**: counts never flip the pill back, or picking "Other" and setting 2 adults would silently read as a couple. a11y is the whole cost of hand-rolling it — each row is a labelled `role="group"`, the value is `aria-live`, and buttons disable at the bounds. Singulars are spelled out per band after the first pass announced "Remove one children" |
| `SuggestInput` — the app's first accessible combobox | Active | 2026-08-20 | Claude | One component behind all four arrive/depart fields, in two modes: free text (places — anything typed stands, which is what keeps the field optional) and pick-only (times — typing filters but only a pick commits, so the stored value stays a strict `HH:MM` with no free-form time parser and nothing silently dropped by `sanitizeClock`). Written rather than adapted from `DestinationSearch` because that component has **no** accessibility — `role="combobox"`, `listbox`, `option` and `aria-activedescendant` appeared zero times in `src/` before this. Two behaviours are load-bearing and inherited from bugs `DestinationSearch` already paid for: closing on document `mousedown` and a `relatedTarget`-guarded `onBlur` rather than a bare one (blur fires before an option's click and kills it), and swallowing Enter unconditionally (all four sit inside the plan `<form>`). Opens scrolled to the selection — without it a 96-row time list set to 8:15 PM opens at midnight |
| Arrive/depart times as a scrollable 15-minute list | Active | 2026-08-20 | Claude | Replaces `<input type="time">`, whose desktop rendering is a bare text spinner. **Known trade:** this gives up the OS wheel that iOS and Android opened for free, in exchange for one control that behaves the same everywhere and matches the theme. Fifteen minutes is the granularity an arrival time is known to. `formatClockLabel` is in `format.ts` and unit-tested for the two hours a bare `h % 12` gets wrong (midnight reads as 12 AM, noon as 12 PM) |
| Arrive/depart labelled `optional`, with real places to pick from | Active | 2026-08-20 | Claude | "Airport or station" read as a demand for knowledge a first-time visitor doesn't have — the fields were already optional in code but nothing on screen said so. Now the `Field` cell takes an `optional` flag, the placeholder invites rather than asks, and the list is fetched from `/api/arrival-points` the moment the destination resolves. Fed by `destinationCoords`, newly exposed from `useTripCamera` (the state already existed) — keyed on coordinates rather than the typed string so the blur-geocode path works too. Cleared in the effect's cleanup, so a slow response for an abandoned destination can't land after the new one's |

| `GlobePoster` — the static backdrop that stands in for the globe | Active | 2026-08-19 | Aryan | The stand-in for the surfaces that have no viewer: a cold load of `/trips` or `/profile`, a trip page sitting on its city overview, and the seconds any globe spends streaming its first tiles. (It covered the landing page too for one revision; that page now gets a real `static` globe instead, since a parked viewer costs nothing per frame and the photorealistic Earth is the thing the hero copy is written against.) The problem it solves is unchanged: every route composes over a full-bleed backdrop, but not all of them are *using* the 3D scene. This draws what they actually showed — a CSS earth-limb still framed to the same horizon as the hero pose (`top: 46%`, which is where the curve lands at 2,500km/-45°), or, when a destination is known, that city's photo through the same cool/de-vibranced filter the photorealistic tileset is pushed into so the swap isn't a jump in saturation. The photo comes from `usePlacePhoto(destination, "full")`, the same cache key the itinerary header and `/trips` collage already use, so a trip page usually paints it with no request at all. Carries the one explicit way into 3D ("Explore in 3D"), since otherwise the only routes in are a stop click and Play tour |
| Download control on `ItineraryCard` — self-contained HTML itinerary export | Active | 2026-08-25 | Claude | A `<a download>` pill beside the destination heading, linking to `GET /api/trips/[id]/export` (see backend.md). Gated on `trip?.id && trip.id !== "preview"` so it's absent on the pre-save result view and on `/trip/preview`, since neither has a real database row for the export route to read. Deliberately a plain anchor, not a button + fetch — the browser's native download handling is the whole mechanism, no client state needed |
| Drafts tab on `/trips`, and a plan that saves itself the moment it's generated | Active | 2026-08-28 | Claude | The result view used to hold a finished itinerary in React state alone, so `Keep this trip` was the only thing between a two-minute model call and losing it — and pressing Back was enough. `HomeView` now writes the plan to a `draft` trip row as soon as it arrives (`writeDraft`, fired without `await` so the arrival is not blocked on an insert, and **fail-soft and deliberately silent**: an error block at the moment a long wait finally pays off reports a problem the traveler didn't cause about a plan that is on screen and working). Every pre-save edit — refine, a drag on the board, an inline day edit — is coalesced into one PATCH `DRAFT_AUTOSAVE_MS` (1200ms) after the plan settles, by **one effect watching `itinerary`** rather than a call at each of the four edit sites, so an edit path added later is caught for free; the comparison is object identity, sound because every edit path here builds a fresh object. `save()` promotes the row instead of inserting, and awaits an insert still in flight so a Save pressed in the first moment after arrival can't create a duplicate. `FeedbackLoop` states the draft's status in one line beside the button, because a silent safety net is one nobody trusts and the traveler who presses Back is exactly the one who never learned it was there. `/trips` gained a tab strip — **`aria-pressed` toggle buttons in a named group, not `role="tablist"`**, since that role promises arrow-key navigation this markup doesn't implement — that appears only when a draft exists, so a traveler with none sees the page exactly as it was; the active tab is *derived* each render rather than trusted from state, or discarding the last draft would leave it pointing at a list that is gone. Draft cards are the same `MemoryCard` with a `Draft` chip in the slot the old postage stamp held and re-worded destructive copy ("Discard this draft?"), because a draft is a whole itinerary and not a lesser preview of one. Reopening a draft lands on `/trip/[id]`, which already works in full — chat, drag board, cost inputs all key off the row, not its status — so it only needed a banner with its own `Keep this trip`, placed above the overspend warning as the one thing on the page with a deadline. Verified against the running server: the tab strip and draft chip render at `/trips`, the banner renders on a draft's page and not on a saved trip's, and `/`, `/profile` and `/trip/latest` all still answer |
| The plan draws itself on the map while the model writes it | Active | 2026-09-02 | Claude | A generation showed nothing for ~310s and then everything at once. The model's JSON is now parsed out of its own token stream, so each completed stop reaches the client as it is written and each day's corrected coordinates as that day closes — the route, the pins and the day cards build themselves over the whole wait — and the ~150s `critique` pass moves off the critical path: **the plan is interactive at ~160s** instead of ~310s, with a revision landing later only if the review changed anything. That is what retired the full-bleed opaque wait layer (which never had a row of its own here, so there is nothing to collapse): the map is the wait, and the layer shrinks back to the darkened foot band it already had, keeping the three things that had to survive — Cancel, the four-group progress strip, and the destination facts. **`showPlan` is the single funnel** for both the streaming and the plain-fallback path, and every side effect of arrival now lives inside it, behind its ownership guard: `await runStreamed(...)` resolves on stream *close* now, so the tab-title flash, the desktop Notification, the wizard's sessionStorage draft and its `?step=` param were all being deferred 150s behind the critique — the notification fired into exactly the silence it exists to break, and a reload at t=200s restored the form and took the finished plan off screen. The band deliberately unmounts at plan time rather than standing over a finished, editable plan for another two minutes; instead the review reports itself with one muted `role="status"` line in the panel footer for as long as it runs, and the days it actually rewrote carry the same unseen-day dots the chat already uses (`markUnseenDays`). Measured on a live 4-day Kyoto run: the critique rewrote day 3 alone, so it is one dot, not four. A mid-critique socket drop no longer fails the run out from under the plan — that gate covers the thrown path as well as the `error` frame. The card is read-only while streaming (`editable={!streamingPlan}`): a drag would land in `itinerary` and the next streamed stop would overwrite it |

## Enhancements

| Enhancement | Since | Developer | Notes |
|---|---|---|---|
| Hovering the map never moves the camera; hovering the itinerary still does | 2026-08-31 | Claude | The peek is gated on **where the hover came from**, not on which engine is drawing — `setHoveredIndex(index, source)` with `source` of `"itinerary"` or `"map"`. Pointing at a row in the plan leans the camera in, because you are reading and asking *where is this*; pointing at a card on the map only lights it up, because a pointer already on the card is already looking at the place and moving the ground under it is the map arguing with the hand. Same rule on Map and on Satellite. **The highlight is identical either way** — both surfaces write the same index, which is what has always made it bidirectional. Only the flight is conditional. `source` defaults to `"itinerary"`, so a new list of stops peeks without having to know this exists and `StopMarkerLayer` is the single caller that opts out. A map-sourced hover is routed through the peek's existing `null` path rather than returning early, and that detail is load-bearing: it is what makes a peek already in flight lean back *out* when the pointer leaves an itinerary row and lands on a marker card, instead of being stranded leaned in with its saved pose abandoned. `setHoveredIndex` stopped being a bare `useState` setter to carry the source, so it joins the other stable `useCallback`s already listed in the provider's dependency arrays for exactly that reason. An engine-level `MapRenderer.hoverPeek` flag was built first and removed: the distinction is the hover's origin, and a per-engine policy got the Satellite half wrong. Verified on both, panel open and camera close so that both a row and a card are hoverable — **map card**: MapLibre 1907m → 1907m, Cesium 1028m → 1028m, both 0.0%; **itinerary row**: MapLibre 1907m → 283m (the tight-pocket ring), Cesium 1028m → 405m |
| Stop names appear when the camera is close, not when the pointer is | 2026-08-31 | Claude | The reveal is keyed on **camera-to-stop distance**, not on zoom, and that is the design rather than an implementation detail. Zoom is a property of the view; this is a property of each stop, and on a pitched camera those are not the same thing — the near edge of the frame can be 800m out while the far edge is 9km, so a single zoom threshold either floods the horizon with names or hides the street you are standing over. Distance answers per marker, so names surface as the ground comes up to meet you. It also makes a pitch correction unnecessary rather than approximate: a true 3D distance to the point already *is* the pitch-corrected number, where zoom plus a pitch fudge is an estimate of it. And it runs off `MapRenderer.cameraDistanceM`, so **Satellite reveals on the same rule** — which the Map/Satellite toggle needs in order to stay honest. Band: hidden beyond 7000m, full within 2800m, which is what MapLibre zoom 13.5 → 14.8 corresponds to at this viewport and latitude — so a day framed for reading carries no names and coming in on a stop brings its neighbourhood up smoothly. Day cluster badges are exempt: they name a *group*, not a location, and they are the entire at-a-glance layer of a trip framed whole. **Hover no longer reveals anything.** `namedDay` was `focusedDay ?? hoveredDay`, so pointing at a day badge popped that day's names up; the badge still lifts its day out of the dim, but it no longer names anything. In exchange the overview now emits **every** day's stops as candidates, which the old rule could not afford (~45 serif names over one city) and the distance gate makes free — at that framing every one of them is beyond the band and draws nothing, and down on a street the stops on it are named whichever day they belong to. Written in the existing `postRender` loop straight to the DOM, cached per node so a still camera writes nothing, and applied *before* the declutter scan so a name nobody can see never takes the slot of one they can. `pointerEvents` follows, so a half-faded name cannot swallow a click meant for the map. The search-result labels take the same rule in a symbol layer's own vocabulary (`text-opacity`/`text-size` interpolated 13.5 → 14.8/16). **The `case` for the selected pin had to go inside the interpolation's outputs, not around it** — MapLibre rejects `["zoom"]` anywhere but as the direct input of a top-level `step`/`interpolate`, and the wrapped form is a style error that silently drops the property and leaves every label at full opacity. Caught by the `error` listener added with the engine, which is what it is for. Verified on both engines: whole trip framed → 0 names, 2 day badges; hovering a badge → still 0; zoomed onto a stop → names appear (5 MapLibre, 3 Cesium), zero console errors |
| One editing surface, wearing the result page's own chrome | 2026-08-31 | Claude | `SplitEditor` is the only way to edit a plan again, and it is now the same box as the page it opens from: `DockedPanel`'s open geometry at its **default** width rather than the `wide` one it briefly borrowed from the chat window — measured, panel and editor are `x=896 w=520 h=786` in both states. It carries the result page's chrome rather than a dialog's: the destination photograph under the same `BlurredPhotoLayer` treatment, the same `BudgetBar` reading the same itinerary (so a stop deleted here moves the bar the traveller was watching there), and the day strip in the same arrow-clipped shape, with 'All Days' added — the one state the result page has no equivalent of and this surface needs, since a cross-day drag wants both ends drawn. The in-place editing that briefly replaced it is gone, along with `EditableStopList` and `AutoTextarea`. Its case was that switching modes moves nothing, which was true and was measured — but it cost a documented near-copy of `StopList` that had to change in lockstep with it, and it only ever answered editing *within* a day. Adding a stop from the map, moving one between days with both drawn, and adding a day are the same job and had no home in it. `ItineraryCard` loses its `editing` flag, its stop/day mutation helpers and the per-day add controls; the day pencil and the card's Edit control both open the editor, and `showTripRoute` goes back to a constant `soloFocus` since there is no longer a mode for it to track. `ArrangeBoard.tsx` remains dead and can go with the next sweep |
| Editing happens in the result card itself, not in a second window <details><summary>Eliminated 2026-08-31 (Claude)</summary>Reverted by decision: editing is one surface again, and it is `SplitEditor` — see the enhancement below. The argument here was sound and the measurements hold; what it cost was a second copy of every stop row that had to be changed in lockstep with the first, and it answered only the half of editing that happens inside one day. Adding a stop from the map, moving one between days with both days drawn, and adding a day are the other half, and they had nowhere to live.</details> | 2026-08-31 | Claude | Third attempt at this surface and the first that does not open anything. `ArrangeBoard` was a full-screen board that covered the map; `SplitEditor` replaced it with a portalled panel at 62%/880px. Both fixed the previous complaint and both kept the underlying one: entering edit mode swapped one DOM tree for another, so the panel resized (520px → 880px), the scroll position reset, and the hero photo, budget bar and per-day spend band vanished and came back — the plan you read and the plan you edited were visibly two documents. `ItineraryCard` now has an `editing` flag instead. Hero, budget bar, day tabs and spend band sit outside it and never re-render because of it; inside the day panel each region swaps for an equivalent built from the *same* measurements. **Verified, not asserted:** panel box identical across the switch (x=896 w=520 h=786 both), stop avatar column identical (x=921 both), day heading 18.306px/600 both. Type scale matches by construction — name 16.272px/500 (base, `font-medium`), time/duration/why/note 14.238px (`text-sm`). **`EditableStopList` deliberately copies `StopList`'s geometry rather than sharing it** — `space-y-4`, `flex gap-3`, the 2.5rem avatar, the connector at `top-11 left-5` with `h-[calc(100%-1.75rem)]`. A shared row parameterised by `editing` is two components behind one prop: the read row is a single `<button>` wrapping the stop with a Typewriter and `whileInView` motion, the edit row is five fields plus a grip and a bin, cannot be a button (a button cannot contain a textarea), and must not animate or every keystroke that reorders would replay the entrance. Change one, change the other. **The grip and bin live in the day panel's own left/right padding, absolutely positioned** — in flow they would consume width from the flex row and shift the avatar, and with it the connector line, which is the exact shift this mode exists to avoid. **The route effect now keys off a structural signature** (`routeShape` — every stop's lat/lng in order) rather than `routeDays` identity. That is load-bearing: `routeDays` carries `name` and `time`, so keyed on it the effect would call `showTripRoute` per typed character — and that call is not a repaint, it reframes the camera, clears `hoveredIndex`/`activeIndex`, rebuilds every entity and re-runs the ~1.3s height probe. Typing a stop name would have flown the camera twenty times and dropped the highlight the row and its marker share. `soloFocus` goes false while editing so a cross-day move stays a decision you can see. `AutoTextarea` clears `height` before reading `scrollHeight` (or the box can only grow) and does it in `useLayoutEffect` (or every line-count change flashes). **Known and accepted:** an edit row is ~29px taller than its read row (+150px over a 5-stop day) because empty optional fields — `why`, `note` — must render to be fillable, where read mode renders nothing. Inherent to editing in place; nothing above the list moves. **`SplitEditor.tsx` is now unimported but deliberately left on disk** — the other session was editing it for the renderer refactor while this landed. It and `ArrangeBoard.tsx` are both dead and should be deleted together once that settles |
| Route ribbons shade like glass, and the day pool is recut to a curated Apple-grade set | 2026-08-28 | Claude | Two changes behind one intent. **The pool** keeps the `{core, glow}` pair structure and the `dayIndex % DAY_PALETTES.length` mapping from the row below, and swaps the five pairs for a curated set picked for the character of each pair rather than for even spacing on a hue wheel: 1 Pacific Teal / Frost `#00F2FE`/`#4FACFE`, 2 Hyper Violet / Neon Rose `#F355DA`/`#7000FF`, 3 Solar Ember / Tangerine `#FF6A00`/`#EE0979`, 4 Electric Emerald `#00F5A0`/`#00D9F5`, 5 Ultra Iris / Cobalt `#654EA3`/`#EAAFC8`. Emerald is a green chosen to read as light rather than vegetation — chroma past anything chlorophyll reaches, ambient pulling toward cyan rather than yellow. Solar Ember is still in the 0-50° band `--accent` owns, so `emphasisColorFor`'s white-swap guard still earns its keep unchanged. **Ultra Iris is the one entry that is not high-chroma**, and that is worth knowing before someone reports it as a bug: a twilight indigo at roughly a third the chroma of the other four, with an ambient *lighter* than its core — the only pair that inverts that relationship. Over dark water or shadowed terrain it is the casing rather than its own luminance keeping it legible. Raise the entry if a day 5 ever reads as missing rather than recessive; do not thicken the ribbon. **The shader** is the substantive part: `src/lib/glassRibbon.ts` registers a custom Cesium material fabric and a duck-typed `MaterialProperty`, replacing `PolylineOutlineMaterialProperty` on the arc cores. The reason it has to be a shader is that the casing, a rounded-body falloff and a specular streak are all functions of *position across the line's width*, and that value (`materialInput.st.t`) exists only in the fragment shader — no arrangement of stacked polylines can express it, because a stack of lines is a stack of flat strips. The casing half is a verbatim copy of Cesium's own PolylineOutline GLSL (same `halfInteriorWidth` split, same `czm_antialias` on the same distance term) so the edge is bit-identical to before and `ARC_CASING_WIDTH` keeps its "total across both edges" meaning; only the interior is new. Shading is computed in **interior** coordinates, not raw `st.t` — anchored to the full width the highlight would drift toward one edge and fall into the casing on the narrow taper segments. Registration goes through `Material._materialCache.addMaterial`, which is private API and the only registration point there is; `translucent: true` is declared rather than inferred, because Cesium infers it from the fabric's registration-time uniforms and every alpha that matters here arrives later from a `CallbackProperty` — inferred, the ribbon would batch opaque and never fade. Two limits stated rather than papered over: "along the top curve" is the ribbon's own consistent side, not world up, since a polyline carries no surface normal to orient a highlight against; and `RIBBON_SPECULAR_INTENSITY` has to stay low (0.34) because the body is already a saturated neon and a stronger streak blows to white exactly where the eye lands hardest. The sheen dims with the day's state on **rgb**, not alpha — the shader adds `specularColor.rgb` and never reads its alpha, so fading it the way every other material here fades would have done nothing. Cost class is unchanged: the property reports non-constant exactly as the outline material did, so the ribbon stays in the same per-material static batch and only uniforms are re-read. Verified in Chromium on a live 5-day Lisbon trip — shader compiles with no console errors, the streak is visible along each ribbon's shoulder at 3x, all five palettes resolve to their badges, and hovering a plan row still lights one card while the rest recede; 562 tests green |
| Days are drawn from a pool of neon core/glow palette pairs, not a flat colour ramp | 2026-08-28 | Claude | `DAY_COLOR_TOKENS` (six single tokens) became `DAY_PALETTES` (five `{core, glow}` pairs), and the pairing is the substantive part rather than the recolour. The **core** is the object — ribbon body, ring cores, beam core, and the day badge's border and text. The **glow** is the light coming off it — the ribbon's halo, the beam's bloom, the outermost radar ring, the outer footprint disc, the badge's outer bloom. The glow is a neighbouring hue, not a dimmer copy of the core, because that is what emission does as it falls off; a halo that is only a faded core reads as a blur. Pool: A Cyber Cyan `#00F3FF`/`#00A3FF`, B Neon Magenta `#FF007F`/`#9D00FF`, C Electric Amber `#FF6B00`/`#FF0055`, D Acid Lime `#00FF66`/`#00CC99`, E Plasma Violet `#B500FF`/`#4A00E0`. `buildRouteGeometry` now takes a `DayPalette` rather than a token string; `RouteDayCluster` carries both tokens so the badge and the geometry cannot pick different entries out of the pool, and `StopMarkerLayer` writes `--day-color` and `--day-glow` per element. The beam's cylinder became `BEAM_COLUMN_SLICES` stacked cylinders lerping core→glow with falling alpha, since a Cesium material is one uniform and cannot hold a gradient — the same reason the ground discs are stacked. **Mapping is `dayIndex % DAY_PALETTES.length`, not a hash, and that is a correctness choice:** modulo cannot give two *adjacent* days the same palette, a hash can, and separating a cluster from its neighbours is the whole job. Both are deterministic; only one is collision-free where it matters. **Electric Amber knowingly breaks a standing rule** — the 0-50° amber/red band belongs to `--accent` ("you are pointing at this") and `--map-pin-red` (the destination pin). Included by explicit request, with the collision handled rather than accepted: `emphasisColorFor` swaps the emphasis tint to white on any day whose core falls inside the accent's band, so hovering a day-3 stop still shows feedback instead of tinting amber onto amber. It is a pure exported function with tests on both branches, and it has exactly one fallback colour — a second warm palette would have nothing left to swap to. Verified in Chromium on a live 5-day Lisbon trip: days 1-4 resolve to cyan/magenta/amber/lime with matching badge borders and glow blooms; 562 tests green |
| The route moves: tapered ribbons, a travelling pulse, radar rings and light pillars | 2026-08-28 | Claude | The second pass on the neon route (row below), turning static geometry into something that reads as directional. **Taper:** Cesium has no per-vertex polyline width, so each arc is cut into `ARC_TAPER_SEGMENTS` consecutive polylines of falling width sharing their boundary vertices — 16px at the stop being left to 9px at the stop arrived at. Because the casing stays a fixed pixel count while the ribbon narrows, the neon *body* tapers much harder than the outline does: 11px of colour to 4px. The step at each join is `(start - end) / segments`, which is why that ratio is kept near 1px; the cost is that the core entity count per arc is multiplied by the segment count, and they are static so they batch. **Travelling pulse:** a `PolylineDashMaterialProperty` with a transparent `gapColor` and an animated `dashPattern`. That pattern is a 16-bit mask the shader tests per fragment (`maskTest = floor(dashPattern / pow(2, maskIndex))`), so rotating it one bit per step slides the lit band a sixteenth of a dash along the line, and rotating left moves it toward increasing vertex index — chronological. It is a uniform, so no geometry rebuilds; the alternative (re-sampling a moving window of positions) would put every arc in Cesium's dynamic batch. Two shader facts worth keeping: the dash is measured in **screen space** off `gl_FragCoord`, not along arclength, so spacing is constant in pixels at any zoom; and it rides `PULSE_LIFT_M` above the ribbon because coincident polylines z-fight. This does not reopen the old dashes-vs-solid decision — the ribbon underneath stays unbroken and the light moves *over* it. **Radar rings:** two rings became three, brightening in sequence from the inside out (`PULSE_RING_LAG` a third of a cycle), which reads as a wave travelling outward. Still alpha and not radius, for the dynamic-batch reason above. **Light pillars:** the stem is now a cylinder (world metres, so it fattens as the camera descends and vanishes from orbit) plus an emissive halo plus the hard core — a polyline-only beam is screen-space and therefore the same thickness at every range, which is exactly what a shaft of light is not. Wider at the base than the top so it pools into the ring rather than projecting out of it. **Labels:** stop names and place names moved to Rajdhani (`--font-map-label`, the second and final exception to the One Face Rule, scoped to the marker layer), which is condensed enough to buy back slots in the declutter scan since a name's cost there is horizontal pixels. Chips went to `rgb(10 14 23 / 0.75)` with `backdrop-filter: blur(8px)` on both badges and stop names. **The blur on stop names is a knowingly-taken cost, not an oversight:** the anchor's `transform` is rewritten every `postRender` frame and a filtered element re-rasters its backdrop when it moves, up to fifteen times a frame on a dense day. The 0.75 fill carries the contrast alone, so it is the first line to cut if a long trip stutters, and a receded card drops it already. **Hover and dim survive all of it, which was the explicit requirement:** every new material multiplies by the day's `stateAlpha` and every new entity is wired into `applyTints`/`applyWidths`/`reposition`. The ribbon body, pulse and rings take emphasis through their own per-frame callbacks and must never be written with a `ConstantProperty`, which would replace the callback and freeze the animation on first hover — noted in the code at both ends. Active-state widths scale each taper segment by a factor rather than to a common width, or selecting a day would flatten the taper exactly when it is being read most closely. Verified in Chromium against a live Kyoto trip: hovering a plan row lights that stop's card (white text, amber border, denser chip) and its adjacent arc amber while every other card recedes to 0.4 and drops its filter; 550 tests green |
| The route reads as neon ribbons over the satellite imagery, not lines on it | 2026-08-28 | Claude | A whole-map pass on legibility, and every part of it comes from the same fact: the globe is Google Photorealistic 3D Tiles, so any colour at any luminance can land behind any pixel of the route. **Palette:** the six-entry day ramp was Apple's systemBlue, a system green, a purple, a rose, a soft cyan and a pink — four of which the *background* can produce, so an arc over water, a park or a sand-toned block was the colour of the thing behind it. Replaced by six fully-saturated neons (`--route-neon-cyan` `#00F3FF`, `-magenta` `#FF007F`, `-violet` `#9D00FF`, `-spring` `#00FF9C`, `-indigo` `#5B6BFF`, `-orchid` `#FF5CF0`), 53-143° apart, none of them a hue terrain, water, rooftops or vegetation can make. `--route-blue` and `--route-casing`'s old `#0060DF` are gone, which retires the third of the three constraints from the original per-day-colour rejection — day 1 is no longer pinned to the single-day route's old colour — while the other two (nothing in the 0-50° amber/red band, `Stop` carries no day identity) still hold. **Geometry:** arcs went from a 3px solid stroke under a 6px halo to a 13px `PolylineOutlineMaterialProperty` — a ~7px neon body between two 3px `--route-casing` edges — over a 20px halo at `glowPower` 0.1. This deliberately reverses the halo half of *Route arcs quieted* (above): that row took brightness down because a thin bright line's only defence was brightness, and a dozen days of it bloomed into a smear. The casing removes that dependency — a near-black border supplies its own local contrast wherever it lands — so the halo could go wide and low-powered, which bleeds *less* into a neighbouring day than the old 6px at 0.12 despite covering three times the pixels. The altitude half of that row is untouched and still doing the decluttering. One trap worth writing down: Cesium's `outlineWidth` is the total across **both** edges (`halfInteriorWidth = 0.5 * (width - outlineWidth) / width`), so halve any per-edge number before using it. Stems got the same construction at 9px/4. **Stops:** the two flat discs became two discs at lower alpha under two emissive rings, the inner one pulsing on a staggered lag. Rings are polylines traced around the circle, not `ellipse.outline`, because `outlineWidth > 1` is silently ignored on Windows/ANGLE; the pulse is alpha-only because a `CallbackProperty` on `positions` would move every stop's geometry into Cesium's dynamic batch and rebuild it per frame. `reposition()` re-traces them, which is the one thing a new entity type here must never forget. **Labels:** day badges are now Orbitron (`--font-map-display`, the single documented exception to DESIGN.md's One Face Rule, scoped to `.marker-day-label` and nothing else) at 0.18em tracking, on a `--map-glass` chip edged and lit in the day's own neon. Stop names and place names take the same chip without the neon. The stop card's halo-only era ended for a measurable reason: the halo is a black shadow, and a black shadow under white text over a white plaza has nowhere to go. `backdrop-filter` is on the day badge **only** — one per day against up to fifteen stop names, and the anchor's `transform` is rewritten every `postRender` frame, so a filtered element there re-rasters its backdrop once per frame per card. It is the first thing to cut if a long trip stutters. Verified in Chromium against a live Kyoto trip at overview and street range: no console errors, badge computing to Orbitron/`#00F3FF`/`rgba(10,14,23,0.6)`, 527 tests green |
| A route is framed across its long axis, not down it | 2026-08-24 | Claude | Lifting the arcs fixed the day view and left the overview a tangle, because the framing pitch was -60 (30 degrees off straight down) and the heading was hardcoded to 0 — due north. Both throw away exactly the axis the lift puts the separation in, and the heading was the larger of the two: a day threads a corridor, Mumbai's runs almost due north-south, and looking along a corridor stacks every stop in it into one line no matter how high the arcs go. `routeViewHeadingDeg` now faces the route side-on, from the principal axis of its stops — the eigenvector of their 2x2 covariance, not the bounding box, which reports no axis for a diagonal route — with longitude scaled by cos(lat) as `buildDayClusters` does it. Of the two perpendicular headings the nearer north wins, so an east-west route keeps facing north exactly as before and only the corridor case turns; a cluster rounder than 1.25:1 keeps facing north too, or a day-tab click would spin the map to an axis picked out of noise. Pitch went to -45, the hero's own pose. Two knock-ons handled: the framing sphere now includes each arc's apex (grouped per day — two consecutive days are joined in the flat list by a pair no arc is drawn between) because pitching over spends frame height on exactly the elevation it has to contain; and the panel bias moved from world east onto the camera's own right vector, `(cos h, -sin h)` over (east, north), since those are the same vector only while the heading is north and at heading 90 an east-shifted aim point would push the route away from the camera instead of out from under the panel. Measured after: heading 89.9 on the whole-trip overview, 0 on day 4's east-west route, route still clear of the panel in both. The 2D control is unaffected — it is a pitch snap, not a SceneMode change |
| Route arcs quieted, and raised into real arches | 2026-08-24 | Claude | Two complaints, one cause. The halo came down (9px/α0.5/power0.2 → 6px/α0.22/power0.12) and the shimmering dashed core became a solid stroke with a tenth of the alpha swing (0.5-0.9 → 0.78-0.92), so a screen holding six days is lines rather than bloom. But brightness was never the reason overlapping days were indistinguishable — altitude was: `ARC_LIFT_RATIO` 0.08→0.30 with the cap at 30km instead of 180m, sampled 256 points instead of 96, so a day doubling back over its own ground separates vertically instead of merging. The apex now leaves frame above the marker cards at street range, deliberately reversing an earlier decision to keep it inside the band the cards occupy. One measured trap: at a 2px core the arcs stippled themselves back into dashes, because `resolutionScale` is `1.5/devicePixelRatio` so a 2x display renders the canvas at 0.75 and a 2px line lands on 1.5 buffer pixels that an upscale with no FXAA breaks into dots — 3px is solid, and the highway lines sharing the buffer were the control that showed width was the variable. `arcLift()` is exported and tested |
| Play tour pulls the itinerary list along with the camera | 2026-08-24 | Claude | The tour has called `setActiveIndex` since it was written and the matching row already lit up, but nothing scrolled, so the camera flew to stop 6 while the panel still showed stops 1-3. `StopList` now follows `activeIndex` — selection only, never `hoveredIndex`, or sweeping the pointer down the rows would yank the list under the cursor. A globe marker click gets the same follow for free. Not `scrollIntoView` (it walks every scrollable ancestor; the day-tab strip's comment records that shoving the docked panel down ~100px) and not a one-shot `scrollTo` either: the sticky hero *shrinks* as the panel scrolls, so the row's position is a function of the scroll offset and a target measured before the jump overshot the tour's last stop clean off the bottom of the panel. A rAF loop eases a fifth of a freshly measured remainder per frame, which converges through any layout shift above the row, with a 700ms backstop for the last row, where centring would mean scrolling past the bottom and the remainder never reaches zero |
| The globe goes to dusk and night with the stop you are looking at | 2026-08-24 | Claude | Click a 9pm dinner and the city darkens; step Play tour back to a 7am breakfast and it comes up. Four bands (`dayPhase` in `mapRoute`, dawn 05:30 / day 07:30 / dusk 17:00 / night 19:30) off the clock hour in `Stop.time`, which now rides into `RouteStop` — fixed bands rather than real sunrise/sunset, which Open-Meteo returns but nothing persists into `Itinerary`. Driven by `hoveredIndex ?? activeIndex`, the same pair every other paired highlight here reads. It was `activeIndex` alone for one revision, to keep a pointer sweep down the rows from strobing the planet, and that was the wrong fix twice over: `showTripRoute` clears the selection on every day switch, so on a freshly opened day hover is the *only* thing pointing at anything and the tint simply never fired. The right fix is a 250ms dwell (`useDwelledPhase`, the same threshold and reasoning as `PEEK_DWELL_MS`), keyed on the resolved phase rather than the stop index — so sweeping five afternoon stops wants `"day"` throughout and commits nothing, and only actually resting on an evening stop turns the lights down. Measured: `day` held for 248ms, `night` at 275ms. Implemented as one `mix-blend-mode: multiply` sheet over the canvas, **not** in Cesium: Google's photorealistic tiles are daylight photography with the shadows baked in, so `scene.light` is ignored by their unlit materials and the one lever that does bite — re-tinting `tileset.style`'s `color()` — re-evaluates the style across every resident tile, which is not a thing to do 60 times a second. The sheet is a composited layer the GPU already owns and CSS gives the crossfade away; `day` is `transparent`, and a fully transparent multiply is a no-op, so daylight costs nothing and the landing page's decorative globe needs no special case. Only `background-color` animates, because `mix-blend-mode` snaps. Lives inside the z-0 globe container so it sits above the canvas and below the marker layer at z-5 — the world dims, the cards and panel keep their real colours |
| The hover peek's zoom is chosen per stop, from concentric rings around it | 2026-08-24 | Claude | `peekRangeM()` in `peekRange.ts`, 25 tests in `peekRange.test.mjs`. The peek was one relative number — half the camera's current distance, floored — which is the right *shape* of rule (how much zoom reads as a lot depends on where you started) but blind to what decides whether the peek said anything: a day framed at 8km halves to 4km, and a pocket of five places 150m apart is the same blob at 4km it was at 8km. The pointer is on one row and the globe answers with five. Now: rings double outward from the stop being pointed at, and the innermost ring holding another stop of that day names an **absolute** range — 250m for a neighbour inside 100m, then 350 / 700 / 1400 / 2800 / 5600 / 11000m, past which there is no pocket and the plain halving answers. Each range is 2.5x the *geometric centre* of its ring (at range R the camera sees ~1.15·R of ground, so 2.5·d puts the neighbour ~28% of the frame away, unmistakably a second stem); anchoring on the outer edge instead was tried and framed a 128m neighbour as if it were 200m off, which is the error in the one direction that matters. **Absolute is the whole point.** The version before this multiplied the neighbour distance but capped how much closer than the pre-peek pose a peek could go, and measured across every stop of every saved trip that cap — not the pocket — decided 33 of 57 stops: a pair 52m apart resolved to 5099m on a day whose framing happened to include a far shrine, and to 300m on a tightly framed day. Whether a pocket became legible depended on something invisible to the traveller, which is exactly the "works sometimes" this rule exists to remove. Rings also mean every stop in one pocket shares a range, so sweeping four rows of it pans at a steady altitude instead of re-scaling per row. The statistic is the **nearest** separable neighbour, not an average of the nearest few — an average was written first and erased the case that motivates the rule (Nishiki Market and the sushi counter 76m away, on a day whose other stops are kilometres off). Neighbours under 25m are dropped: a café on a temple's own coordinates is a real itinerary shape and no zoom separates two stops at one point. Other days are excluded — drawn, but dimmed and unnamed. One floor remains, `PEEK_MIN_HEIGHT_M` 200m of *height* above the target, converted through the pitch the peek holds (a range is a distance along the view direction, so 250m is 217m high at -60° and 73m at -17°, and the traveller can drag the camera as low as they like); with the stem's 150m that is ~350m of real clearance. Dropping the cap means dives up to ~50x, so `peekFlightSeconds()` grows the flight an eighth of a second per doubling, capped at 1.8s — 40x in the 0.8s a 2x lean takes is a jump cut, and the way back replays the way in's duration. Verified on the globe: a 19km-framed Lisbon day peeking to 350m lands on Praça da Figueira at building level with the pocket's other stop distinctly separate, and Kyoto's 76m pair reads as two places at street level. **Cost, accepted deliberately:** a deep peek streams a full tile set in each direction, where the old cap traded legibility for that bandwidth. The 300ms dwell is what keeps it from happening by accident |
| The hover peek only fires when the pointer, not the page, arrived on the row | 2026-08-24 | Claude | Resting on an itinerary row leans the camera a relative 2x toward that stop (`PEEK_*` in `mapCamera.tsx`). Two ways that fired when nobody asked. **Scrolling:** a list moving under a still pointer emits exactly the `pointerenter`/`pointerleave` a hover does, and wheel scrolling arrives in discrete ticks whose gaps outlast the 300ms dwell — so one flick down a day peeked at, and flew home from, several rows nobody looked at, each one a camera flight plus a burst of tile requests. Now both directions wait for `SCROLL_SETTLE_MS` (350ms) of scroll quiet *and* for pointer movement recorded after it; `pointermove` only counts when the coordinates actually changed, because browsers report content moving under a still pointer as a move. A row held back that way is re-armed by the next real nudge (`peekRearmRef`) — the hovered row does not change on that nudge, so without the hook there is no state change to re-run the effect and the peek would stay dead as long as the pointer sat where the scroll left it. **Row-to-row:** rows do not touch, so the pointer is over *no* row while it crosses the gap between two, and acting on that immediately pulled the camera all the way out and dived back in — the one thing the peek is meant not to look like. The un-hover now waits `PEEK_LEAVE_GRACE_MS` (220ms), which a landing hover supersedes through the effect's cleanup, so the camera crosses sideways from one stop to the next. Leaving the list for good still reads as immediate: the 0.8s flight dwarfs the delay. Scroll/pointer/key timestamps live in refs, never state — at wheel rates, state would re-render every consumer of the camera context per frame. Verified in Chromium by sampling the marker layer's screen positions, which are projected from the camera every frame: 0px of camera movement through a six-tick scroll (the highlight still follows the rows), and a 594→479 crossing between two stops' peek poses that never returns to the 443 home framing |
| FeaturedPlans' card CTA goes full width on a phone | 2026-08-22 | Claude | Measured off the reference rather than guessed, and it is a breakpoint rule, not a flat change: at 375px its card CTA is 335px inside a 375px card with `justify-content: center`, and at 1154px it is 153px in a 1090px card. So `w-full justify-center` with `sm:w-fit sm:justify-start` — `sm` because that is where this card's own layout flips, from a text column that is the whole card to one that is half of it. Centred rather than `justify-between`, which is what the reference computes and what the app's two other full-width pills (the Hero CTA at portrait, the mobile menu's) already do. After: 335px at 375px, matching the reference exactly, and unchanged at 640px and up |
| One vertical nav rule below `sm`, and `SiteFooter` on `/trips` and `/profile` | 2026-08-22 | Claude | The wordmark's rule is `sm:`-gated so a 375px bar draws one vertical hairline plus the bottom one, not two either side of a middle cell that is empty on `/` — the reference's own phone arrangement rules off only the toggle. And `SiteFooter` moved out of `blue-hour/` into `components/` (it takes no props and knows nothing about a scene; that folder only described who imported it) and now renders on the landing, `/trips` and `/profile`. Each host cancels its own `<main>` padding around it with the `-mx-5 -mb-*` technique `ScrollStory` and `/trips`' hero already use; on `/trips` that cancel is conditional on `trips.length > 0`, mirroring the hero's, since the empty state's `<main>` has no padding. Measured after: `footerWidth === overlayWidth` and zero dead space below the footer on both new hosts, both engines |
| Three-cell navbar, and `/profile`'s header inside its own cell | 2026-08-22 | Claude | The bar is the reference's three cells now: wordmark │ route links │ Profile. Two declines from the previous pass reversed, both wrong for the same reason — the trailing rule is the mirror of the wordmark's, not a CTA frame, and its absence was *why* the first rule had to be conditional: `/profile` has no outbound links, so there was nothing to divide from and that one route rendered a bare strip. `hasNavLinks` became `isUserFacing` (still a positive list, not `!isInternal` — the negation would hand the built-in 404 a Profile link, and Next renders that 404 inside the root layout). Profile now renders on `/profile` too, as the current page via `navLink(true)` + `aria-current="page"`. Caught in design: `hidden` on the Profile link is a **silent no-op** — `navLinkBase` already sets `inline-flex` and Tailwind emits display utilities alphabetically, `.hidden` at byte 16947 vs `.inline-flex` at 17026 in this app's own CSS — so the breakpoint rides a wrapper. On `/profile`, `SectionOpener` moved inside the form column with a new `rule={false}` prop and the grid took `border-y`, so the vertical rule runs the region's whole height instead of starting below a full-width heading. `browser-matrix` gained a `nav` column (cells/trailing-controls) because nothing else covers this bar |
| `/profile` restyled after the reference's contacts page, with a recent-trips preview | 2026-08-20 | Claude | Four moves, all reusing what already shipped. `SectionOpener` for the header — the label sits *beside* the heading, which is the one exception the No-Kicker Rule was amended for; an eyebrow stacked above it, which is literally what the reference does, is still banned. `.profile-glass` deleted outright rather than restyled: the Mounted-Surface Gate had already taken the globe off this route, leaving a tinted shadowed card floating on flat canvas for no reason. Two-column split above `lg` with a `sticky` photography column (the reference's contact form is short enough not to need that; this page is not). And a recent-trips preview — lead photo + up to two thumbnails + "See all in My memories" — reusing `listTrips`, `toTripSummary`, `usePlacePhoto` and the `contrast-105 saturate-110` grade from `ItineraryCard`/`MemoryCard`, with `.scene-void` as the zero-trips fallback |
| `.glass-itinerary.is-opaque` — one frost-off modifier for every globe-less surface | 2026-08-20 | Claude | Three surfaces were still running a 56px `backdrop-filter` over flat `--canvas` after the globe was gated: the plan step's form card, `/trip/latest`'s empty-database card, and `/trip/[id]`'s not-found card. Rather than a third scoped override, the one-off `.glass-itinerary.memory-card` rule added for `/trips` was generalised into a shared `.is-opaque` modifier and applied at all four call sites — same rule count, four surfaces instead of one. Written at two classes so it cannot lose a source-order coin toss to the single-class rule it overrides. Measured blur surfaces: plan step 3 -> 2, not-found 2 -> 1, `/trip/latest` 2 -> 1. Deliberately excluded: `.glass-nav` (content scrolls under it on every route) and `ConfirmDialog`'s panel (its own `::backdrop` is 55% black plus a 4px blur over the live page, so there is something behind it to frost) |
| Cesium boots on two surfaces instead of eight | 2026-08-20 | Claude | Measured on a production build, cold load at 1440x900, first 6s: `/profile` was paying **5410ms of long tasks across 32 tasks** (~90% main-thread saturation), a 2287KB chunk, 33 `/cesium/` asset requests, 1100KB of Google tiles and a live WebGL2 context — for a settings form. `/trips` 5457ms. `/` 1525ms (lower only because HeroPoster paused the render loop behind the opaque beats; the boot cost was paid in full). Deleted `globeVisibility.ts` and replaced it with `globeWanted` on `MapCameraProvider`, declared by `TripView` and `HomeView` via `useGlobeOnScreen` — a pathname cannot express the predicate, since `/` serves three steps from local state and `/trip/<unknown-id>` renders a glass card. Latched at generation *start*, so the 2.3MB import lands inside a ~150s loader and the pending destination flight replays on `setViewer`, opening the result view already framed. Viewer is built once and never destroyed (a swap is unrecoverable); non-globe surfaces get `visibility: hidden`. After: 0ms of long tasks and no canvas on `/`, `/trips`, `/profile`; `/profile` 2732KB/115 requests -> 445KB/24 |
| The landing lost the globe; `/profile` and `/trips` lost the glass | 2026-08-20 | Claude | HeroPoster sat on the live globe and was the only one of four beats where globe pixels reached the eye. It now sits on `.scene-void`, extracted from `/trips`' empty state so the two share one recipe — radial, centred, no top hairline, full `min-h-dvh`, which is what separates "arrival" from a third `.scene-band`. It also dropped `.hero-legible`: that text-shadow existed to keep an unobstructed globe, and the ratio is now measurable at ~12:1. With a flat `--canvas` behind them, `.profile-glass` and `/trips`' memory cards went opaque — a 56px blur of a flat colour is a blur of nothing, the argument `.scene-photo-frame` and TierPicker's price badge already made. Contrast rose: #cbd5e1 from unmeasurable-over-terrain to 9.7:1 on `/profile` and 12.1:1 on `/trips`. Blur surfaces: `/profile` 2 -> 1, `/trips` 1+2N -> 1+N |
| Landing `Hero` rebuilt with zero per-frame JS; globe render loop paused while covered | 2026-08-19 | Claude | A Chrome trace of a real session showed every scrolling frame resolving on the main thread (`SCROLL_MAIN_THREAD` on 1688 of 3426) rather than the compositor — frames were late, not dropped (2.1%). `Hero` lost all five infinite GSAP tweens, the `pointermove` cursor parallax and its `[perspective:2300px]`, and now runs no JS at all: the one ambient loop is `.hero-light` (a warm wash on `--accent` translated across the still photograph), the three fog banks are static, and the scroll cue's fade is the app's first `animation-timeline: scroll()` behind `@supports`. Fog lost `filter: blur()` and `will-change` — a moving blurred layer re-rasters forever — with the softness moved into the gradients' own falloff. `HeroPoster` now pauses Cesium's render loop (976ms of that trace, its largest entry) via IntersectionObserver while the opaque beats cover the globe. Also fixed a latent bug: `[animation-delay:Nms]` generates no CSS here, so this block's documented 0/90/180ms stagger had never run |
| Full visual theme pass: warm `stone`/`orange` palette, consistent `rounded-xl`/`rounded-lg` + `shadow-sm` card system, input focus rings | 2026-08-05 | Aryan | Replaced plain `gray`/`white`/ad-hoc-radius styling |
| `BudgetBar` uses green when under budget, red when over | 2026-08-05 | Aryan | Previously always orange regardless of budget status |
| Page `<title>`/description fixed to "TripMate" copy | 2026-08-05 | Aryan | Was still the default "Create Next App" boilerplate |
| Geist font actually renders | 2026-08-05 | Aryan | Was loaded via `next/font` but silently overridden — see Bugs |
| `DayList` day cards get a soft sky-blue tint on high-rain (≥50%) or extreme-temperature days | 2026-08-05 | Aryan | Derived client-side from `weatherDetail`, no new model-authored field needed |
| `DayList`/`ItineraryMap` prop signatures grew (`budget`, `hoveredStop`, `onHoverStop`) | 2026-08-05 | Aryan | Both `src/app/page.tsx` and `src/app/trip/[id]/page.tsx` updated to pass them through |
| Added `lucide-react` dependency | 2026-08-05 | Aryan | Used by `WeatherIcon` |
| `PreferenceStep` restyled to the `.glass-panel` treatment | 2026-08-05 | Claude | It's only ever rendered floating over the home page globe now, alongside `TripSearchForm` |
| Added `cesium` and `framer-motion` dependencies | 2026-08-05 | Claude | `cesium` needs its static Workers/Assets/ThirdParty/Widgets copied into `public/cesium` — see `scripts/copy-cesium-assets.mjs`, wired as a `postinstall` |
| `TripSearchForm` relayout: vertical centered card → wide, top-aligned horizontal glass bar (`bg-slate-900/60 backdrop-blur-xl border-white/10 shadow-2xl`), fields laid out inline on desktop (destination / start / end / budget / CTA) | 2026-08-05 | Aryan | Home page search step in `page.tsx` also moved from vertically-centered to top-aligned (`pt-2 md:pt-6`, no forced `min-h`) so the globe stays the unobscured hero backdrop instead of being covered by a centered card |
| `TripSearchForm` typography/contrast pass: heading gets a text-shadow for legibility over bright globe imagery, subheading bumped to `text-white/85`, labels to `rgba(240,240,240,0.9)`, inputs to `bg-slate-900/65` + `border-white/15` + `focus:border-orange-500/80` + `placeholder:text-white/40` | 2026-08-05 | Aryan | |
| "Plan My Trip" CTA upgraded to a gradient button (`from-orange-500 to-amber-600`) with hover scale/brightness and a glowing orange shadow | 2026-08-05 | Aryan | Previously a flat `bg-orange-600` button |
| `useCesiumViewer` gained `sampleAverageColor()`, and the viewer now sets `contextOptions: { webgl: { preserveDrawingBuffer: true } }` | 2026-08-05 | Aryan | Draws the WebGL canvas onto an offscreen 2D canvas and averages a small box via `getImageData` — needed for adaptive header contrast. `preserveDrawingBuffer` is required or the buffer is cleared before any readback sees it |
| `PageHeader` gained `variant`/`isDark` props | 2026-08-05 | Aryan | Defaults to the existing `"light"` behavior everywhere except the home page, which opts into `"adaptive"` — zero risk to `/trips`, `/trip/[id]`, `/llm-trace` |
| `BackButton` shared component; `/profile` gains a back control; "Refine with AI" moved to the top of both result panels | 2026-08-16 | Claude | The chevron + ghost-pill markup lived inline in `PlaceDetailPanel`; extracted once `/profile` needed the third copy. `/profile` pops history (`router.back()`, falling back to `/`) rather than linking a fixed destination, since it's reachable from both `/` and `/trip/[id]`. Refine moved off the panel foot, where it sat under the `LlmTraceFab` and behind a full scroll of a long trip — it now shares the home result view's existing Back row, and sits above the card on `/trip/[id]`, which has no such row |
| Stop list spans the full itinerary card; the desktop photo column beside it is gone | 2026-08-18 | Claude | `StackedPhoto` rendered three Wikipedia thumbnails in a fixed `w-28` column at `lg`+, so the stop names wrapped inside roughly 128px less than the card actually had. The list is now a direct child of the day panel. The per-row circular `StopAvatar` thumbnails in `StopList` are untouched — they sit *inside* the list and removing them widens nothing |
| Chat panel renders guardrail alerts and the day scope of each turn | 2026-08-18 | Claude | An assistant turn now shows, in order: `Updated day 2` / `Updated days 1 and 3` (from the route's `daysModified`, so scope is stated rather than implied), the change list, then any `warnings` as ⚠️ rows on an amber wash. Warnings read louder than the changes they qualify because the point of one is that the plan is now something the traveler may not have intended. Ops the applier rejected are surfaced too instead of reading as "done" |
| Drag-and-drop stop rearranging, with live re-timing and instant guardrails | 2026-08-18 | Claude | Drag a stop by its grip handle to reorder a day, or drop it on any **day tab** to move it to that day (the card shows one day at a time, so the tabs are the only always-visible cross-day target — and any day is one drop away, unlike an edge-advance gesture). `@dnd-kit/core` + `/sortable` chosen over framer-motion's `Reorder` (single-container only) and native HTML5 DnD (no touch events at all). A handle rather than a draggable row, because the row is already a button that opens the stop's detail; 6px activation distance so a tap still reads as a tap. dnd-kit owns `transform` on an outer wrapper and framer-motion keeps the reveal on the inner node — sharing one node makes a dragged row fight its own entrance animation. `DragOverlay` renders the moving chip in a portal so it isn't clipped by the card's `overflow-hidden` when dragged up to the tabs. Cross-day drops switch the card to the target day, so the stop is watched landing rather than just vanishing |
| Drag-and-drop in Focus Mode's day preview too | 2026-08-18 | Claude | The preview pane beside the chat was read-only, so the one surface open *while* editing was the one that couldn't be rearranged. `DayTimeline` rows now carry the same grip handle as `StopList`. Focus Mode has no day tabs to drop onto, so the preview's own **stepper arrows** become the cross-day targets while a stop is in flight (`Drop on an arrow to move days` replaces the day counter mid-drag) — which is the "drag it right to send it to the next day" gesture, made explicit instead of an edge-hover to discover. Drops go through `applyDraft`, so they join the existing dirty/Save/Cancel buffer and commit only on Save, unlike the main card where a drop persists immediately. Guardrail findings render under the timeline, judging the draft before it is ever saved. Disabled while a chat turn is in flight (`draggable={!busy}`) |
| Hold a dragged stop over a day to open it, then drop into an exact slot | 2026-08-19 | Claude | Dropping straight onto a day tab can only append by clock time. Dwelling ~420ms over another day now switches the view to it mid-drag, so the target day's stops are on screen and the stop can be placed in a chosen position — the folder-opens-when-you-hover pattern. The dwell is what stops a drag *past* day 3 en route to day 4 from yanking the view. Required drag ids to carry the day (`stop:<day>:<index>`, was `stop:<index>`): after a switch the old ids named different stops. The dragged stop is also held in component state rather than read from the rendered list, since its source day is no longer on screen once the view moves |
| Numbered day strip in Focus Mode's preview | 2026-08-19 | Claude | The prev/next arrows only reach the adjacent day, so moving a stop from day 1 to day 9 meant hovering through eight of them. Trips longer than two days now get a scrollable row of numbered day chips, each clickable and each a drop target. Scrollable rather than wrapping, so the header height can't change mid-drag and move the targets out from under the pointer |
| `dwellVerdict()` — the hover-to-open rule as a pure function | 2026-08-19 | Claude | Extracted from `useDayDwellSwitch` because the rule is what kept breaking, not the timer: a countdown restarted on every pointer event never elapses, and a switch permitted immediately after a switch oscillates. Both were found by hand in the browser; they are now asserts (including day 0, the falsy trap in this shape of code) |
| The day ✨ icon and "Refine with AI" now open the same window | 2026-08-19 | Claude | The icon opened `scope: "day"`, which looked identical to the whole-trip window but had no day navigation and a chat forbidden from touching other days — so it read as the same feature quietly not working. Both entry points now open trip scope; the icon just starts on the day it was clicked from. The day-scoped path stays in `FocusEditMode`/`buildChatEditPrompt` (it is a real guarantee worth keeping) but is no longer reachable from the UI |
| In-card drag handles (arrange mode in the stop list) | <details><summary>Eliminated 2026-08-19 (Claude)</summary>Handles lived in the itinerary card's own stop list, gated behind a day's pencil. The card shows one day at a time, so a cross-day move meant dragging onto a day *tab* and holding it there until that day opened — a gesture that needed explaining, and that never showed the traveller the day they were aiming at until after they had committed. Superseded by the full-page ArrangeBoard below, where both ends of a move are visible for the whole gesture. `StopList` is read-only again and no longer imports dnd-kit.</details> | 2026-08-19 | Claude | |
| Grip handle moved clear of the stop avatar | 2026-08-19 | Claude | It was `absolute -left-1` with `w-5`, so it spanned −4px→+16px while the avatar starts at 0 — the handle was sitting on top of the icon it was meant to sit beside, not merely near it. Now `-left-8` against the list's `pl-8`, which puts it fully outside the row with a 12px gap |
| `ArrangeBoard` — the whole itinerary on one page, drag anything anywhere | 2026-08-19 | Claude | A day's pencil (or the card's "Arrange days" button) opens a full-screen board: one column per day, every day on screen at once, so a stop can be dragged between any two days directly with both ends visible. Days are a **wrapping grid** (`auto-fill, minmax(min(100%,16rem), 1fr)`) rather than one horizontally-scrolling row — on a ten-day trip the later days land on further rows and are reached by scrolling down, the direction every other page scrolls, instead of hiding day 10 behind a horizontal scrollbar. The column itself is a drop target, not just its cards, or a day with nothing in it would be unreachable. Whole cards are draggable here (no grip to hunt for) because nothing on this surface competes for the click, unlike the card's list where the row opens a stop's detail. **Portalled to `document.body`**: it is opened from inside a blurred docked panel, and `position: fixed` is contained by any ancestor with a filter, so rendered in place it would be trapped in a 520px column. Per-day rename moved here as an uncontrolled title field; guardrail findings sit in the column of the day they describe, budget findings under the board. Escape or Done closes; background scroll is locked while open |

| Three-state globe: `off` / `static` / `live` (`GlobeMode` in `mapCamera`) | 2026-08-19 | Aryan | `GlobeBackground` still never unmounts (the Persistent Globe Rule holds), but the viewer inside it is now built on demand and handed over in two stages. `off` is no viewer at all (the poster stands in) and is where every route starts. `static` is the real globe parked at `HERO_VIEW` with `enableInputs = false` — the landing page's backdrop, which under `requestRenderMode` streams in and then draws nothing at all. `live` enables input, and `flyTo` is its single activation signal, so every real entry point already routes through it: "Plan a trip", choosing a destination, clicking a stop or marker, Play tour. `static` ⇄ `live` is one flag on the existing viewer and deliberately **not** a remount — the mount effect depends on a boolean (`globeMode !== "off"`), because a rebuild costs seconds of re-fetched tiles. `resetToHome` demotes `live` → `static` and flies home rather than tearing down. `showDayRoute`/`showHighways` never promote: they queue into the pending-request refs that already existed for the cold-load race, so a trip page holds its route until someone actually wants 3D. `MapControls`' own `import("cesium")` moved off a route check onto `ready` for the same reason the rest of this exists |
| `requestRenderMode` — the globe renders on change, not on a clock | 2026-08-19 | Aryan | Cesium's default is a 60fps loop redrawing an identical frame for as long as the tab is open; on a stationary city overview that is a continuously busy GPU for a picture that never changes. With `requestRenderMode: true` + `maximumRenderTimeChange: Infinity` a still camera issues no draw calls at all (the second flag removes the clock-advance trigger, which is pure waste for a scene with nothing time-varying in it). The cost is that scene changes which *don't* move the camera must ask: `requestRender()` now follows entity adds, the hover-emphasis material swap, the delayed route-altitude correction and the Overpass highway response. Tile streaming is handled separately and *not* per arriving tile — see the refinement-stall bug below for why that distinction is the whole ballgame. Camera flights are unaffected: their tween runs in `initializeFrame`, outside this gate |
| Idle auto-rotation, scoped to the one section that shows the globe | 2026-08-19 | Aryan | Removed outright in the first pass of this work (it was the single most expensive thing the app did — a rotating camera renders every frame by definition, which defeats `requestRenderMode` and keeps Google's tile traversal re-evaluating), then restored, because the landing story's reveal is *designed* around arriving at a moving Earth after three bands of stills. What changed is the scope, not the motion: same 0.05 rad/s, but driven from `HeroPoster` via an IntersectionObserver so it runs only while that section is on screen — Hero, ImageRow and HowItWorks are opaque bands, and turning the globe behind them is work nobody can see. Also gated to `static` mode (in `live` the camera belongs to the user), skipped entirely under `prefers-reduced-motion`, and stepped at `SPIN_FPS` 30 rather than every frame, which halves the cost for a 0.1°-per-step difference. The rAF delta is clamped at 100ms: rAF stops in a background tab, and integrating the raw gap would snap the globe through everything it missed — the hazard the old `lastTime` reset half-covered. `stopAutoRotate`/`startAutoRotate` and their call sites in `mapCamera`/`MapControls` stayed gone; nothing needs to reach into the viewer to stop a spin that is a piece of React state |
| The landing hero renders one frame of Earth, then idles | 2026-08-19 | Aryan | The hero's backdrop is a real photorealistic globe again, but in `static` mode: parked at `HERO_VIEW`, input disabled, and under `requestRenderMode` it issues draw calls only while its tiles stream in and then goes to zero for as long as the copy is being read. Input is off rather than merely unused — a wheel event over one of the scroll story's own gaps would otherwise zoom the Earth *and* wake the render loop until the inertia settled, which is the exact cost this is avoiding. "Plan a trip" is what hands the camera over (`activateGlobe`), on the reasoning that the button is the moment the globe stops being scenery: a globe you cannot grab, on the screen that asks you to choose a place, reads as a broken one. `HERO_VIEW` moved into `mapCamera` and is imported by `GlobeBackground`, so the pose the viewer opens at and the pose `resetToHome` flies back to can no longer drift apart |
| Cross-fade between the poster and the canvas | 2026-08-19 | Aryan | The canvas fades in on `tileset.initialTilesLoaded` rather than on viewer construction — revealing at construction shows a blank blue sphere for the seconds Google's tiles take, which reads as a bug rather than as loading. A 4s timer backstops it so a stalled or rate-limited tile fetch can't strand the app on a poster the camera has already flown away from. On teardown the poster is restored *before* the viewer is destroyed, so the fade runs over a still-painted scene instead of over the bare page background |
| Hover peek: resting on an itinerary row leans the camera in toward that stop | 2026-08-19 | Aryan | Hovering already emphasised the stop's stem on the globe and cost one frame; it now also leans the camera in, and leaving the list flies back to wherever the camera was before the run of peeks began. The zoom is **relative, not a destination**: half the distance between the pre-peek camera and the stop (`PEEK_ZOOM_FACTOR` 2), holding the pre-peek heading and pitch so only the distance changes. A fixed 1,500m altitude shipped first and was wrong — from a day framed at 4km it is a 2.7x dive, from a tight day already at 2km it barely moves, and snapping the pitch to the click's -35° made it read as the camera going somewhere rather than leaning in. The anchor is deliberately the *pre-peek* pose and not the live camera: halving from the live camera compounds, so hovering four rows would be 2x, 4x, 8x, 16x and end inside the pavement. `PEEK_MIN_RANGE_M` (800m) floors it, which also keeps it clear of the click's 600m so selecting a stop after hovering it still visibly goes somewhere. Moving between rows flies directly between peek poses rather than pulling out and diving back in: same read, half the flight time and half the tiles. Three gates, each a cost decision: a viewer must already exist in `live` mode (hover must never be the thing that builds one — on a trip page still showing its poster, hover does nothing), the pointer must rest `PEEK_DWELL_MS` (300ms) first so sweeping a list costs zero, and it is skipped under `prefers-reduced-motion` since it is unrequested camera motion in response to a pointer. `cancelPeek()` runs from `flyTo`, `showDayRoute` and `resetToHome`, without which a click landing mid-dwell would be followed by the peek flying elsewhere, and the next unhover would drag the camera off the stop just selected. **Cost:** ~0.8s of flight plus tile settle per *settled* hover, then back to zero draw calls; a sweep costs nothing |

## Bugs

| Bug | Found | Fixed | Developer | Notes |
|---|---|---|---|---|
| The 2D/3D toggle's "3D" returned to a pitch the app uses nowhere | 2026-08-31 | 2026-08-31 | Claude | `MapControls` held a local `OBLIQUE_PITCH = -60` with a comment saying it matched the day-route framing. It did when it was written, and stopped when `ROUTE_FRAME_PITCH_DEG` moved to **-45** for a documented reason — at 30° off straight down a day's arcs project back onto the ground line they span, which is the whole point of lifting them. Nothing failed and nothing said so: pressing 2D and then 3D left the camera 15° steeper than the pose a route is framed at and the pose the landing hero opens on, so "go back to 3D" quietly meant "go somewhere the app never puts you". The tilt slider's `defaultValue={60}` carried the same stale number, so the thumb also started 15° off the real camera until the first readout frame moved it. Both now read `ROUTE_FRAME_PITCH_DEG` — imported, not restated, so it cannot drift again without the framing drifting with it. Verified on both engines: framed **-45** → 2D **-89.9** → 3D **-45**, slider 45 |
| "Back to itinerary" left the camera on the city instead of the day it came from | 2026-08-25 | 2026-08-25 | Claude | `closeDetail` flew to `destinationCoords` at `DESTINATION_HEIGHT_M` — 15km, north-up, over the whole destination, plus the red destination pin. That read as "back out" for as long as a day's framing was also near-nadir and north-up, and stopped the moment a day got its own heading and pitch (`routeViewHeadingDeg`, `ROUTE_FRAME_PITCH_DEG`): closing a stop discarded the side-on view of the open day and landed somewhere ~24km of longitude away, with the day's route almost entirely off-frame and the panel still showing that day. Fixed by lifting the framing block out of `showTripRoute` into `flyToRouteFraming` and replaying it from a new `lastRouteRef` (kept after a draw, unlike `pendingRouteRef`, which is cleared once replayed). Reframes rather than redraws: calling `showTripRoute` again would rebuild every entity and, worse, clear `activeIndex`/`hoveredIndex`, so going back would drop the selection that lights the row and holds the day/night tint. `reframeRoute` returns false when no route is drawn, so the destination flight stays as the fallback for the one case that needs it — a stop detail opened on the home page before a trip exists. Also removes the pin from this path, since a labelless flight is what clears it. Measured across the round trip: day framing at heading 88.3/pitch -45.2/19232m, into the stop at 452m, back to heading 88.3/pitch -45.2/19238m and the same centre — 6m of altitude, no pin. Before: heading 0, 10615m, centre 24km off, one pin. `drawnDaysOf` now holds the "which days are on screen" rule that the camera framing, the height probe and the arc-apex reservation all need |
| Opening a stop from an itinerary row labelled it twice on the globe, and named it wrong | 2026-08-25 | 2026-08-25 | Claude | `useTripCamera.selectStop` passed `stop.name` to `flyToPlace`, and a label makes `flyTo` drop the red teardrop pin plus a Cesium label — on top of the `.marker-title-card` already naming that stop. `StopMarkerLayer` avoids this deliberately on the marker-click path ("a pin plus a card is one label too many"); the row path did not. **But dropping the argument alone would have been a regression, which is the part worth recording.** The declutter scan is first-come-first-served in visit order, so the *selected* stop routinely lost its own card to whichever neighbour came earlier — and co-located stops are the norm, not an edge case: every trip in the dev database has a day where a hotel is the transfer, the breakfast and the evening return, and Mumbai day 7 puts four stops on one coordinate. Measured before the fix: clicking "Crawford Market" left its own card `visibility: hidden` and its co-located predecessor "Private car to Crawford Market" holding the only card, with the pin's label the one readable name for the stop. Removing the pin there would have replaced a duplicate label with a wrong one. So the card had to become the real single authority first: the pointed-at stop (`hoveredIndex ?? activeIndex`) is now ordered to the front of the marker list, ahead of the day badges too — a deliberate reversal of the "clusters first" rule for this one card, since a badge names a group and gets pulled back into frame rather than dropped, while this card is now the only thing naming the stop the traveller asked for. Then the label came off the `flyToPlace` call. Verified against a running server by stashing and unstashing the change over an identical click: before, pin present and the wrong card visible; after, no pin, "Crawford Market" visible and lit, the predecessor yielding, and the camera pose byte-identical (452m altitude, stem top projecting to the viewport centre). Don't split these two edits |
| Opening a stop from an itinerary row never marked it selected on the globe | 2026-08-25 | 2026-08-25 | Claude | Clicking a stop's *marker* has always called `setActiveIndex` (StopMarkerLayer); clicking its *row* went through `useTripCamera.selectStop`, which flew the camera and opened the place detail and set nothing. Three behaviours hang off that index and all three were quietly absent from the row path: the row stayed lit only while the pointer was on it, the stop's marker and the arcs touching it took no `setEmphasis` amber, and the day/night tint fell back to daylight the moment the pointer left the row — so opening an 8:45pm stop from the list put the city back in daylight, which is how this was spotted. Fixed at the shared path rather than by threading a flat index down through `StopList` and `PlaceDetailPanel` (the "up next" list has the same gap, from a derived subset with no index to thread): `setActiveStop` on the map camera resolves a stop object against `routeStops` and is called once, in `selectStop`. Matched on name **and** coordinate **and** time, not coordinate alone — one coordinate legitimately carries several stops, a hotel being the transfer, the breakfast and the evening return, and for the tint specifically, picking the wrong one of those is the difference between 9am and 8:45pm. Reads a ref beside the state so the callback stays stable, since `selectStop` closes over it and that closure is handed to every row. A miss leaves the selection alone rather than clearing it |
| `/trip/[id]/print` printed 9 blank pages with the dev chrome on top | 2026-08-23 | 2026-08-23 | Claude | Two bugs behind one symptom, and the page-count being right (9, scaling with the trip) was the clue that the content was present but not painted. **The blankness was a stacking bug caused by the print block's own fix for it:** `.content-overlay { position: static }` unpinned the scroller correctly, but `z-index` applies only to *positioned* elements, so going static silently dropped its `z-index: 10` — and AppShell's globe container, still `absolute inset-0 z-0` with an opaque `bg-canvas` at the document's full 6629px, began painting over every line. `elementsFromPoint` under `emulateMedia({media:"print"})` returned that div at all four corners of the content. Fixed with `position: relative`, which keeps the stacking order while still flowing in normal order at auto height. **Second bug:** the chrome-hiding rule was a hand-listed set of four class names that had already missed the globe container — replaced with the structural `.app-shell > *:not(.content-overlay)`, since only the overlay ever holds a document and the list otherwise needs maintaining forever. `LlmTraceFab` mounts as a *sibling* of `.app-shell` (from `LlmTraceFabProvider` in the root layout, not inside AppShell) so it is out of that rule's reach and carries its own `print:hidden`, as does `DevInspectorOverlay`. Also reset `.app-shell`'s `bg-canvas` for print: Chrome drops it by default, but ticking "Background graphics" brought back two dark columns either side of the document. Verified by rendering real PDFs with the already-present `playwright` devDependency rather than trusting computed styles — the computed styles had been entirely correct while the output was blank |
| No Earth at all when Google's photorealistic tiles fail after the tileset is built | 2026-08-24 | 2026-08-24 | Claude | The route arcs, stems and day labels drawn over a blank white page — reported as "Earth is not appearing". `GlobeBackground` sets `scene.globe.show = false` once the photorealistic tileset is added, because those tiles *are* the ground, and the `try/catch` around it only covers **building** the tileset, which is a single fetch: the root tile. Everything after that was unguarded, so a root that succeeds while its children 502 (or hang) leaves the planet switched off with nothing put in its place. Reproduced in Chromium by letting the root through and failing the children — every itinerary overlay intact, hanging over white. Now the tileset is watched rather than assumed: `tileLoad`/`tileFailed` (public, unlike `tileset.statistics`) track when a tile last arrived; an 8s watchdog falls back if none ever does, and 6 failures with nothing arrived in 10s falls back too, which is the case where tiles were fine for an hour and the city just flown to cannot fetch one. The test is deliberately "has anything arrived lately", not "did anything fail" — failed tiles at the edge of a pan are routine and must never cost a working Earth its photography. The fallback is the flat OSM-imagery globe the no-token path already used, extracted to `showFlatImagery()` and now reachable from all three failure modes; the tileset is left in the scene, so if Google recovers the Earth simply becomes photographic again. Verified across four scenarios: healthy (381 Google tiles, 0 OSM, unchanged photoreal Kyoto), children failing (falls back, route legible on OSM), requests hanging with no error event at all (falls back on the watchdog), and a *slow but working* connection at 3s per tile (does **not** fall back — the false-positive that would quietly downgrade every slow connection to flat imagery) |
| A hover landing during the peek's lean-out sent the camera to a meaningless intermediate pose | 2026-08-24 | 2026-08-24 | Claude | `peekReturnRef` was cleared when the return flight *started*, so a row hovered while it was still flying found no saved pose and captured the camera mid-flight as its own "home". Every later lean-out then went to that arbitrary point — measured 4.5km from where the run of peeks had actually begun, on a Kyoto day. Latent for as long as the peek has existed and easy to miss, because it needs a hover inside the flight's own window; the deeper dives the ring model introduced fly for up to 1.8s and made it reproducible. Now cleared in the flight's `complete` callback, guarded by identity so a newer run's pose is never the one dropped. Found by reading the camera's own ECEF position before and after a hover run in Chromium — the marker layer's screen positions, which the first probe compared, disagree with the camera by hundreds of pixels even when it is byte-identical, because the declutter scan leaves stale transforms on the cards it hides |
| FeaturedPlans' card text column crushed to 144px between 768px and 1279px | 2026-08-22 | 2026-08-22 | Claude | Two grid rules stacked: the outer grid went two-up at `md` while each card already split text/photo at `sm`, so four columns crossed the viewport and a text column measured `(vw - 192) / 4` — 144px at `md`. The definition list survived that, which is why it looked shippable, but the title wrapped to three lines and the CTA label wrapped to two with its mark orphaned beside the second. Moved the outer two-up to `xl`: 272px there, against 208px at `lg` which fits the 200px CTA by 8px and is not a margin worth shipping. Between `md` and `xl` the cards are one per row with the internal split intact, so the column runs 348px to 604px. Every `md:` modifier moved with it — the shared borders, the internal gutters, the equal row heights — since each describes only the two-up arrangement |
| `/trips`' own `<h1>` was invisible on arrival | 2026-08-22 | 2026-08-22 | Claude | "My memories" rendered as nothing until the visitor scrolled. `useLineReveal` masks each line with GSAP's `mask: "lines"` (an `overflow: clip` wrapper) and drives it from a ScrollTrigger at the hook's default `start: "top 85%"` — a scroll-*in* threshold. This heading never scrolls in: the hero is `min-h-dvh` with `items-end`, so the h1 is pinned to the bottom of the first viewport, permanently below the 85% line. `gsap.fromTo` applies its `yPercent: 100` start state immediately, the trigger never fires, and the words sit clipped out of their own mask. Measured on arrival: **1440 puts the h1's top at 767px against a 765px threshold — a 2px miss** — and 375 at 709 against 690; a 40px scroll returned the transform to 0 and the headline appeared, which is exactly why it survived review, since it looked like a working scroll animation rather than a headline that was never there. Fixed with `start: "top bottom"` at this call site only, which fires on init for anything already on screen and still animates (sampled 26.0 → 3.3 → 0.02 → 0px across 250/500/900ms). A sweep of all four routes at both widths found no other parked line, so the hook's default is left alone — it is correct for the four beats the reader scrolls to |
| `/trips`' hero counted all trips but named the ends of only five | 2026-08-22 | 2026-08-22 | Claude | The subline read "6 trips remembered, from Mumbai to Paris" while a Lisbon card sat in the grid below it: the count came from `trips` and the range from `tiles`, which `slice(0, 5)` caps for the five-cell collage. One sentence, two referents, and the page contradicted its own opening line. Fixed by reading `first`/`last` off `trips`; `tiles` stays capped because that is a statement about the collage, which is decoration |
| `/trips` and `/profile` inherited the landing page's document title | 2026-08-22 | 2026-08-22 | Claude | Both tabs read "TripMate — Plan your trip" — the root layout's title, inherited because neither route set one. Wrong copy in the place a visitor sees most and can least ignore: several TripMate tabs open were indistinguishable, and a bookmark or shared link to the memories page advertised the trip wizard. `/trip/[id]` had already set the convention with `"<destination> · TripMate"`, so both follow it: "My memories · TripMate" and "Your travel profile · TripMate", each with its own description. Verified across all four routes in both engines |
| Two destructive controls on `/trips` were under the 44px target | 2026-08-22 | 2026-08-22 | Claude | The memory card's delete chip was 36px and both `ConfirmDialog` buttons were 37px — including "Delete trip", which permanently deletes an itinerary. The chip keeps its drawn 36px and reaches 44px through `before:-inset-1`, because that size was measured against the photograph and on a phone the control is permanently visible (`@media (hover: none)`), so a 44px disc would be a heavier object parked on every card; verified the hit area extends exactly 4px past the circle with the card link taking over cleanly beyond it. The dialog's buttons took `min-h-11` + `inline-flex items-center` and dropped `py-2` rather than keeping both, measuring 44–45px. Still short and deliberately untouched: `SiteFooter`'s two 31px links at 375, shared across three routes |
| `/profile`'s header sat flush against the first hairline, and two range utilities on its rows were inert | 2026-08-22 | 2026-08-22 | Claude | The ruled form column is `divide-y`, so each `Section`'s `py-5` puts every hairline in a symmetric 20px well — except the first, because the `SectionOpener` above it had no bottom padding at all. Measured 1px above the rule against 20px below, so the subline read as having fallen through the divider. Fixed with a 40px gutter on a wrapper at the call site, not inside `SectionOpener`, which the landing also renders: 40 rather than a matching 20 because this block is the page header and the boundary under it separates groups rather than sibling rows, and 40px is the `gap-10` the grid already uses. Separately, `Section` carried `first:pt-0 last:pb-0` and neither could ever match — the divide container's first child is the opener and its last is the save row, so no `<section>` is `:first-child` or `:last-child`. Deleted rather than made to work. Verified p-to-hairline 1px → 41.7px at 1440 and 41px at 375, all seven other wells unchanged at 20/20, Chromium and WebKit, against a production build |
| Nineteen controls on `/profile` are 32px tall against the app's own 44px target rule | 2026-08-22 | — | Claude | `GroupTypePicker` (4), `ChoicePicker` for energy and crowds (6) and `InterestPicker` (9+3) all render `py-1.5` chips at 32px, short of the 44px DESIGN.md requires and asks to be reached with `min-h-11` plus horizontal padding. **Open deliberately, not overlooked.** All three are `HomeView`'s controls too — `InterestPicker` is also in the bench and backend consoles — so raising them redesigns the trip wizard's density on a surface nobody asked about, which is a change with its own review rather than a rider on a profile polish. The one undersized control `/profile` actually owns, the 20px "See all in My memories" link, was fixed in place to 44px. Also short and also shared: the footer's two 31px links at 375, and eight 28px star buttons in "What matters most" |
| Four controls had no hover or press feedback at all | 2026-08-22 | 2026-08-22 | Claude | "Refine with AI" on `/trip/[id]` — the app's most prominent secondary action — plus "Switch to 2D view", "Reset map to face north" and the mobile "Show the map" sheet handle. Each carried a `hover:bg-white/10` utility and a `transition` on a property that never moved. Cause: `.glass-control` sets `background` as an **unlayered** rule, and cascade layers rank unlayered author CSS above anything `@layer utilities` emits at any specificity — so the utility could not apply, and failed silently with the class present in the markup. Correlation across every visible hover target on the route was exact: carries a glass class → dead, does not → works. The same class had already eaten `focus-visible:ring-*` via `box-shadow`, and `DockedPanel` documents that case on the line directly above where its own dead hover sat. Fixed by defining `:hover`/`:active` beside the base rule in `globals.css`, and **darkening rather than lightening** — the one slate at 0.72 and 0.82, per the Darken-Never-Lighten Rule, since these float over terrain that is sometimes a snowfield. The `white/10` the utilities asked for was a rule violation before it was dead. `MapControls`' shared `buttonClass` was split into `buttonShape` + `buttonClass` + `glassButtonClass` rather than stripped, because its hover is correct for the two in-pill zoom buttons. Verified 0 dead of 8 controls at 1440 and 3 of 3 at 375, Chromium and WebKit, measured before→after on each |
| The hero's two "mutually exclusive" aspect branches both matched at exactly 3:5 | 2026-08-22 | 2026-08-22 | Claude | `min-aspect-ratio` and `max-aspect-ratio` are both *inclusive*, so at 375×625 both queries matched and emission order decided per property: the CTA took its portrait width (335px, full-bleed) while keeping landscape `position` and `z-index` (`relative`, z-4), floating 186px above the bottom of a hero it is meant to be pinned to. 375×624 and 375×626 were each individually correct, which is what hid it — and the component's own comment asserted that two mutually exclusive media queries cannot collide. Fixed with range syntax (`aspect-ratio<=3/5` / `aspect-ratio>3/5`), which shares no value; Lightning CSS downlevels it to `(max-aspect-ratio:3/5)` and `not all and (max-aspect-ratio:3/5)`, an exclusive pair. Portrait owns the boundary and owns *everything* on it, the two `<picture>` `media` attributes included, so the crop cannot disagree with the layout. Verified 18/18 coherent branches — 375/600/900 wide, each at one below, exactly at, and one above 3:5, in both engines, checking position, z-index, button width, section aspect, headline z-index, image crop and front-layer offset together |
| 5.3KB of the production stylesheet was class names lifted out of documentation | 2026-08-22 | 2026-08-22 | Claude | Tailwind v4's source detection reads every file in the project and does not care that a file is prose, so any token shaped like a utility becomes a real rule — including one quoted in a bug report *to explain that it was removed*. This log was the largest single source: the entry above about the stray plan-card hairline was itself emitting the broken override it describes, top-level and unconditional. Also found in `.tsx` comments and in `globals.css`. Making the repo's own docs convention — log every fix in detail, naming the classes — a standing leak into the shipped bundle. Fixed at the root with `@source not "../../**/*.md";` in `globals.css` rather than by rewording each mention, since rewording is a guard at every call site and nothing would remind the next entry. Nothing here is MDX and no markdown renders into the app, so the only candidates dropped are ones that exist solely in prose. Stylesheet 104,970 → 99,679 bytes. Prose inside `.tsx` and `.css` cannot be excluded the same way, so those name classes in words instead |
| `/trips`' two headings asked for bold and rendered semibold | 2026-08-22 | 2026-08-22 | Claude | `.font-display` sets `font-weight: 600` unlayered, which outranks the layered `font-bold` on both "Your memories start here" and "My memories" — verified `hasFontBold: true, computedWeight: "600"`. **Fixed by deleting `font-bold`, not by making it work:** 600 *is* the display step DESIGN.md specifies, so `.font-display` was already the intended weight and the utility was the mistake. Byte-identical render, one less claim the markup could not back. Same treatment for two inert `tracking-tight` sites on the wordmarks (`Navbar`, `BrandMark`), where `.font-display`'s own -0.06em is what the design wants. Left alone deliberately: 18 inert `font-semibold` sites beside `.font-display`/`.font-scene-display`, identical computed value, 18 files of churn for zero visible difference |
| A dead keyboard-reveal utility sat in front of the rule actually doing the work | 2026-08-22 | 2026-08-22 | Claude | The memory card's delete button carried a focus-visible opacity utility, dead against `.memory-card-delete { opacity: 0 }` (unlayered). Keyboard focus *did* still reveal the button — through `.memory-card-slot:focus-within .memory-card-delete` in `globals.css`. So the utility was dead code that looked load-bearing, and deleting the `:focus-within` rule while trusting it would have made the control permanently invisible to anyone not using a pointer, with the markup still appearing to cover the case. Fixed by removing the utility and naming `:focus-within` as the owner in both the CSS comment and the component's. Verified: focus takes computed opacity 0 → 1 in Chromium and WebKit with the utility absent from the class list |
| The top-right plan card carried a stray hairline above it at two-up | 2026-08-22 | 2026-08-22 | Claude | Pre-existing, surfaced by moving the two-up breakpoint. The horizontal rule was an unprefixed nth-child(n+2) border-top plus a nth-child(2) border-top-zero at the two-up breakpoint to lift it off card 2, which sits in row 1. **That override never applied.** Tailwind orders by utility rather than by variant, so the zero-width rule is emitted before the plain one — bytes 88175 and 88429 of this app's own stylesheet — and at equal specificity (`.class:nth-child(…)`, 0-2-0) the later rule wins whatever media query wraps the earlier one. Fixed by splitting the ranges instead of overriding: `max-xl:` for stacked, `xl:` for two-up, so neither rule has to beat the other. Verified `top=[0 1 1 1]` stacked and `top=[0 0 1 1] left=[0 1 0 1]` two-up, both engines. Same class of trap as the display utilities on the navbar's Profile link |
| The landing headline lost its last glyph and its period on a phone, reading "Somewher" | 2026-08-22 | 2026-08-22 | Claude | Not clipping, which is what it looked like and what I first reported. Every overflow measurement said the text fit — `scrollWidth - clientWidth` was 0 at every width from 320 to 640, and a canvas `measureText` cross-check put the run at 289px inside 335px of available width at 375px. Two of those measurements were worthless: `.line-reveal-line-mask` carries `overflow: clip`, which does not create a scroll container, so `scrollWidth === clientWidth` there *always* and can never report overflow. What actually covered the glyph was the hero's FRONT steppe layer at `z-3`, deliberately in front of the `z-2` headline for depth — found by `elementsFromPoint` over the last glyph, which named an `IMG` at `z-3` as topmost. On the portrait crop that layer's hillside rises on the right far enough to swallow the word's tail: measured at 375x812, front-layer box top y=299 against a headline spanning 306-358. Fixed by raising the headline to z-4 on the portrait branch only, joining its own subline which was already in front at all sizes. Deliberately not unconditional — on the landscape crop the horizon genuinely crosses only the letter bottoms, which is the intended effect and verified still intact at 768x1024 and 1024x768 |
| Flying to a stop left it blurry — tile refinement stalled at whatever level had loaded when the flight ended | 2026-08-19 | 2026-08-19 | Aryan | Introduced same-day by `requestRenderMode`. Refinement is a multi-frame conversation: a frame traverses the tree, requests the tiles meeting the current screen-space error, and only the *next* frame can request the level below once those parents land. The 60fps loop hid this entirely — the traversal just ran forever, so every view reached full detail eventually. The first fix drove it per arriving tile (`tileset.tileLoad` → `requestRender`), which is a *chain*: it survives only while tiles keep completing, and any gap in it — a request that errors, a frame whose traversal is waiting on a parent and asks for nothing, `skipLevelOfDetail` jumping levels — ends it permanently, because with the camera stopped nothing exists to restart it. Replaced with a per-frame predicate on `postRender`: request another frame while `tileset.tilesLoaded` is false ("all tiles that meet the screen space error **this frame** are loaded"). It cannot stall, since it is re-read every frame rather than chained off an event, and it goes false again by itself whenever the LOD controller raises the detail ceiling or the camera lands somewhere new. Reads `globe.tilesLoaded` instead on the fallback imagery path, and deliberately **never** on the photorealistic one: `globe.show` is false there, a hidden globe is not updated, and a predicate that can never become true is a render loop that never ends. Verified against the shipped Cesium source that `Scene.render` clears `_renderRequested` *before* `preRender`/`render` and raises `postRender` last, so a request made from `postRender` schedules the next frame rather than being cleared unread |
| Day tab strip stopped following the selected day, hiding days 5+ on a long trip | 2026-08-19 | 2026-08-19 | Claude | The scroll-into-view effect measured `tab.offsetLeft`, which is relative to the nearest **positioned** ancestor — and wrapping each tab in a `relative` drop zone (for the drag work) silently made that wrapper the offset parent, so every tab reported ~0 and the strip scrolled to the start. The arrows still changed the day; the strip just never revealed it, so a 7-day trip looked like a 4-day one. Now measured from `getBoundingClientRect()` deltas, which don't care about layout ancestry, and the `relative` is gone from the wrapper (a ring paints fine without it) |
| Drag handles didn't appear when the day's pencil was clicked | 2026-08-19 | 2026-08-19 | Claude | `DayHeader` kept `isEditing` in private state and merely *reported* it upward, so the flag that draws the rename form and the flag that draws the drag handles were two separate pieces of state for one intent, free to disagree. `isEditing` is now a controlled prop owned by `ItineraryCard` (the component that needs it for both purposes), with local state kept only as a fallback for callers that don't pass it. Also adds an explicit **Done rearranging** button: leaving the mode previously meant finding the rename form's tick or cross, which says nothing about the handles |
| In Focus Mode, dragging over a stepper arrow flipped the previewed day back and forth | 2026-08-19 | 2026-08-19 | Claude | The arrows' droppable ids were **relative to the day on screen** (`day:${previewIndex - 1}`), so the target under a stationary pointer changed meaning the moment a dwell switched days: the left arrow stopped being "day 1", the collision then resolved to the opposite arrow, and the view oscillated. Fixed structurally — the arrows are click-only steppers again, and the numbered day chips (ids absolute, `day:<i>`, so they name the same day before and after a switch) are the only cross-day drop targets; they now render for every multi-day trip, not just 3+. Plus a 900ms post-switch cooldown, which also gives the pointer time to travel down into the day that just opened. The itinerary card was never affected because its tab ids were already absolute |
| Dropping a stop on some day targets silently did nothing (e.g. day 1 → day 2 worked, day 2 → day 1 did not) | 2026-08-19 | 2026-08-19 | Claude | Three compounding causes. (1) The day tabs and stepper arrows were registered with `useDroppable({ disabled: !dragging })`, but dnd-kit measures drop rects **when a drag starts** — a target that only comes into existence on the render triggered by `onDragStart` is never measured and can never be hit. Only one arrow is enabled at a time, which is what made it look directional. Now registered unconditionally; only the styling depends on drag state. (2) `closestCenter` compared the dragged row's centre against every droppable, and a tall stop row's centre is nearer than a 32px arrow's, so the list won the collision even with the pointer over the arrow — replaced by `pointerWithin` with `closestCenter` as the keyboard-drag fallback. (3) `MeasuringStrategy.Always`, since hover-to-open swaps out every row mid-drag |
| `body { font-family: Arial }` in `globals.css` overrode the Geist font that was loaded via `next/font`, so the custom font never actually rendered | 2026-08-05 | 2026-08-05 | Aryan | Removed the hardcoded override, wired `font-family` to the `--font-sans` variable instead |
| `eslint-plugin-react-hooks`'s `static-components` rule flagged `WeatherIcon` for "creating a component during render" | 2026-08-05 | 2026-08-05 | Aryan | Was picking a Lucide icon component reference and using it as a dynamic JSX tag; refactored to a `switch` returning an explicit icon element per condition instead |
| `TripDashboard`'s right (list) panel rendered as a sliver a few pixels tall instead of half the viewport | 2026-08-05 | 2026-08-05 | Aryan | Its `absolute inset-0` root had nothing to size against — the wrapping `motion.div` in `page.tsx` had `relative z-10` but no explicit height, and CSS auto-height ignores out-of-flow descendants. Changed the wrapper to `absolute inset-0` itself |
| Adaptive header contrast stayed white-on-light-terrain | 2026-08-05 | 2026-08-05 | Aryan | Two compounding issues: (1) sampling the header bar's exact horizontal center landed on the map/panel seam once the dashboard splits the screen, blending both sides together — moved the sample point to sit under the title specifically; (2) the luminance threshold (150) was miscalibrated against this imagery, where empirically deep space ≈ 4 and any visible terrain/ocean ≈ 140-190 — lowered to 110 |
| Cold-loading `/backend` and clicking the TripMate wordmark left the app with no globe for the life of the tab | 2026-08-20 | 2026-08-20 | Claude | `GlobeBackground`'s construction effect had `[]` deps and gated on `isGlobeHiddenRoute(pathname)` captured at first render, so the skip-boot decision was permanent. Its own comment claimed `/backend` had "no inbound or outbound links to the rest of the app" — but `Navbar` renders an unconditional wordmark `<Link href="/">` on every route. `MapControls`' parallel `import("cesium")` *did* re-run on `[pathname]`, so it pulled all 2.3MB for a component that then rendered `null` forever. Fixed structurally by replacing the path list with a demand-driven latch |
| Navigating `/trip/[id]` -> `/profile` left the previous trip's route arcs and glow pools floating in the settings form's margins | 2026-08-20 | 2026-08-20 | Claude | `.map-chrome-hidden` hides `.map-controls` and `.stop-marker-layer` — two DOM layers — but arcs are Cesium entities *inside* the canvas, and `ProfileForm` never called `resetToHome()`. Closed for every surface at once by giving the canvas `visibility: hidden` wherever `globeWanted` is false, rather than adding a reset per call site |
| `eslint-plugin-react-hooks`'s `set-state-in-effect` rule flagged `LlmTraceFab`'s list/detail fetch effects | 2026-08-06 | 2026-08-06 | Aryan | List view: was calling `setLoading(true)` redundantly at the top of a mount-once effect — initial state was already `true`, just deleted the line. Detail view: was resetting `trace`/`error` to `null` synchronously before each fetch when `id` changed — switched to `key={selectedId}` on the component instead, so switching traces remounts it with fresh state rather than an in-effect reset |

## Eliminated

<details><summary>`.profile-glass` — Eliminated 2026-08-20 (Claude)</summary>

A cobalt/teal frosted-glass panel wrapping `/profile`'s form, built to separate a settings form
from the live globe behind it. The Mounted-Surface Gate took the globe off this route, at which
point the blur was a blur of nothing — so it went opaque and kept its tint, border and shadow. That
left a card drawn around content for no reason other than that a card used to be needed there.

Deleted with the page's redesign, along with the DESIGN.md rule that defended it. The
accent-retint argument that rule recorded (don't switch `--accent` to brass mid-app on a route
reachable from the amber result view) is still live and is preserved in the `.blue-hour-scene`
entry, which is what it was arguing against.
</details>

<details><summary>Globe idle auto-rotation — Eliminated 2026-08-20 (Claude)</summary>

A continuous slow drift on `scene.postRender`, eased out after 4s idle over 2.5s, paused on
`pointerdown` and resumed on `pointerup`, permanently locked by `stopAutoRotate()` on the first
camera flight and only ever unlocked by `resetToHome()`'s flight-complete callback. About 60 lines
in `GlobeBackground`, plus call sites in `mapCamera` (x3), `MapControls`, and comments in
`HomeView`, `useStopTour`, `DESIGN.md` and `docs/mac-safari-testing.md`.

Unreachable once the globe was gated to two surfaces. Both of them command the camera on arrival —
`TripView` flies to the trip, `showDayRoute` frames the day — so `rotating` went false on the first
flight and stayed false: `startAutoRotate` was only reachable from a flight-complete callback that
cannot fire while the render loop is paused, and `pauseSpin`/`resumeSpin` were gated on a `locked`
flag nothing could clear. The drift was never visible again.

The one part that needed proving rather than reasoning was `wake()`, the `wheel` handler, whose
docblock claimed it was "load-bearing under `requestRenderMode`" because `postRender` only fires on
frames that rendered. Tested with it removed, on a scene confirmed asleep (0 painted frames in
1.5s): 8 wheel notches took the camera from 4561m to 1236m across 30 painted frames, and a drag
moved it and returned to 0 frames within 2.5s of release. Cesium requests its own renders for both
gestures; `wake()` was only ever restarting the drift.

Also removed the older **Do** rule that told every camera-dependent probe to call
`stopAutoRotate()` first — the hazard it guarded (a `setView` pose being rotated away during the
settle window, which once corrupted a tile-detail probe into reporting 2,054m of geometric error at
900m) no longer exists. The replacement rule is about the scene being *asleep*, not about it moving.
</details>

<details><summary>Per-page duplicated header markup — Eliminated 2026-08-05 (Aryan)</summary>

Each of the three pages (`/`, `/trips`, `/trip/[id]`) had its own inline
`<div className="flex items-center justify-between">` header block with
slightly-diverging classnames. Replaced by the shared `PageHeader` component.

</details>

<details><summary>Single-step "fill form → generate immediately" flow — Eliminated 2026-08-05 (Aryan)</summary>

Submitting the trip form used to call `/api/itinerary` directly. Replaced by a
two-step flow: submitting the form now shows the `PreferenceStep`, which is
what actually triggers generation (or skips straight to it with no
preferences, functionally identical to the old behavior).

</details>

<details><summary>Plain-text weather label in DayList — Eliminated 2026-08-05 (Aryan)</summary>

Each day card used to show `day.weather` (the model's free-text summary) as a
plain `<span>`. Replaced by `WeatherPopover`, which shows a dynamic icon +
temperature range and expands to a detail panel — falling back to the same
plain text only when structured `weatherDetail` isn't available.

</details>

<details><summary>Plain white 2-column trip form + `ItineraryMap` on the home page — Eliminated 2026-08-05 (Claude)</summary>

The home page (`/`) used to open straight onto a plain white form, and render
results with the Leaflet-based `ItineraryMap`. Replaced by the 3D Cesium globe
landing/dashboard experience: `TripSearchForm` (glass form over the globe) →
`PreferenceStep` (now also glass) → `TripDashboard` (globe + itinerary panel
split view). `ItineraryMap`/Leaflet is untouched and still active on
`/trip/[id]` (saved-trip view) — this only replaced the home page's map.

</details>

<details><summary>`/llm-trace` + `/llm-trace/[id]` pages, and the "LLM trace" header nav link — Eliminated 2026-08-06 (Aryan)</summary>

The trace viewer was a separate section of the app: a nav link in `PageHeader`
to a `/llm-trace` list page, which linked to `/llm-trace/[id]` detail pages.
Replaced by `LlmTraceFab` — a persistent bottom-right icon (mounted once in
the root layout, so it's on every page without a nav link) that expands into
a panel showing the same list/detail views inline, with a collapse button
back to just the icon. The underlying data/API routes
(`/api/llm-traces`, `/api/llm-traces/[id]`) are unchanged — this only changed
how they're presented. See `.claude/skills/dev-analytics-fab/SKILL.md` (what
to put in a widget like this) and `.claude/skills/floating-widget-ui/SKILL.md`
(the FAB/panel UI pattern itself) for the general patterns this follows.

</details>
